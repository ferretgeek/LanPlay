# LanPlay 审查、修复与验收清单

更新时间：2026-08-08  
适用范围：`LanPlay/`、`lanplay-scraper/`  
当前交付：`LanPlay-v1.0.4-release.apk`（`versionCode 5`、`versionName 1.0.4`）

状态说明：

- `已通过`：已有本次源码、自动化、模拟器或真实 SMB 证据。
- `功能通过／性能未通过`：功能正确且未崩溃，但模拟器性能未达到实机门槛。
- `未验证`：缺少荣耀目标机、批准样本或外部写入授权，不能据静态检查宣称通过。

## 一、程序结构与关键路径

- Android 客户端使用 Kotlin、Jetpack Compose、Media3 与 VLC。SMB 访问由 SMBJ 和本地媒体代理完成，播放状态通过 MediaSession/前台服务维持；设置、加密数据库、备份恢复、刮削元数据、隐私模式与应用锁分别有独立数据和 UI 路径。
- PC 刮削器位于 `lanplay-scraper/`，负责扫描媒体、匹配与生成供客户端增量导入的索引；检查点和完整索引具有不同语义，避免中途中断导致客户端误删历史数据。
- `LanPlay/tools/verify.ps1` 覆盖连接、扫描、共享发现、64 位偏移读取、首帧、Seek、断网恢复、长跑、VLC 兼容、备份、锁屏、桌面伪装、隐私通知等动态门禁。
- 基准测试模块生成 Baseline/Startup Profile，并提供不读取私人媒体的合成画廊。正式版无法进入该隐藏测试路径。

## 二、已确认并修复的问题

### 既有审查项

| 编号 | 问题与修复 | 状态 |
|---|---|---|
| A1 | 回收站移动视频成功、字幕失败时会误报整体失败且本地记录可能未清理；现返回结构化结果，视频成功即清理关联数据，失败项可查看和重试。 | 已通过 |
| A2 | 首次刮削、索引损坏或丢失时无法区分检查点与最终索引；现显式标记完整性，不完整或旧格式索引只增量导入，不删除陈旧记录。 | 已通过 |
| B1 | Media3 调整字幕偏移会重开视频；现 SRT/VTT 使用基础时间轴并在播放端实时偏移，复杂字幕继续走 VLC。 | 已通过 |
| B2 | 异步主线程操作可能落到已经替换的新播放引擎；现调用时捕获目标实例，并在执行前校验。 | 已通过 |
| B3 | 本地代理超过 4 个会话会立即失败；现使用有界可等待许可，并覆盖成功、错误与释放路径。 | 已通过 |
| B4 | 修改任意服务器会关闭所有 SMB 连接；现只关闭对应辅助连接，不打断其他服务器上的播放。 | 已通过 |
| B5 | 清理失败后会丢失预览项和大部分错误；现保留失败记录、进度、文件与原因，可只重试失败项。 | 已通过 |
| B6 | 雪碧图设置未接入播放器；现首次播放后台生成，拖动和快进时异步展示，失败不阻塞播放。 | 已通过 |
| B7 | 双指字幕偏移未实现；现按同向竖移与指间距变化区分偏移和缩放，并锁定单次手势。 | 已通过 |
| B8 | 返回播放器时未按设置淡出；现启用时先黑场淡出再退出，关闭时立即退出。 | 已通过 |
| B9 | 快进灵敏度预设包含不适合手势的长时档；现统一为 30、60、90、120、180、300 秒，并兼容既有自定义值。 | 已通过 |
| B10 | 音量与亮度手势缺少灵敏度和安全上限二次确认；现补齐设置与完整状态机。 | 已通过 |
| B11 | 批量删除缺少确定进度、失败明细与精确重试；现显示完成数、当前文件和失败原因并保留失败选择。 | 已通过 |
| B12 | 播放服务每秒重建通知；现只在会话或状态变化时更新，进度交由 MediaSession 推算。 | 已通过 |
| B13 | 隐私模式仍解码海报并实时模糊；现不加载原图，使用不可逆占位表面，需要的入口保留临时揭示。 | 已通过 |
| C1 | 显式停止后仍保留预加载文件头；现仅连续切集保留，停止、删除、错误与销毁立即释放。 | 已通过 |
| C2 | 单集循环的“上一集”会移动索引；现上一集和下一集均保持当前项目。 | 已通过 |
| C3 | 安全窗口曾只在 release 开启；现所有正常构建默认开启，测试画廊仅对可调试或 shell profile 构建开放。 | 已通过 |
| C4 | 错误日志每次写入都会重写整个文件；现日常追加，超过阈值才批量压缩。 | 已通过 |
| C5 | 长 SMB 打开操作可能阻塞停止或切换；现显式跟踪和取消任务，旧任务不能提交状态。 | 已通过 |
| C6 | 续播确认框出现前可能闪过恢复位置画面；现确认前保持暂停和黑场，选择后才淡入。 | 已通过 |
| C7 | 物理音量键突破软上限后的解锁状态保留过久；现松键立即复位，下次必须重新确认。 | 已通过 |
| V1 | NanoHTTPD 在客户端断开时可能未关闭数据流；现代理输入流有空闲看门狗与幂等释放。 | 已通过 |
| V2 | 已核实 SMBJ 0.14 的连接路径会使用项目注入的 SocketFactory，现有连接超时真实生效。 | 无需修改 |
| V3 | 进度条长按检测与 Slider 竞争事件；现使用不消费事件的统一观察器，Slider 保持唯一拖动消费者。 | 已通过 |

### 本轮实测发现

| 编号 | 实际问题 | 修复与结果 | 状态 |
|---|---|---|---|
| D1 | Baseline Profile 和 Macrobenchmark 直接启动未导出的 `MainActivity`，设备上会被系统拒绝。 | 统一通过导出的 `.LauncherAlias` 启动，Profile 生成与 2 项宏基准均通过。 | 已通过 |
| D2 | 隐藏性能画廊的判定晚于窗口初始化，正式版额外参数与安全窗口边界不够可靠。 | 在窗口附着前完成判定，并只允许 debug/profileable 构建；正式版实测参数被拒绝且仍进入首次引导。 | 已通过 |
| D3 | debug 播放测试钩子在全新安装上未完成首次引导，播放器拿不到 Surface，导致自动化首帧假失败。 | 测试入口先标记引导完成；干净安装和已有数据两条 UI 路径均通过。 | 已通过 |
| D4 | 本地媒体代理的跨窗口 Seek 会把辅助窗口误计为主窗口重置，导致统计归类错误。 | 新建辅助窗口时同步重置主统计窗口；跨窗口识别结果恢复正确。 | 已通过 |
| D5 | Seek、重连、长跑验证脚本会把首帧前采样计入停顿，并可能按事件顺序错配目标。 | 增加首帧前置条件、按目标时间关联结果、精确过滤系统编解码器记录；30 分钟结果重新审计为首帧后 0 次停顿。 | 已通过 |
| D6 | 备份 JSON 因序列化默认值省略而没有显式写出 `version=2`，不满足稳定格式约定。 | 备份编码启用默认值输出，并新增序列化回归测试；导出、系统选择器导入和临时文件清理通过。 | 已通过 |
| D7 | 整理功能压力测试生成的超长标签超过清理钩子的匹配范围，导致测试数据残留。 | 写入和清理统一执行标签/备注长度规范化，完整写读清理通过。 | 已通过 |
| D8 | Gradle 依赖验证元数据缺少 BOM/POM 校验和，严格构建可能失败。 | 按 Maven Central 原件补齐校验和；严格依赖验证下单测、lint、Profile 和 release 构建通过。 | 已通过 |
| D9 | 播放会话进入非空 UI 分支后，异步启动服务时仍以 `session!!` 二次读取可变化状态；停止播放竞态下存在低概率空指针窗口。 | 分支入口固定为不可空会话快照，异步服务标题与播放器均只使用该快照；重新执行单测、lint、release 和 Connected UI 门禁通过。 | 已通过 |
| D10 | 小米 HyperOS 3 的 `dumpsys media.metrics` 格式与 AOSP 不同，且设备同时存在主包、测试包和 Baseline Profile 包时，验证脚本可能取到错误 UID，造成硬解记录假缺失。 | 主包 UID 改为精确包名匹配，并同时支持 AOSP 与 HyperOS 3 Android 16 的媒体指标格式；4K/1080p 均正确识别高通硬解码器和真实分辨率。 | 已通过 |
| D11 | Media3 Seek 会在旧长 Range 尚未释放时打开新长 Range；代理把新请求固定分配到 4 MiB 辅助窗口，即使目标块已在 48 MiB 主缓存内，窗口内 Seek 仍发生网络读取。 | 为读取器增加有效期与主读取器所有权；当新长 Range 的目标块已缓存时原子交接主窗口并失效旧读取器，索引类短 Range 继续使用辅助窗口。小米实测窗口内由秒级降至 93 ms，窗口外 391 ms。 | 已通过 |
| D12 | Seek 验证可能在主缓存尚未达到目标水位时发起，无法区分缓存缺陷与预读未完成。 | 验证器先等待 35 秒缓冲水位，再执行窗口内/外 Seek；预读管线在互斥区内校验读取器有效性，避免交接后的旧请求继续消费缓存。 | 已通过 |
| D13 | 单轮同时运行 release lint 与 R8 时，Gradle 的 512 MiB Metaspace 上限耗尽。 | Metaspace 上限调整为 768 MiB，正式门禁用单 worker 串行执行；单测、lint、R8、资源收缩、签名和 release 构建完整通过，峰值并发反而更低。 | 已通过 |

## 三、实际验收结果

| 门禁 | 本次证据与结论 |
|---|---|
| Python 刮削器 | Python 3.11 虚拟环境下 22/22 单元测试通过，`pip check` 通过。 |
| PowerShell | 3 个 PowerShell 脚本全部通过 AST 解析。 |
| Android 单元测试 | 31/31 通过，0 失败、0 跳过；Kotlin 编译通过。 |
| Android lint | `lintRelease` 通过，0 error；30 条为依赖版本、KTX、兼容分支等非阻塞建议。 |
| Release 构建 | R8、资源收缩、签名与 `assembleRelease` 通过；最终串行门禁使用 1 个 worker，项目上限仍为 2，禁用并行和常驻 daemon。 |
| 静音调试 | 小米实机全程保持 `STREAM_MUSIC` 静音且扬声器音量为 0；300 次随机交互后再次核对仍为 0。 |
| 敏感信息 | 受检源码、脚本、模板和文档未发现真实 SMB 参数、密码或私人媒体信息。 |
| 模拟器清理 | 已删除此前 3 个 AVD 及旧 API 36 系统镜像；当前只保留 `LanPlay_HONOR_WIN_RT_API36`。 |
| 荣耀近似镜像 | API 36、1272×2800、560 dpi、120 Hz、4 核、6 GB 名义内存、8 GB data；Google APIs x86_64 带 ARM 转译。它用于尺寸和通用 Android 16 回归，不等同于 MagicOS 或 ARM 实机。 |
| 小米 13 实机 | 2211133C、Android 16 / HyperOS 3、arm64、Snapdragon 8 Gen 2；1080×2400、440 dpi，实际 120 Hz。作为通用 ARM、性能、触控和布局证据，不替代荣耀 MagicOS 专属门禁。 |
| 连接与发现 | 小米实机上的 SMB 3.1.1 连接、目录列举、局域网扫描和共享发现通过；扫描发现 2 台主机用时 1,235 ms，共享发现 2 项用时 260 ms，0 错误。报告不记录真实地址或名称。 |
| 64 位文件路径 | 大于 24 GB 的批准样本在 0、4 GB、4 GB+1、12 GB 和文件末尾的偏移交叉校验一致。 |
| Media3 播放 | 小米 1080p 使用 `c2.qti.avc.decoder`：一次完全冷启动为 2,085 ms（高于 2 秒门槛 85 ms），随后两轮通过，最新 1,737 ms；1920×1080、丢帧 0、停顿 0。4K 使用 `c2.qti.hevc.decoder`，3840×2032 首帧 1,124 ms、丢帧 0、停顿 0。 |
| SMB 吞吐与调优 | 小米 300 MiB、6 通道实测 31.89 MiB/s。2,048 KiB/6 通道吞吐峰值 35.19 MiB/s，但独立 4K 首帧退化到 2,465 ms；为首帧和稳定性保留 512 KiB/6 通道默认值。 |
| 断网恢复 | 通过 USB 控制真实 Wi-Fi 断开/恢复，应用产生并处理 36 次实际重连事件，恢复到 7,832,139 ms 播放位置并继续 `PLAYING`，无崩溃。 |
| Seek | 修复后小米实测窗口内 93 ms（门禁 <200 ms）、窗口外 391 ms（门禁 ≤1.5 s）；跨窗口分类正确，大于 4 GB 位置继续播放，12 项检查全部通过。 |
| VLC 兼容 | 小米明确切换到 VLC 原生路径，1080p 首帧 1,032 ms、1920×1080、`libvlc hardware`，持续 `PLAYING` 且 0 错误；结束后恢复 Media3。 |
| 30 分钟长跑 | 小米真实 4K SMB 长跑 901 个采样，43,423 帧、丢帧 0、停顿 0、最小稳定缓冲 30.7 秒、重连 0、崩溃 0；活动刷新率 120 Hz，扣除系统后的调试网络内存约 268 MiB。 |
| 私密播放状态 | 小米画面抓取、PiP、后台位置推进、前台服务、通知和系统 MediaSession 暂停/恢复全部通过；通知不暴露私人标题。 |
| 数据功能 | 小米上的书签、标签、备注写入/读取/清理通过；HyperOS 系统文件选择器完成备份导出和导入往返，JSON 显式为 version 2、不含密码字段，测试备份已删除。 |
| 恢复能力 | 在小米一次性空数据安装上同时破坏主库并清理 WAL/SHM，重试仍安全停留恢复页；确认后旧数据库和原密钥成组归档并建立干净数据库。未对用户真实数据执行破坏操作。 |
| 应用锁与桌面伪装 | 重启后锁定、临时 PIN 解锁、关闭锁定以及工具别名切换/恢复均通过。 |
| Connected UI | 小米已有数据与清空数据后的首次引导路线通过；默认恢复测试按安全设计跳过，随后在明确的一次性损坏数据上单独执行并通过。 |
| 实机视觉 | 小米 13 在 130% 字号下检查合成画廊的竖屏与横屏实际渲染，双列布局、标题、辅助信息和安全区无溢出或遮挡；测试后恢复原字号和旋转设置。 |
| Baseline Profile | 最终生成 16,375 条规则，并生成 Startup Profile；相对旧版增加 3,629 条、删除 747 条。 |
| Macrobenchmark | 冷启动 5 次中位 947.9 ms；合成画廊帧 CPU 时间 P50 31.6 ms、P95 39.6 ms。仅为模拟器趋势证据。 |
| 正式版冷启动 | 小米最终 APK 强制停止后 20/20 启动成功：最小 146 ms、平均 188 ms、P95 211 ms、最大 214 ms（系统 `am start -W` 的 `TotalTime`）。 |
| 正式版压力 | 小米执行 `RUNNING_CRITICAL` 后进程存活；禁用系统键和应用切换的 300 次定向随机事件完成，系统退出记录中的 Java 崩溃、原生崩溃和 ANR 均为 0。 |
| 正式版加固与升级 | 先安装 1.0.3，再以 `adb install -r` 原位升级到 1.0.4 成功；两版签名一致。1.0.4 非 `DEBUGGABLE`、非 `profileableByShell`，APK Manifest 与 DEX 均不存在 `TestHookReceiver`，`run-as` 和外部直启内部 Activity 被拒绝，显式性能画廊参数无效。 |
| 隐私截图 | 小米上的最终 release 系统截图为整屏纯黑，`FLAG_SECURE` 生效。 |

## 四、仍需荣耀目标机或额外授权的门禁

| 验收内容 | 当前限制 | 状态 |
|---|---|---|
| MagicOS 10 后台策略、荣耀 2K+ 物理屏、MagicOS 专属触控/热控与 VLC 字幕 Surface | 小米 13 已覆盖通用 ARM、120 Hz、触控、横竖屏、后台播放和硬解路径，但不能替代荣耀 ROM、2K+ 面板和 MagicOS 厂商策略证据。 | 未验证 |
| ASS/SSA、VobSub、SMI 等复杂字幕及字幕 Surface 长跑 | 当前批准配置中没有相应字幕样本；未把测试文件写入私人共享。 | 未验证 |
| 回收站/删除对真实 SMB 文件的端到端操作 | 会修改真实外部文件，当前没有获批的一次性样本；只验证了单元、静态和非破坏路径。 | 未验证 |
| 真实旧版 SQLCipher 数据库迁移、掉电中断与恢复 | 小米已通过人为持续损坏后的归档重建，但真实历史库迁移和掉电中断仍需要脱敏旧库或专用样本。 | 未验证 |
| TalkBack 完整操作路径 | 未在用户日常手机上擅自启用会改变全局交互方式的无障碍服务；现有语义测试不能替代真人 TalkBack 验收。 | 未验证 |
| 荣耀目标机最终性能 | 小米已通过 4K、120 Hz、Seek、首帧、长跑和内存门禁；荣耀目标机上的同项数据仍需在手机可用时复测。 | 未验证 |

## 五、交付物

- 当前 APK：`LanPlay-v1.0.4-release.apk`
- 文件大小：52,515,478 字节
- APK SHA-256：`e6b8221015a204707decfdf8b4ce4d2b033dbb545b010b463e28906b2f9bce79`
- 签名：APK Signature Scheme v2 有效，单一 RSA 4096 签名者
- 证书 SHA-256：`a9ea9b29632edcf24363b45cf18664281b960b44bf25d4e89042b7467e6288d6`
- `LanPlay-v1.0.3-release.apk` 与 `LanPlay-v1.0.2-release.apk` 暂时保留作为可回滚旧包；它们不是当前交付版本，1.0.3 已实机验证可原位升级到 1.0.4。
