# LanPlay 播放器技术规格

> 配套文档：[需求文档.md](./需求文档.md) · [设计系统.md](./设计系统.md)
> 版本：v1.4 · 2026-07-29
> 目标设备：荣耀 WIN RT · 骁龙 8 Elite · Android 16 · **实际可用刷新率 120Hz**

本文件收录播放引擎的全部深度技术规格：SMB IO、解码、音频链路与蓝牙补偿、手势系统。这些是"能用"和"好用"的分界线，也是现成播放器做砸的地方。

> **v1.3 统一约束：** 代理仅监听 `127.0.0.1` 并使用随机 token 与 64 位 Range；APP 不访问公网；播放视频层不做模糊；所有音频焦点丢失/duck 请求均暂停且不自动恢复；VobSub `.idx/.sub` 只在私有会话临时目录成组存在并随会话清理。若后文章节存在不同表述，以本条和《需求文档》v3.3 的“当前契约”为准。本文是规格，不是审查结论；实现状态必须从当前源码和动态证据独立确认。

---

## 目录

- [1. SMB IO 层](#1-smb-io-层)
- [2. 双内核策略](#2-双内核策略)
- [3. 解码性能最大化](#3-解码性能最大化)
- [4. 音频链路与蓝牙延迟补偿](#4-音频链路与蓝牙延迟补偿)
- [5. 手势系统](#5-手势系统)
- [6. 刷新率适配](#6-刷新率适配)
- [7. 竞品借鉴清单](#7-竞品借鉴清单)
- [8. 调试面板](#8-调试面板)

---

## 1. SMB IO 层

> 卡顿成败全在这一层。带宽有 8 倍余量、编码是最好解的 H.264 8bit、SoC 是骁龙 8 Elite——现成播放器还卡，问题只可能在数据怎么读。

### 1.1 为什么走本地 HTTP 代理

要**同时喂两个内核**。Media3 用 `DataSource` 接口，libVLC 用自己的 IO 回调，两套完全不同。在 IO 层统一成 HTTP（两个内核都原生支持 HTTP + Range）后：

- 切换内核不改 IO 层任何代码
- HTTP `Range` 天然表达 seek 语义
- 预读、缓冲、限速、统计集中在一处实现和调优
- 回环（127.0.0.1）传输开销是内存拷贝级别，可忽略

### 1.2 代理设计要点

| # | 设计点 | 具体要求 |
|---|---|---|
| 1 | 监听地址 | 仅 `127.0.0.1`，端口动态分配，**不监听 0.0.0.0** |
| 2 | 会话令牌 | 每个代理会话生成高熵随机 token；会话内可用于多个 Range 请求，`release()` 后失效，不是单请求一次性 token |
| 3 | 明文回环 | 网络安全配置仅允许 `127.0.0.1` 与 `localhost` 的 cleartext；代理实际固定绑定并生成 `127.0.0.1` URL |
| 4 | Range 支持 | 完整实现 `Range: bytes=start-end`、`206 Partial Content`、`Content-Range`、`Accept-Ranges: bytes` |
| 5 | **64 位偏移** | 所有文件偏移使用 `Long`。素材最大 12.3 GB，用 `Int` 必在 4 GB 处崩 |
| 6 | 读块大小 | 默认 **512KB**，可配 512KB / 1MB / 2MB；当前候选版本的最优值仍需目标机重测 |
| 7 | **流水线并发** | 同时在途 **4~8 个** read 请求。服务端 `Smb2CreditsMax = 2048`，绰绰有余。**这是打满带宽的关键** |
| 8 | **前向预读** | 环形缓冲区，前向预读 **48 MB**（≈ 最高码率下 36 秒时长），可配 16 / 32 / 48 / 64 MB |
| 9 | 窗口内 seek | 目标偏移落在已缓冲窗口内 → **直接返回缓存，零网络请求**，瞬时完成 |
| 10 | 窗口外 seek | 丢弃缓冲重新预读，但**复用同一 SMB Session 和文件句柄**，不重连不重认证 |
| 11 | Session 所有权 | 连接按 `(PLAYBACK/AUX, canonical target identity)` 隔离，lease 携带 generation；失效必须校验 expected generation，旧代不能关闭新连接 |
| 12 | 断线重连 | 指数退避（0.5 / 1 / 2 / 4s，最多 5 次），成功后从**断点偏移**继续，播放器无感 |
| 13 | 文件句柄缓存 | 播放期间保持打开，避免反复 open / close |
| 14 | **独立读通道** | 元数据探测/缩略图/下一集预载走 AUX identity holder；仍共享设备网络与进程预算，不能表述为绝不争用 |
| 15 | **有界资源** | HTTP executor、代理会话、预读队列、旁路字节和下一集预载均有上限；取消/异常路径必须归还预算 |
| 16 | **并行 Range 隔离** | ≤40 MiB 容器索引走有界分片旁路；同时出现的更大长 Range 使用独立 4 MiB 轻量窗口和独立文件句柄，不驱动主播放窗口；非文件尾短读必须失败而非补零 |
| 15 | 统计埋点 | 实时暴露吞吐 / 缓冲水位 / 在途请求数 / 重连次数 / 命中率 → 调试面板 |

### 1.3 缓冲区状态机

```
        ┌──────────────────────────────────────────┐
        │  环形缓冲区 (48 MB)                        │
        │                                          │
        │  ├─── 已消费 ───┼─── 已缓冲 ───┼── 空 ──┤ │
        │              读指针          写指针        │
        └──────────────────────────────────────────┘

播放推进：读指针前移 → 空出空间 → 写指针继续流水线预读
seek 命中：目标 ∈ [读指针, 写指针]  → 移动读指针，零请求
seek 未中：目标 ∉ 窗口              → 清空缓冲，写指针跳到目标，重启流水线
                                      （SMB Session / 文件句柄不变）
```

### 1.4 播放器侧配套（Media3）

| 项 | 配置 |
|---|---|
| `LoadControl` | `minBufferMs = 30000`、`maxBufferMs = 120000`、`bufferForPlaybackMs = 2500`、`bufferForPlaybackAfterRebufferMs = 5000` |
| `DataSource` | `DefaultHttpDataSource.Factory()` 指向本地代理，连接超时 8s，读超时 65s，`setAllowCrossProtocolRedirects(false)`；读超时覆盖 SMB 60s 恢复窗 |
| `LoadErrorHandlingPolicy` | `DefaultLoadErrorHandlingPolicy` 最少重试 10 次，避免短时断网时 Media3 先于 SMB 恢复链结束播放 |
| `WifiLock` | 播放期间持有高性能 Wi-Fi 锁，以降低 OEM 省电导致断流的风险；是否中断取决于系统策略，须在荣耀 MagicOS 锁屏/后台动态验证 |
| `WakeLock` | `setWakeMode(C.WAKE_MODE_NETWORK)` |

### 1.5 目标指标

| 指标 | 目标 |
|---|---|
| 首帧（1080p） | ≤ 2.0 s |
| 首帧（4K / 12.3 GB） | ≤ 3.0 s |
| Seek 窗口内 | < 200 ms（瞬时） |
| Seek 窗口外 | ≤ 1.5 s |
| 有效吞吐 | ≥ 8 MB/s；当前链路上限待重新采集，不沿用历史 11MB/s 假设 |
| 缓冲水位 | 稳定 ≥ 30 秒时长；调试指标合并 Media3 已缓冲时长与代理尚未交付给 Media3 的前向预读，并分别保留组成项 |
| 2 小时连播卡顿 | **0 次** |

---

## 2. 双内核策略

### 2.1 分工

| 场景 | 内核 | 原因 |
|---|---|---|
| **默认** | **Media3 (ExoPlayer)** | 硬解稳定、首帧快、内存低、与 Android 音频框架结合最好 |
| 字幕为 `.ass` / `.ssa`（含特效） | **libVLC** | 内置 libass，Media3 不支持特效字幕渲染 |
| 音轨为 DTS / TrueHD / EAC3 | **libVLC** | Media3 不支持这些格式的解码 |
| Media3 解码器初始化失败 | **libVLC**（提示后一键切） | 兜底 |
| **需要精确音频延迟补偿时** | **libVLC 更简单**（见 §4.4） | libVLC 有现成的 `setAudioDelay()` API |
| 用户强制指定 | 遵循设置 | — |

**VLC 字幕边界：** 当前 VLC 路径只使用 libVLC 原生字幕 Surface，以完整保留 ASS/libass 定位、动画和 VobSub 图形；不启用应用层纯文本替代。字号会透传给 VLC，但颜色、描边、背景与底距等 Compose 字幕样式不保证在 VLC 路径生效。MagicOS 上 ASS 与 VobSub Surface 可用性必须以目标机视觉证据验收，未取证前不得宣称已有降级保护。

### 2.2 统一抽象

> 下列是目标能力的概念接口，不是当前源码签名。当前权威接口见 `LanPlay/app/src/main/java/com/lanplay/player/player/IPlayerEngine.kt`；字幕准备、画面变换、截图和帧率控制分别由 Repository、PlaybackController、UI/PixelCopy 与 `VideoFrameRateController` 协作。

```kotlin
interface IPlayerEngine {
    // 生命周期
    fun attach(surfaceView: SurfaceView)
    fun open(url: String, startPositionMs: Long)
    fun play(); fun pause(); fun release()

    // 控制
    fun seekTo(positionMs: Long)
    fun setSpeed(speed: Float)
    fun setVolume(volume: Float)            // 0.0 ~ 2.0（>1.0 为增强）

    // 字幕
    fun setExternalSubtitle(uri: String, charset: String?)
    fun setSubtitleDelayMs(deltaMs: Long)
    fun setSubtitleEnabled(enabled: Boolean)

    // 音轨
    fun selectAudioTrack(index: Int)
    fun setAudioDelayMs(deltaMs: Long)      // ★ 蓝牙延迟补偿（§4）

    // 画面
    fun setAspectMode(mode: AspectMode)
    fun setZoom(scale: Float, offsetX: Float, offsetY: Float)
    fun setRotation(degrees: Int)           // 0 / 90 / 180 / 270
    fun setMirror(horizontal: Boolean)
    fun setPreferredFrameRate(fps: Float)   // ★ 刷新率适配（§6）
    suspend fun captureFrame(): Bitmap?     // 截图

    // 状态
    val positionMs: Long
    val durationMs: Long
    val state: StateFlow<PlaybackState>
    val tracks: StateFlow<TrackInfo>
    val decoderInfo: StateFlow<DecoderInfo> // ★ 调试面板（§8）
}
```

### 2.3 切换流程

```
保存当前 position 与全部播放参数
    ↓
release() 旧内核（不关闭 SMB 代理）
    ↓
初始化新内核，attach 同一个 SurfaceView
    ↓
open(同一个代理 URL, savedPosition)
    ↓
恢复全部参数（速度/字幕/偏移/音轨/比例/缩放）
```

**SMB IO 层完全不动**，切换耗时应 ≤ 1.5 秒。

---

## 3. 解码性能最大化

> 冻结样本的编码负载理论上低于骁龙 8 Elite 能力，但 CPU、丢帧、温度和功耗必须以荣耀目标机量化。目标是尽量降低 CPU/丢帧/功耗，不能把“零 CPU、零丢帧”写成当前结论。

### 3.1 必须做对的六件事

| # | 措施 | 具体做法 | 验证方式 |
|---|---|---|---|
| **1** | **确保走高通硬解器** | `MediaCodecSelector.DEFAULT`（硬解优先）。**不要**手动指定解码器名 | 调试面板显示解码器名，必须是 `c2.qti.avc.decoder` 或 `OMX.qcom.video.decoder.avc`。若显示 `c2.android.*` 或 `OMX.google.*` 说明**掉到软解了**，必须排查 |
| **2** | **用 SurfaceView 而非 TextureView** | `PlayerView` 设 `surface_type="surface_view"`；自绘时直接用 `SurfaceView` | SurfaceView 走独立硬件图层，解码器输出**零拷贝**直达显示。TextureView 要多一次 GPU 纹理合成，4K 下开销可观 |
| **3** | **异步 MediaCodec 队列** | Media3 1.2+ 默认启用；确认未被关闭。旧版需 `experimentalSetAsynchronousBufferQueueingEnabled(true)` | 减少解码线程与主线程的同步等待 |
| **4** | **解码器回退链** | `DefaultRenderersFactory.setEnableDecoderFallback(true)` | 硬解初始化失败时自动降级而非直接报错 |
| **5** | **播放时关闭一切 GPU 特效** | 播放器界面**不做任何模糊**、不做圆角裁切视频层、字幕层独立不与视频合成 | 见[设计系统 §3.2 红线](./设计系统.md#32--性能红线不可违反) |
| **6** | **正确的色彩与 HDR 信息** | 从容器读取 `ColorInfo` 传给解码器。当前素材是 SDR `yuv420p`，不涉及 HDR，但代码要能正确处理 | 避免色彩偏移 |

### 3.2 解码器选择策略（三档，设置可选）

| 档位 | 行为 | 适用 |
|---|---|---|
| **硬解（默认）** | 硬件解码器优先，失败回退软解 | 99% 场景，当前全部素材 |
| **硬解优先 +** | 强制硬解，失败直接报错并建议切 libVLC，**不回退软解** | 排查"为什么发烫" |
| **软解** | 强制软件解码 | 硬解出现花屏/绿屏等异常时的兜底 |

### 3.3 首帧优化

| 措施 | 说明 | 收益 |
|---|---|---|
| **代理预热** | 点击文件的瞬间就启动 SMB 预读（在播放器 UI 还在转场动画时），不等播放器初始化 | 省 200~400ms |
| **解码器预创建** | 进入播放器时立即创建 MediaCodec 实例，与首块数据到达并行 | 省 100~200ms |
| **下一集预载** | `LocalMediaProxy.preloadHead()` 通过 AUX SMB 通道缓存下一集前 16MiB，下一次 `publish()` 按 identity/凭据/规范路径取用 | 对应 A-06；当前候选版本的首帧收益和总内存仍待目标机验证 |
| **首块优先** | 代理收到请求后优先准备默认 512KB 首块，不等待完整前向窗口 | 收益待目标机量化 |
| **跳过不必要的探测** | 已有 Room 缓存的元数据（时长/分辨率/帧率）直接传给播放器，不重新探测 | 省 100~300ms |

### 3.4 功耗优化

| 措施 | 说明 |
|---|---|
| **帧率声明** | 向系统声明视频源帧率，由系统选择显示模式；省电收益和最终 Hz 待目标机验证（§6） |
| **纯黑背景** | 播放器界面纯黑，OLED 像素熄灭 |
| **控制栏隐藏时释放模糊** | 不是只改 alpha——隐藏的 `RenderEffect` 仍消耗 GPU，必须真正移除 |
| **后台播放时释放视频解码器** | 只播音频时（切到后台/PiP 关闭），detach Surface 并释放视频轨 |
| **预读节流** | 缓冲水位达到上限后暂停预读，不持续占用 Wi-Fi 射频 |

### 3.5 丢帧监控

Media3 的 `DecoderCounters` 提供：

```kotlin
val counters = player.videoDecoderCounters
counters.droppedBufferCount        // 累计丢帧
counters.maxConsecutiveDroppedBufferCount  // 最大连续丢帧
counters.renderedOutputBufferCount // 已渲染帧
counters.videoFrameProcessingOffsetCount   // 渲染时间偏移统计
```

**调试面板实时显示**，并设告警阈值：连续丢帧 > 5 或丢帧率 > 0.5% 时在面板高亮。

### 3.6 验收标准

| 项 | 标准 |
|---|---|
| 解码器名 | `c2.qti.*` 或 `OMX.qcom.*`（**不能是 `c2.android.*`**） |
| 播放 4K 时 CPU 占用 | ≤ 8%（解码在硬件单元，CPU 只做 IO 与 UI） |
| 2 小时连播丢帧 | ≤ 10 帧（≈ 0.005%） |
| 机身温度 | 目标机记录环境温度、表面温度与是否降频；不得用散热规格替代实测 |
| 播放 1 小时耗电 | ≤ 12% 目标；记录系统最终显示模式和测试条件 |

---

## 4. 音频链路与蓝牙延迟补偿

> 常戴无线耳机，这一节是重点。

### 4.1 先说清楚一件事：系统已经补偿了大部分延迟

Android 的 `AudioTrack.getTimestamp()` 会报告音频输出链路的延迟，**Media3/ExoPlayer 会自动用这个值做音视频同步**。所以：

- Android 通常会尝试系统级同步，但不同耳机、codec、固件和内核仍可能有残余偏移。
- 当前没有荣耀目标机与真实耳机的完整证据，必须以人工音画校准门禁为准。
- ❌ **不要在自动补偿之上再叠加一个固定预设值**——那会变成双重补偿，比不补更糟

所以本项目的设计是：**默认 0 补偿（信任系统），提供手动微调 + 按设备记忆 + 一键校准工具**。

### 4.2 音频输出设备检测

```kotlin
// 监听输出设备变化
audioManager.registerAudioDeviceCallback(object : AudioDeviceCallback() {
    override fun onAudioDevicesAdded(added: Array<AudioDeviceInfo>) = refreshOutput()
    override fun onAudioDevicesRemoved(removed: Array<AudioDeviceInfo>) = refreshOutput()
}, handler)

// 判定当前输出类型
enum class AudioOutput {
    BUILTIN_SPEAKER,    // TYPE_BUILTIN_SPEAKER
    WIRED_HEADSET,      // TYPE_WIRED_HEADPHONES / TYPE_WIRED_HEADSET / TYPE_USB_HEADSET
    BLUETOOTH_A2DP,     // TYPE_BLUETOOTH_A2DP
    BLE_HEADSET,        // TYPE_BLE_HEADSET（LE Audio）
    OTHER
}
```

### 4.3 蓝牙编解码识别（可选增强）

当前 APK 未声明 `BLUETOOTH_CONNECT`，只识别输出设备大类和显示名，不读取 SBC/AAC/aptX/LDAC 或系统链路延迟。以下属于未来可选增强，不能作为当前调试面板或验收结果。

| 编解码 | 典型端到端延迟 | 说明 |
|---|---|---|
| SBC | 200 ~ 250 ms | 最通用，延迟最高 |
| AAC | 150 ~ 200 ms | 苹果系常用 |
| aptX | 80 ~ 120 ms | — |
| aptX HD | 100 ~ 150 ms | — |
| **aptX Adaptive** | 50 ~ 90 ms | 骁龙 8 Elite 支持，看视频最佳 |
| LDAC | 150 ~ 220 ms | 音质优先，延迟较高 |
| LC3（LE Audio） | 20 ~ 50 ms | 延迟最低 |

**规划用途（当前未实现）**：在用户明确授权 `BLUETOOTH_CONNECT` 后显示 codec 与仅供参考的延迟范围。当前调试面板不得显示猜测 codec 或“系统报告延迟”，也不得在未声明权限时触发授权流程。

### 4.4 延迟补偿的实现

| 内核 | 实现方式 | 难度 |
|---|---|---|
| **libVLC** | `MediaPlayer.setAudioDelay(delayUs: Long)` —— **原生支持，直接调用** | ⭐ 简单 |
| **Media3** | 当前不实现自定义延迟，使用系统默认同步；用户设置非零补偿时切换 VLC | 当前策略 |

**当前策略**：
1. libVLC 使用原生 `audioDelay` 应用补偿。
2. Media3 保持系统默认同步，不维护自定义 `MediaClock/AudioSink`。
3. 用户设置非零补偿时切换到 libVLC；切换、进度和暂停状态仍需目标机复验。

**补偿范围与步进**：
- 范围 **-500ms ~ +500ms**
- 粗调步进 **50ms**，微调步进 **10ms**
- 正值 = 音频延后播放（画面比声音快时用）
- 负值 = 音频提前（声音比画面快时用）

### 4.5 音画同步校准工具

播放器 ⋮ 菜单 → 音画同步。

```
┌─────────────────────────────────────────┐
│  音画同步校准                             │
│                                         │
│  当前输出：AirPods Pro（蓝牙 · AAC）      │
│  预估链路延迟：约 180ms（系统已自动补偿）   │
│                                         │
│         ◀ ─────────●───────── ▶         │
│              +120 ms                    │
│      [-50ms] [-10ms] [归零] [+10ms] [+50ms] │
│                                         │
│  提示：找一段有说话的画面，边看嘴型边调，      │
│       直到口型和声音对上                   │
│                                         │
│  ☑ 记住这个设备的设置                     │
│                                         │
│              [完成]                      │
└─────────────────────────────────────────┘
```

**要点**：
- 校准时**视频继续播放**，实时生效，所见即所得
- 显示当前输出设备名与编解码，让用户知道在调哪个设备
- 「记住这个设备」勾上后，下次连同一耳机自动应用
- 「归零」一键恢复系统默认（信任自动补偿）

### 4.6 按设备记忆

```kotlin
@Entity(tableName = "audio_device_profile")
data class AudioDeviceProfile(
    @PrimaryKey val deviceKey: String,   // 蓝牙设备名 + 地址 hash（不存明文 MAC）
    val displayName: String,             // "AirPods Pro"
    val outputType: AudioOutput,
    val codec: String?,                  // "AAC"（若可读取）
    val audioDelayMs: Long,              // 用户校准值
    val lastUsedAt: Long
)
```

**应用时机**：
- 播放开始时，检测当前输出设备 → 查表 → 应用补偿值
- **播放中切换输出设备**（拔耳机 / 连蓝牙）→ 实时切换到对应的补偿值，并用 Toast 提示「已切换到 XXX，音画同步 +120ms」

### 4.7 蓝牙场景的其他必要适配

| # | 场景 | 行为 | 实现 |
|---|---|---|---|
| 1 | **耳机拔出 / 蓝牙断开** | **立即暂停**（避免外放尴尬） | 监听 `AudioManager.ACTION_AUDIO_BECOMING_NOISY` |
| 2 | 蓝牙重新连接 | **不自动恢复播放**，通知栏显示可继续 | 避免突然出声 |
| 3 | **来电** | 暂停，焦点恢复后不自动播放 | `AUDIOFOCUS_LOSS_TRANSIENT` |
| 4 | **其他应用播放音频** | 永久/临时焦点丢失及 duck 请求均暂停；只允许用户手动继续 | 当前防突发外放策略 |
| 5 | **耳机线控 / AVRCP 按键** | 播放/暂停、上一个/下一个 | 当前为前台 `PlaybackService` + framework `MediaSession`，不是 Media3 `MediaSessionService` |
| 6 | 锁屏 / 通知栏控制 | 显示脱敏标题、进度和控制按钮，不显示私人文件名或封面 | `MediaSession` 元数据 |
| 7 | 音频输出切换 | **不中断播放**，无缝切换 | Media3 默认行为，确认不被打断 |
| 8 | 蓝牙耳机音量独立 | 系统层面处理，APP 不干预 | — |

> **第 1、3、4 条是基础正确性，不是可选功能。** 不做的话来电时视频还在放声音，那是 bug。

### 4.8 音频增强（可选）

| 功能 | 说明 | 优先级 |
|---|---|---|
| 音量增强到 200% | `LoudnessEnhancer`（Media3）或 libVLC 的 volume > 100。**默认关闭**，开启时提示可能失真 | P2 |
| 响度均衡 | 抑制忽大忽小的音量，夜间看片友好 | P2 |
| 均衡器 | 5~10 段 EQ + 预设。`Equalizer` AudioEffect | P2 |
| 外挂音轨 | 加载同目录的独立音频文件作为音轨 | P2 |

---

## 5. 手势系统

> 现成播放器最大的痛点：**横滑幅度太大，手指动一点就跳几分钟**。这一节把手势做到精确可控且全部可记忆。

### 5.1 手势总表

| 手势 | 动作 | 默认 | 可配 |
|---|---|---|---|
| 单击 | 显示/隐藏控制栏 | 是 | 可关 |
| 双击**左侧** | 快退 | −10 s | 5/10/15/30 s |
| 双击**右侧** | 快进 | +10 s | 5/10/15/30 s |
| 双击**中央** | 播放/暂停 | 开 | 可关（关闭后中央双击也走快进快退） |
| **横向滑动** | 快进/快退（**§5.2 重点**） | 1 屏宽 = 90 s | 30/60/90/120/180/300 s |
| **左半屏上下滑** | **亮度**（**§5.3 重点**） | 1 屏高 = 100% | 50/75/100/150% |
| **右半屏上下滑** | **音量**（**§5.3 重点**） | 1 屏高 = 100% | 50/75/100/150% |
| 长按 | 临时倍速 | 2.0x | 1.5/2.0/2.5/3.0x |
| 双指捏合 | 画面缩放 | 开 | 可关 |
| 双指拖动 | 画面平移（缩放后） | 开 | 可关 |
| **双指上下滑** | **字幕时间轴偏移**（借鉴 MX Player） | 1/10 屏高 = ±0.5 s | 可关 |
| 三指点击 | 截图当前帧 | 关 | 可开 |
| 边缘 24dp | 让给系统返回手势 | 让出 | 可占用 |
| **物理音量键** | **音量**（§5.10） | 开 | 可关 |
| 长按音量键 | 上一个 / 下一个视频 | **关** | 可开 |
| 长按进度条 | 精细拖动模式（§5.11.1） | 开 | 可关 |
| 双击进度条 | 快捷跳转 0/25/50/75%（§5.11.2） | 开 | 可关 |

> 手势的**生效区域**可限定（§5.9），**屏幕方向**可锁定（§5.8）。

### 5.2 ★ 横滑 seek —— 解决"幅度太大"

#### 5.2.1 核心：绝对时长映射，不是比例映射

**烂做法（现成播放器常见）**：全屏宽度 = 视频总时长。
→ 2.5 小时的视频，屏幕宽 1272px，**1px = 7 秒**。手指动 1cm（约 100px）就跳 **11 分钟**。这就是"幅度太大"的根源。

**正确做法**：全屏宽度 = **固定秒数**，与视频总时长**完全无关**。

```kotlin
// 默认 90 秒 / 屏宽
val secondsPerScreen = settings.seekSensitivity   // 30/60/90/120/180/300
val deltaSeconds = (dragDeltaX / screenWidthPx) * secondsPerScreen * precisionMultiplier
```

默认 90 秒/屏宽下：手指移动 1cm ≈ **7 秒**。这才是可控的。

#### 5.2.2 精度调节：垂直偏移改变步长（借鉴 iOS / nPlayer）

横滑的同时上下移动手指，**改变精度倍率**。这是高端播放器的标志性交互：

```
          手指垂直偏移（相对起手点）
   ↑ −180dp 以上  ┃  精细   ×0.25   →  1 屏 = 22.5 s
   ↑  −80dp       ┃  精确   ×0.50   →  1 屏 = 45 s
   ─   0dp        ┃  标准   ×1.00   →  1 屏 = 90 s   ← 默认
   ↓  +80dp       ┃  粗调   ×2.00   →  1 屏 = 180 s
   ↓ +180dp 以下  ┃  快速   ×4.00   →  1 屏 = 360 s
```

- 档位切换时**轻微震动**（`HapticFeedbackType.TextHandleMove`）
- 浮层实时显示当前档位名（「精细」「标准」「快速」）
- 可在设置中关闭此功能（关闭后恒为 ×1.00）

#### 5.2.3 交互细节

| 项 | 规则 |
|---|---|
| **死区** | 起手后横向移动 **< 16dp 不响应**，避免与单击/双击冲突 |
| **方向锁定** | 移动 16dp 后判定主方向（横 or 纵），**一旦判定，直到抬手不再改变** |
| **不实时 seek** | 拖动过程中**只更新浮层显示**，松手才真正 `seekTo()`。避免频繁 IO 和画面闪烁 |
| **浮层内容** | `01:23:45 → 01:25:12`  大字显示 `+1:27`，下方小字显示精度档位 |
| **松手预览** | 若启用了 seek 预览缩略图，浮层同时显示目标位置的画面（见 §5.6） |
| **边界处理** | 到达 0 或总时长时**停住并震动**，不循环不回绕 |
| **取消** | 拖动中手指移回起点 ±16dp 内并抬手 → 取消，不 seek |

### 5.3 ★ 亮度 / 音量 —— 软上限保护

#### 5.3.1 基础映射

| 项 | 规则 |
|---|---|
| 左半屏（宽度 < 50%）上下滑 | **亮度** |
| 右半屏（宽度 ≥ 50%）上下滑 | **音量** |
| 灵敏度 | 1 屏高 = 100% 变化（默认），可配 50/75/100/150% |
| 死区 | 起手后纵向移动 < 12dp 不响应 |
| 方向 | 上滑增大，下滑减小 |
| 浮层 | 屏幕中央，图标 + 百分比 + 竖向进度条 |

#### 5.3.2 ★ 软上限保护（防爆音 / 防刺眼）

超过一半后**必须抬手再滑一次**才能继续提高。

```
状态机：

  NORMAL ──滑动到达阈值(默认50%)──▶ CLAMPED
                                     │
                              [卡住不再上升]
                              [震动 + 浮层提示]
                              「已达 50%，抬手后再次上滑可继续」
                                     │
                                  抬手 ──▶ ARMED（5 秒内有效）
                                     │
                            5 秒内再次上滑 ──▶ UNLOCKED
                                     │              │
                            5 秒超时 ──▶ NORMAL   [可自由升到 100%]
                                                    │
                                                 抬手 ──▶ NORMAL
```

| 参数 | 默认 | 可配 |
|---|---|---|
| 音量阈值 | **50%** | 40 / 50 / 60 / 70 / 关闭 |
| 亮度阈值 | **50%** | 40 / 50 / 60 / 70 / 关闭 |
| ARMED 有效期 | 5 秒 | 3 / 5 / 8 秒 |
| 到达阈值反馈 | 震动 + 浮层文字 + 进度条在阈值处显示一道**刻度线** | 震动可关 |

**细节**：
- 阈值刻度线在浮层进度条上**常驻显示**（一条细横线），让用户知道限制在哪
- 已 UNLOCKED 时刻度线变为强调色，表示已突破
- **下滑不受限制**，任何时候都能自由降低
- 关闭保护后，行为退化为普通线性滑动

#### 5.3.3 记忆规则（★ 用户明确要求）

| 项 | 存储范围 | 恢复时机 | 说明 |
|---|---|---|---|
| **音量** | **全局**（不按视频） | 每次进入播放器 | 记住绝对值。控制系统媒体音量流 `STREAM_MUSIC` |
| **亮度** | **全局** | 每次进入播放器 | 控制**窗口亮度**（`WindowManager.LayoutParams.screenBrightness`），**不改系统亮度**；退出播放器自动恢复系统亮度 |
| 阈值解锁状态 | 不记忆 | — | 每次进入播放器重置为 NORMAL，安全起见 |
| 所有灵敏度设置 | 全局 | 立即 | 存 DataStore |

> 「下次打开软件或者看其他视频都是同样的」—— 全局存储正是这个语义。若某天想改成按视频记忆，数据结构已预留（放进 `WatchRecord` 即可）。

### 5.4 双指字幕偏移（借鉴 MX Player）

播放中双指同时上下滑动 → 调整字幕时间轴。

| 项 | 规则 |
|---|---|
| 触发 | 两指同时按下并纵向移动 > 16dp |
| 映射 | 1/10 屏高 = **±0.5 秒** |
| 方向 | 上滑 = 字幕提前（负偏移），下滑 = 字幕延后（正偏移） |
| 浮层 | 显示 `字幕 +1.5s`，同时字幕**实时跟着变** |
| 范围 | ±60 秒 |
| 保存 | 抬手后自动写入该视频的 `WatchRecord.subtitleOffsetMs` |
| 冲突 | 与双指捏合缩放冲突时：**两指间距变化 > 24dp 判为缩放**，否则判为字幕偏移 |

### 5.5 手势冲突与优先级

```
按下
 ├─ 单指
 │   ├─ 移动 < 16dp 且 300ms 内抬手 ──▶ 单击（切控制栏）
 │   ├─ 300ms 内第二次点击        ──▶ 双击（按区域：左退/中暂停/右进）
 │   ├─ 按住 > 400ms 未移动        ──▶ 长按倍速
 │   └─ 移动 ≥ 16dp
 │        ├─ |dx| > |dy| ──▶ 横滑 seek（锁定方向）
 │        └─ |dy| > |dx| ──▶ 左半屏亮度 / 右半屏音量（锁定方向）
 └─ 双指
     ├─ 指间距变化 > 24dp ──▶ 缩放
     ├─ 已缩放且同向移动   ──▶ 平移
     └─ 同向纵移 > 16dp   ──▶ 字幕偏移
```

**锁定原则**：一旦判定手势类型，**直到所有手指抬起都不再改变**。这是"跟手"的关键——中途切换手势类型会让用户觉得播放器"飘"。

### 5.6 Seek 预览缩略图（播放进阶组）

拖动进度条或横滑 seek 时，显示目标位置的画面。

**实现方案（SMB 场景的取舍）**：

| 方案 | 做法 | 成本 | 结论 |
|---|---|---|---|
| A. 实时抽帧 | 拖动时实时 seek 解码 | 每次要 SMB 随机读 + 解码，延迟高 | ❌ 不可行 |
| B. **预生成雪碧图** | 首次播放时后台按固定间隔（如每 10 秒）抽帧，拼成雪碧图缓存到本地 | 一次性成本，后续零延迟 | ✅ **推荐** |
| C. 播放中顺带采样 | 播放过程中每隔 N 秒把当前帧缩略存下 | 零额外 IO，但只有看过的部分有预览 | ✅ **作为 B 的补充** |

**采用 B + C**：
- 首次打开视频时，后台低优先级任务生成雪碧图（每 10 秒一帧，160×90，JPEG 质量 60）
- 2.5 小时视频 ≈ 900 帧 ≈ 单张雪碧图约 3~5 MB，可接受
- 生成期间用 C 方案（播放过的位置有预览）过渡
- 雪碧图缓存在应用私有目录，随视频删除而清理
- **可在设置中关闭**（不想产生额外 IO 时）

### 5.7 手势设置页

所有手势可独立开关 + 调参，设置页结构：

```
手势
├── 横滑快进快退
│   ├── [开关]
│   ├── 灵敏度：1 屏 = [90] 秒     （30/60/90/120/180/300）
│   ├── 精度调节（上下移动改变步长）  [开关]
│   └── 显示目标位置预览图          [开关]
├── 亮度（左半屏上下滑）
│   ├── [开关]
│   ├── 灵敏度：1 屏 = [100]%      （50/75/100/150）
│   ├── 软上限保护：[50]%           （40/50/60/70/关闭）
│   └── 记住亮度                    [开关，默认开]
├── 音量（右半屏上下滑）
│   ├── [开关]
│   ├── 灵敏度：1 屏 = [100]%
│   ├── 软上限保护：[50]%
│   └── 记住音量                    [开关，默认开]
├── 双击
│   ├── 左右双击步长：[10] 秒       （5/10/15/30）
│   └── 中央双击播放/暂停           [开关]
├── 长按倍速：[2.0]x               （1.5/2.0/2.5/3.0）
├── 双指
│   ├── 捏合缩放                    [开关]
│   └── 双指纵滑调字幕偏移           [开关]
├── 三指点击截图                     [开关，默认关]
├── 物理音量键
│   ├── 用音量键调音量               [开关，默认开]
│   └── 长按切换视频                 [开关，默认关]
├── 进度条
│   ├── 长按精细拖动                 [开关，默认开]
│   └── 双击快捷跳转                 [开关，默认开]
├── 手势响应区                       [全屏 / 下2/3 / 下1/2 / 中间60% / 自定义]
├── 震动反馈                        [开关，默认开]
└── 边缘 24dp 让给系统返回手势        [开关，默认开]
```

**「恢复默认」按钮**放在页面底部。

---

### 5.8 屏幕方向锁定

> 手持观看时重力感应会乱转，这是最影响体验的一件事。必须彻底解决。

#### 5.8.1 交互设计（两层）

| 层 | 位置 | 行为 |
|---|---|---|
| **快捷层** | 播放器控制栏的方向按钮 | **切换式**：按下 = **锁死此刻的实际方向**；再按 = 恢复自动。这是最符合直觉的交互——"我现在这个角度看着舒服，就锁这个" |
| **默认层** | 设置 → 播放 → 进入播放器时的方向 | 三档：**自动**（默认）/ 强制横屏 / 强制竖屏 |

**为什么快捷层要"锁定当前"而不是"强制横屏"**：`SCREEN_ORIENTATION_LANDSCAPE` 只锁一个横屏方向（Home 键在右）。手持时手机可能是反向横屏，强制到固定横屏会让画面变成倒的。锁定当前方向则怎么拿着舒服就锁成什么样。

#### 5.8.2 实现

```kotlin
enum class OrientationMode { AUTO, LOCKED, FORCE_LANDSCAPE, FORCE_PORTRAIT }

fun applyOrientation(activity: Activity, mode: OrientationMode) {
    activity.requestedOrientation = when (mode) {
        // ★ 用 SENSOR 而非 UNSPECIFIED：会覆盖系统的"旋转锁定"开关，
        //   用户全局关了自动旋转时，播放器内仍能自动转
        AUTO             -> ActivityInfo.SCREEN_ORIENTATION_SENSOR
        // ★ LOCKED 锁死当前实际方向（含反向横屏），API 18+
        LOCKED           -> ActivityInfo.SCREEN_ORIENTATION_LOCKED
        FORCE_LANDSCAPE  -> ActivityInfo.SCREEN_ORIENTATION_SENSOR_LANDSCAPE
        FORCE_PORTRAIT   -> ActivityInfo.SCREEN_ORIENTATION_SENSOR_PORTRAIT
    }
}
```

| 要点 | 说明 |
|---|---|
| `SCREEN_ORIENTATION_SENSOR` | AUTO 档用它，**会覆盖系统的旋转锁定**。用户全局关了自动旋转，播放器内依然能转 |
| `SCREEN_ORIENTATION_LOCKED` | 锁死当前实际方向（包括反向横屏 / 反向竖屏），正是"怎么翻都不转"的语义 |
| `SENSOR_LANDSCAPE` / `SENSOR_PORTRAIT` | 强制档用带 SENSOR 的版本，允许该方向的正反两种朝向，但绝不转到另一个轴 |
| 记忆 | `orientationMode` 存 DataStore（全局），进播放器时应用 |
| 退出播放器 | 恢复 `SCREEN_ORIENTATION_UNSPECIFIED`，不影响浏览页 |
| 图标状态 | 自动 = 旋转箭头图标；锁定 = 锁形 + 当前方向指示。切换时 Toast 提示「已锁定横屏」/「已恢复自动旋转」 |

---

### 5.9 手势响应区

> 手持时手掌边缘、指根容易压到屏幕。限定响应区能根治这类误触。

| 预设 | 响应区域 |
|---|---|
| **全屏**（默认） | 100% |
| 下 2/3 | 顶部 33% 不响应 |
| 下 1/2 | 顶部 50% 不响应 |
| 中间 60% | 上下各 20% 不响应 |
| 自定义 | 上边距 0~40% + 下边距 0~40%，滑块调节 |

**规则**

| # | 规则 |
|---|---|
| 1 | 区域外触摸**只响应单击**（显隐控制栏），不响应滑动、长按、双击、双指 |
| 2 | **例外**：控制栏自身的按钮永远可点，不受区域限制 |
| 3 | **例外**：进度条永远可拖，不受区域限制 |
| 4 | 设置中调整时显示**半透明区域示意图**，实时预览边界（进入设置即显示，5 秒后淡出） |
| 5 | 区域设置存 DataStore（全局） |

---

### 5.10 物理音量键

> 手持时按侧边键比在屏幕上滑更省事，也不用盯着屏幕。

```kotlin
override fun onKeyDown(keyCode: Int, event: KeyEvent): Boolean {
    if (!isPlayerActive || isPipMode) return super.onKeyDown(keyCode, event)
    return when (keyCode) {
        KeyEvent.KEYCODE_VOLUME_UP -> {
            if (settings.volumeKeyLongPressSwitchVideo && event.repeatCount >= LONG_PRESS_THRESHOLD) {
                playNext(); true
            } else {
                // ★ 与手势共用同一个软上限状态机（§5.3.2）
                volumeController.stepUp(); true   // 返回 true 阻止系统音量 UI
            }
        }
        KeyEvent.KEYCODE_VOLUME_DOWN -> { /* 对称处理 */ true }
        else -> super.onKeyDown(keyCode, event)
    }
}
```

| 要点 | 说明 |
|---|---|
| **共用软上限状态机** | 音量键升到阈值同样卡住 + 震动 + 提示，需**松开按键后再按一次**才能继续。与滑动手势完全一致的行为 |
| 自绘浮层 | 返回 `true` 阻止系统默认音量 UI，显示与手势调节**样式完全一致**的浮层 |
| 步进 | 每次按键 = 系统音量流的一格（约 6~7%），长按连续调节 |
| **作用范围** | **只在播放器页面拦截**。浏览页、设置页不拦截，音量键走系统默认行为 |
| PiP 模式 | 不拦截，交给系统 |
| 长按切视频 | `event.repeatCount` 判断。**默认关闭**（避免误触），开启后长按上 = 下一个，长按下 = 上一个 |

---

### 5.11 进度条精细拖动与快捷跳转

#### 5.11.1 精细拖动

| 项 | 规则 |
|---|---|
| 触发 | 长按进度条 **400ms** |
| 反馈 | 进入时震动一次，进度条加粗（4dp → 8dp），滑块放大（12dp → 20dp） |
| 映射 | 手指移动距离 **× 1/8** 映射到时间。2.5 小时的片子，全屏宽拖动只走约 19 分钟，可轻松定位到秒 |
| 浮层 | 「**精细**」标识 + 精确到秒的时间码（tabular figures） |
| 退出 | 松手即退出，恢复正常拖动比例 |

#### 5.11.2 快捷跳转

| 项 | 规则 |
|---|---|
| 触发 | **双击进度条** |
| 表现 | 进度条上方弹出胶囊按钮条：`0%` `25%` `50%` `75%`，280ms spring 升起 |
| 交互 | 单击任一按钮直接跳转并收起；点击别处收起 |
| 自动收起 | 3 秒无操作 |
| 位置提示 | 当前进度所在的区间按钮高亮 |

---

## 6. 刷新率适配

### 6.1 当前策略

目标设备 UI 预计最高常用 120Hz，冻结样本主要为 29.97/59.94fps。两者倍率接近并不等于绝对无 judder；最终 cadence 仍由面板模式、系统合成和厂商策略决定。

当前 `VideoFrameRateController` 调用 `Surface.setFrameRate(sourceFps, FIXED_SOURCE, ALWAYS)`，只向系统声明源帧率，由系统选择合适的 60/90/120Hz 等模式；退出时以 0 清除声明。该 API **不保证强制切到 60Hz**。

### 6.2 验收

- 记录视频源帧率、API 调用结果和 `dumpsys display/SurfaceFlinger` 最终模式。
- 检查切换是否产生黑屏、掉帧、音画不同步或无法恢复 UI 模式。
- 省电收益必须通过目标机功耗对比，不使用“无副作用”“显著省电”等未经测量的结论。
- 如果产品最终硬性要求 60Hz，需要另行实现可控显示模式选择并在 MagicOS 上验收；当前实现不满足“强制 60Hz”。

---

## 7. 竞品借鉴清单

> 明确列出从 nPlayer / MX Player / OPlayer 学什么、不学什么。

### 7.1 值得学的

| 来源 | 优点 | 本项目对应 |
|---|---|---|
| **nPlayer** | 横滑 seek 时**垂直偏移改变精度** | §5.2.2 已采纳 |
| **nPlayer** | 字幕设置极其丰富（字号/颜色/描边/背景/位置/边距/字体） | S-08 已采纳 |
| **nPlayer** | 字幕时间轴调整入口显眼、步进合理 | §5.4 + S-09/G-13 已采纳 |
| **nPlayer** | 硬解/软解可手动切换，并显示当前解码器 | §3.2 + §8 已采纳 |
| **nPlayer** | 支持外挂音轨 | §4.8 列为 P2 |
| **nPlayer** | 画面裁剪模式丰富（原始/铺满/16:9/4:3/拉伸/裁切） | P-08 已采纳 |
| **MX Player** | **双指纵滑调字幕时间轴** —— 极其顺手 | §5.4 已采纳 |
| **MX Player** | 手势跟手度是行业标杆，1:1 无缓动 | §5.5 锁定原则 已采纳 |
| **MX Player** | 双指缩放 + 平移画面 | §5.1 已采纳 |
| **MX Player** | 儿童锁 / 屏幕锁定防误触 | G-18 已采纳 |
| **MX Player** | 解码器分档（HW / HW+ / SW） | §3.2 三档策略 已采纳 |
| **MX Player** | 文件浏览极快（本地索引 + 缓存优先） | C-12 已采纳 |
| **OPlayer** | 密码保护 / 私密模式 | V-01～V-05 已采纳 |
| **OPlayer** | 音量增强超过 100% | §4.8 列为 P2 |
| **OPlayer** | 手势自定义程度高（每项可独立开关调参） | §5.7 已采纳 |
| **OPlayer** | A-B 循环 | A-09 已采纳 |
| **三者共同** | 后台播放 + 锁屏/通知栏控制 + 线控 | §4.7 已采纳 |

### 7.2 明确不学的

| 不学 | 原因 |
|---|---|
| 内置浏览器 / 下载器 | 超出范围，且与"零外发"隐私要求冲突 |
| 在线字幕搜索 | 同上。字幕由 PC 端准备 |
| 广告 / 内购 / 账号 | 自用软件 |
| 支持一切网络协议（FTP/WebDAV/NFS/UPnP） | 只做 SMB，专注 |
| 复杂的播放列表管理器 | 一个目录就是一个列表，够用 |
| 视频转码 / 格式转换 | 超出范围 |
| 花哨的启动动画 | 与"启动极快"目标冲突 |

---

## 8. 调试面板

> 排查问题的唯一可靠手段。播放器 ⋮ → 调试面板，或设置中长按版本号开启。

### 8.1 当前展示内容

当前 `PlayerScreen.DebugPanel` 展示：

- 网络：吞吐、缓冲秒数/MB、在途请求、命中率、重连次数、SMB 方言。
- 解码：当前内核、视频/音频解码器名、尺寸/帧率、渲染帧与丢帧。
- 显示：系统当前刷新率和 Surface 类型。
- 音频：输出设备大类；当前不读取具体蓝牙 codec 或系统链路延迟。
- 内存：Java 堆使用量。

Media3 版本为 1.10.1，libVLC 为 3.6.5。吞吐趋势图、码率/色彩、帧耗时 P50/P99、蓝牙 codec、系统延迟、Native/图片缓存、面板内复制与导出按钮均不是当前已实现字段，不得用示意数值冒充证据。

调试面板的通用 `isHardware` 只是启发式；荣耀最终门禁仍必须读取真实解码器名并匹配批准规则。

### 8.2 告警高亮

面板中以下情况用 `error` 色高亮，方便一眼发现问题：

| 条件 | 含义 |
|---|---|
| 解码器名含 `c2.android.` 或 `OMX.google.` | **掉软解了** |
| 吞吐 < 3 MB/s | 网络瓶颈 |
| 缓冲水位 < 10 s | 即将卡顿 |
| 丢帧率 > 0.5% | 渲染有问题 |
| 连续丢帧 > 5 | 严重掉帧 |
| 帧耗时 P99 > 8.33 ms | UI 掉帧 |
| 重连次数 > 0 | 网络不稳 |

### 8.3 附带工具

| 工具 | 说明 |
|---|---|
| **网速测试** | 对当前共享做一次纯读取测速（读 100MB 丢弃），报告实际 SMB 吞吐 |
| **连接诊断** | 逐项检查：能否 ping 通 → 445 端口是否开放 → SMB 协商成功 → 认证成功 → 能否列目录 → 能否读文件。每项给中文结果 |
| **导出日志** | 最近 200 条错误 + 当前面板快照，导出为文本 |

---

## 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-07-26 | 初版。SMB IO 层、双内核、解码性能最大化、蓝牙延迟补偿与音频链路、完整手势系统（含横滑精度与软上限保护）、刷新率适配（120Hz 修正）、竞品借鉴清单、调试面板 |
| **v1.1** | 2026-07-26 | 新增 **§5.8 屏幕方向锁定**（两层交互：控制栏"锁定当前方向" + 设置默认档；`SCREEN_ORIENTATION_SENSOR` 覆盖系统旋转锁定、`LOCKED` 锁死实际方向）、**§5.9 手势响应区**（5 种预设 + 可视化调整，杜绝手掌误触）、**§5.10 物理音量键**（与滑动手势共用软上限状态机、只在播放器页拦截）、**§5.11 进度条精细拖动与快捷跳转**（长按 400ms 进入 1/8 速、双击弹 0/25/50/75%）。同步更新手势总表与设置页结构 |
| **v1.2** | 2026-07-28 | 统一 APP 联网边界、回环代理/token/Range、播放模糊、音频焦点与 VobSub 会话临时文件契约；最终门禁以目标机动态证据为准。 |
| **v1.3** | 2026-07-29 | 建立新会话独立审查基线：规格不继承旧问题清单，当前实现与正确性必须由源码、构建和目标机证据重新确认。 |
| **v1.4** | 2026-07-29 | 明确 VLC 仅使用原生字幕 Surface 及样式边界；超阈值并行长 Range 使用独立轻量窗口；缓冲指标合并 Media3 与代理前向预读；本地 HTTP/Media3 重试覆盖 SMB 60 秒恢复窗。 |
