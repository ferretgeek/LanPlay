# LanPlay 设计系统

> 配套文档：[需求文档.md](./需求文档.md)
> 版本：v1.3 · 2026-07-29
> 目标设备：荣耀 WIN RT · 2800×1272 · **实际可用刷新率 120Hz（帧预算 8.33ms）**
> 视觉方向：**清晰表面层级** · 6 套浅色主题 + Ink 深色模式 · 内容为王
>
> v1.3 验收口径：滚动区与视频层禁用实时背景模糊；隐私海报/头像遮挡不等于背景毛玻璃。对比度、TalkBack、390×844 / 768×1024 / 1440×900 与字体 100%/125%/150% 必须以目标机动态证据验收，未执行不得写“全部通过”。本文的实现状态只是待独立复核的声明。

---

## 目录

- [0. 设计原则](#0-设计原则)
- [1. 主题引擎架构](#1-主题引擎架构)
- [2. 六套浅色主题与 Ink 色板](#2-六套浅色主题与-ink-色板)
- [3. 毛玻璃规范与性能红线](#3-毛玻璃规范与性能红线)
- [4. 排版系统](#4-排版系统)
- [5. 间距 · 圆角 · 阴影](#5-间距--圆角--阴影)
- [6. 动效系统](#6-动效系统)
- [7. 核心组件规格](#7-核心组件规格)
- [8. 状态设计（空 / 加载 / 错误）](#8-状态设计空--加载--错误)
- [9. 图标与插图](#9-图标与插图)
- [10. 无障碍基线](#10-无障碍基线)
- [11. 实现清单](#11-实现清单)

---

## 0. 设计原则

### 0.1 当前实现与目标的边界

- 当前主题架构是 `LanPlayThemeDefinition + MaterialTheme.colorScheme + animateColorAsState`，不是下文早期设想的完整 `LanPlayColors/GlassSpec/CompositionLocal` 引擎。
- 当前提供 6 套浅色主题；Ink 是深色模式，不是独立浅色主题对象；Android 12+ 另有 Dynamic 选项。
- 完整玻璃、彩色阴影、统一 Motion/Spacing、shimmer、对比度脚本仍是目标，不得写成已完成。
- 隐私模式当前在多个滚动页面使用图片 `Modifier.blur`。为满足性能红线，后续应改成预生成模糊图、马赛克或不透明遮罩，并统一临时揭示交互。
- 当前图标来自 `material-icons-extended` 的 `Icons.Rounded`；没有 Coil、Palette 或 Material Symbols 依赖。

下文色板和组件保留为设计目标；§11 只记录文档维护时观察到的实现声明，新审查必须直接核对当前 Compose 源码和目标机证据。按优先级排列，冲突时上位覆盖下位。

| # | 原则 | 含义 |
|---|---|---|
| 1 | **内容为王** | 海报和画面是主角。UI 永远退到背景，绝不与内容抢注意力。任何装饰性元素若削弱了海报的可读性，删掉 |
| 2 | **帧率优先于质感** | 120Hz 满帧是硬指标，毛玻璃是加分项。冲突时**永远牺牲模糊，不牺牲帧率** |
| 3 | **一套结构，多种气质** | 换主题只换色彩/材质/阴影色调，**绝不改变布局、层级和交互**。用户换主题后不需要重新学习界面 |
| 4 | **不只是换色** | 每套主题有自己的玻璃着色、阴影色调、渐变、选中态。阴影用主题色调深而非纯黑——这是高级感的分水岭 |
| 5 | **克制的动效** | 120Hz 下动画要干脆。没有超过 400ms 的动画，没有逐项错峰入场，没有炫技 |
| 6 | **暗黑保留个性** | 一键切暗黑不是丢掉主题。墨黑底 + 当前主题的强调色变体，个性还在 |
| 7 | **降级不报错** | 元数据缺失、图片加载失败、Android 版本低——全部静默优雅降级，绝不弹错误打扰 |

---

## 1. 主题引擎架构

> **目标方案：** 本节 Kotlin 代码是目标设计，不是当前源码。当前实现见 `ui/theme/LanPlayTheme.kt`；若继续演进，必须用迁移而不是假定这些类型已经存在。

### 1.1 为什么需要"引擎"而不是几个 colorScheme

Material 3 的 `ColorScheme` 只管颜色。本项目要的是**换主题时材质也变**：暖色主题的阴影带暖调、冷色主题的玻璃偏冷、每套主题有自己的渐变。所以需要一层扩展。

```kotlin
// ── 主题定义 ──────────────────────────────────
@Immutable
data class LanPlayTheme(
    val id: String,                 // "mist"
    val nameZh: String,             // "晨雾"
    val nameEn: String,             // "Morning Mist"
    val isDark: Boolean,
    val colors: LanPlayColors,      // 完整色板（§2）
    val glass: GlassSpec,           // 玻璃参数（§3）
    val shadow: ShadowSpec,         // 阴影色调与强度（§5.3）
    val gradient: GradientSpec,     // 强调渐变（§2.9）
    val darkAccent: Color           // 切暗黑时保留的强调色（原则 6）
)

@Immutable
data class LanPlayColors(
    // 表面层级
    val background: Color,
    val surface: Color,
    val surfaceContainerLowest: Color,
    val surfaceContainerLow: Color,
    val surfaceContainer: Color,
    val surfaceContainerHigh: Color,
    val surfaceContainerHighest: Color,
    // 主色
    val primary: Color, val onPrimary: Color,
    val primaryContainer: Color, val onPrimaryContainer: Color,
    // 辅助
    val secondary: Color, val secondaryContainer: Color,
    val tertiary: Color,
    // 边框与文字
    val outline: Color, val outlineVariant: Color,
    val onBackground: Color, val onSurface: Color, val onSurfaceVariant: Color,
    // 语义
    val error: Color, val onError: Color,
    val success: Color, val warning: Color,
    // 项目专用
    val watchedOverlay: Color,      // 已看完卡片的降透明遮罩
    val progressTrack: Color,       // 进度条底槽
    val progressIndicator: Color,   // 进度条填充
    val shimmerBase: Color,         // 骨架屏底色
    val shimmerHighlight: Color,    // 骨架屏高光
    val scrim: Color                // 全屏遮罩
)

@Immutable
data class GlassSpec(
    val tintL1: Color, val blurL1: Dp,   // 轻：顶栏、底部导航
    val tintL2: Color, val blurL2: Dp,   // 中：播放器控制栏、浮层
    val tintL3: Color, val blurL3: Dp,   // 重：全屏弹层、设置面板
    val borderColor: Color,              // 内发光细边
    val borderWidth: Dp = 1.dp
)

@Immutable
data class ShadowSpec(
    val tint: Color,          // 阴影着色（主题色调深，非纯黑）★ 关键
    val ambientAlpha: Float,
    val spotAlpha: Float
)

@Immutable
data class GradientSpec(
    val start: Color, val end: Color,
    val angleDeg: Float = 135f
)
```

### 1.2 CompositionLocal 分发

```kotlin
val LocalLanPlayTheme = staticCompositionLocalOf<LanPlayTheme> { error("No theme") }

@Composable
fun LanPlayThemeProvider(theme: LanPlayTheme, content: @Composable () -> Unit) {
    CompositionLocalProvider(LocalLanPlayTheme provides theme) {
        MaterialTheme(
            colorScheme = theme.colors.toMaterial3(),  // 兼容 M3 组件
            typography = LanPlayTypography,
            shapes = LanPlayShapes,
            content = content
        )
    }
}

// 使用
val t = LocalLanPlayTheme.current
Box(Modifier.background(t.colors.surfaceContainer))
```

> `staticCompositionLocalOf` 只是目标方案之一；主题变化会使整个提供域失效，不能简单表述为天然更省重组。当前实现直接使用 `MaterialTheme`，以后引入自定义 Local 前应以 Macrobenchmark 比较。

### 1.3 主题切换与持久化

| 项 | 规则 |
|---|---|
| 存储 | DataStore：`themeId`（当前浅色主题）+ `darkMode`（跟随系统 / 强制浅 / 强制深） |
| 切换动效 | 当前对颜色 token 做 220ms 插值，不对整棵页面 `Crossfade`，避免丢失播放、滚动和输入状态 |
| **一键暗黑** | 顶栏或设置里的开关。切到深色时使用**墨黑 Ink 底 + 当前主题的 `darkAccent`**，主题个性保留 |
| 跟随系统 | 默认。系统切深色时自动切 Ink |
| 预览 | 设置页的主题选择器显示**真实的迷你卡片预览**（一张假海报 + 玻璃条 + 按钮），不是纯色圆点 |
| 立即生效 | 切换无需重启，无需重新加载数据 |

### 1.4 动态取色（可选，P2）

Android 12+ 可读取系统壁纸取色（`dynamicLightColorScheme` / `dynamicDarkColorScheme`），作为 6 套浅色主题之外的 **Dynamic** 选项，默认不启用。Ink 是深色模式而非“第 7/8 个主题卡”。

---

## 2. 六套浅色主题与 Ink 色板

> 以下色值是设计基线。当前尚未完成全部 token、Ink 和 Dynamic 的脚本化对比度验证，不能写“全部通过”。
> 浅色主题一律**白卡片 + 极浅底**，保证海报图放上去干净通透、不暗沉。

### 2.1 晨雾 Morning Mist（冷灰蓝 · 默认）

```
background              #FAFBFC
surface                 #FFFFFF
surfaceContainerLowest  #FFFFFF
surfaceContainerLow     #F7F9FB
surfaceContainer        #F2F4F7
surfaceContainerHigh    #ECEFF3
surfaceContainerHighest #E6EAEF

primary                 #4A6FA5      onPrimary            #FFFFFF
primaryContainer        #DDE7F5      onPrimaryContainer   #16304F
secondary               #7B8FA8      secondaryContainer   #E6ECF3
tertiary                #6B8E9E

outline                 #C6CFD9      outlineVariant       #DDE2E8
onBackground            #1A1D21      onSurface            #1A1D21
onSurfaceVariant        #5A626C

error #BA1A1A   success #2E7D5B   warning #B4762A

glass.tintL1            #FFFFFF @ 72%     blur 12dp
glass.tintL2            #FFFFFF @ 78%     blur 24dp
glass.tintL3            #FFFFFF @ 86%     blur 40dp
glass.border            #FFFFFF @ 85%

shadow.tint             #16304F           ambient 5%  spot 8%
gradient                #4A6FA5 → #6B8E9E  135°
darkAccent（切暗黑时）    #8FB3F0
```

### 2.2 薄荷 Mint（清新绿）

```
background              #F7FBF9
surface                 #FFFFFF
surfaceContainerLowest  #FFFFFF
surfaceContainerLow     #F3F9F6
surfaceContainer        #EAF4EF
surfaceContainerHigh    #E2EFE8
surfaceContainerHighest #D9E9E1

primary                 #2E9E7A      onPrimary            #FFFFFF
primaryContainer        #D3EFE4      onPrimaryContainer   #073A2A
secondary               #86BFA8      secondaryContainer   #E1F1EA
tertiary                #4F9E8E

outline                 #BCD5CA      outlineVariant       #D5E8DF
onBackground            #16241E      onSurface            #16241E
onSurfaceVariant        #4C6459

error #BA1A1A   success #2E9E7A   warning #B4762A

glass.tintL1            #FFFFFF @ 74%     blur 12dp
glass.tintL2            #FFFFFF @ 80%     blur 24dp
glass.tintL3            #FFFFFF @ 87%     blur 40dp
glass.border            #FFFFFF @ 88%

shadow.tint             #073A2A           ambient 5%  spot 8%
gradient                #2E9E7A → #4F9E8E  135°
darkAccent              #5FCFA8
```

### 2.3 海盐 Sea Salt（通透青蓝）

```
background              #F6FAFB
surface                 #FFFFFF
surfaceContainerLowest  #FFFFFF
surfaceContainerLow     #F2F8FA
surfaceContainer        #E8F2F5
surfaceContainerHigh    #DFECF0
surfaceContainerHighest #D5E6EB

primary                 #3B8EA5      onPrimary            #FFFFFF
primaryContainer        #D2EBF3      onPrimaryContainer   #06343F
secondary               #7FB8C9      secondaryContainer   #DFF0F5
tertiary                #5A9CA8

outline                 #B7D3DB      outlineVariant       #D3E5EA
onBackground            #142226      onSurface            #142226
onSurfaceVariant        #495F66

error #BA1A1A   success #2E7D5B   warning #B4762A

glass.tintL1            #FFFFFF @ 74%     blur 12dp
glass.tintL2            #FFFFFF @ 80%     blur 24dp
glass.tintL3            #FFFFFF @ 87%     blur 40dp
glass.border            #FFFFFF @ 88%

shadow.tint             #06343F           ambient 5%  spot 8%
gradient                #3B8EA5 → #7FB8C9  135°
darkAccent              #6FC4DC
```

### 2.4 樱雪 Sakura（柔粉）

```
background              #FDF8F9
surface                 #FFFFFF
surfaceContainerLowest  #FFFFFF
surfaceContainerLow     #FCF5F7
surfaceContainer        #F9EDF0
surfaceContainerHigh    #F5E5EA
surfaceContainerHighest #F1DDE3

primary                 #D4788F      onPrimary            #FFFFFF
primaryContainer        #FADDE4      onPrimaryContainer   #4A1526
secondary               #E5A9B8      secondaryContainer   #FBE9EE
tertiary                #C08BA5

outline                 #E4C8D0      outlineVariant       #F0DDE2
onBackground            #2A1D21      onSurface            #2A1D21
onSurfaceVariant        #6B535A

error #BA1A1A   success #2E7D5B   warning #B4762A

glass.tintL1            #FFFFFF @ 73%     blur 12dp
glass.tintL2            #FFFFFF @ 79%     blur 24dp
glass.tintL3            #FFFFFF @ 86%     blur 40dp
glass.border            #FFFFFF @ 86%

shadow.tint             #4A1526           ambient 5%  spot 8%
gradient                #D4788F → #E5A9B8  135°
darkAccent              #F0A8BC
```

### 2.5 暖砂 Warm Sand（米杏奶油）

```
background              #FDFBF7
surface                 #FFFFFF
surfaceContainerLowest  #FFFFFF
surfaceContainerLow     #FBF7F1
surfaceContainer        #F6F0E6
surfaceContainerHigh    #F1E9DC
surfaceContainerHighest #EBE2D2

primary                 #C08B5C      onPrimary            #FFFFFF
primaryContainer        #F6E4CE      onPrimaryContainer   #4A2F14
secondary               #D9B78F      secondaryContainer   #F8EEE0
tertiary                #A89372

outline                 #DCCDB6      outlineVariant       #EDE2D2
onBackground            #2A231A      onSurface            #2A231A
onSurfaceVariant        #6B5D4B

error #BA1A1A   success #2E7D5B   warning #B4762A

glass.tintL1            #FFFEFB @ 74%     blur 12dp
glass.tintL2            #FFFEFB @ 80%     blur 24dp
glass.tintL3            #FFFEFB @ 87%     blur 40dp
glass.border            #FFFFFF @ 88%

shadow.tint             #4A2F14           ambient 6%  spot 9%
gradient                #C08B5C → #D9B78F  135°
darkAccent              #E0B888
```

### 2.6 紫藤 Wisteria（淡雅紫）

```
background              #FAF9FC
surface                 #FFFFFF
surfaceContainerLowest  #FFFFFF
surfaceContainerLow     #F7F5FB
surfaceContainer        #F1EEF7
surfaceContainerHigh    #EAE6F3
surfaceContainerHighest #E3DEEF

primary                 #7B6BA8      onPrimary            #FFFFFF
primaryContainer        #E6E0F5      onPrimaryContainer   #241A44
secondary               #A899C7      secondaryContainer   #EFEBF8
tertiary                #8E7BA8

outline                 #CFC7E0      outlineVariant       #E2DCEF
onBackground            #1F1B29      onSurface            #1F1B29
onSurfaceVariant        #575070

error #BA1A1A   success #2E7D5B   warning #B4762A

glass.tintL1            #FFFFFF @ 73%     blur 12dp
glass.tintL2            #FFFFFF @ 79%     blur 24dp
glass.tintL3            #FFFFFF @ 86%     blur 40dp
glass.border            #FFFFFF @ 87%

shadow.tint             #241A44           ambient 5%  spot 8%
gradient                #7B6BA8 → #A899C7  135°
darkAccent              #B0A0DC
```

### 2.7 墨黑 Ink（OLED 深色 · 一键暗黑）

```
background              #000000       ← 纯黑，OLED 像素熄灭省电
surface                 #0C0C0E
surfaceContainerLowest  #050506
surfaceContainerLow     #101012
surfaceContainer        #141416
surfaceContainerHigh    #1C1C1F
surfaceContainerHighest #242428

primary                 ← 使用当前浅色主题的 darkAccent（未选时用 #7C9EFF）
onPrimary               #0A1633
primaryContainer        #2A3B63       onPrimaryContainer   #D6E2FF
secondary               #A0B4E0       secondaryContainer   #2A3244
tertiary                #8FB8C4

outline                 #3A3A40       outlineVariant       #26262A
onBackground            #E8E8EA       onSurface            #E8E8EA
onSurfaceVariant        #A0A0A8

error #FFB4AB   success #6FD9A8   warning #E5B368

glass.tintL1            #18181C @ 66%     blur 12dp
glass.tintL2            #18181C @ 74%     blur 24dp
glass.tintL3            #18181C @ 84%     blur 40dp
glass.border            #FFFFFF @ 10%     ← 深色用微光边代替阴影

shadow                  深色主题不用投影，改用 outlineVariant 描边 + 表面提亮
gradient                darkAccent → darkAccent 调暗 20%   135°
```

### 2.8 主题在关键场景的表现差异

| 场景 | 浅色主题 | 墨黑 Ink |
|---|---|---|
| 画廊背景 | `background` 纯色 | 纯黑 `#000000` |
| 卡片 | 白底 + 彩色柔和阴影 | `surfaceContainer` + 1px `outlineVariant` 描边（**无阴影**，深色下不可见） |
| 顶栏/底栏 | 白色玻璃 72% + blur 12 | 深色玻璃 66% + blur 12 |
| 已看完遮罩 | 白色 @ 35% 覆盖（降对比） | 黑色 @ 45% 覆盖 |
| 播放器界面 | **强制纯黑**（与主题无关） | 纯黑 |
| 骨架屏 | `#EDEFF2` → `#F7F8FA` 扫光 | `#151517` → `#1E1E21` 扫光 |

> **播放器界面永远纯黑**，不跟随浅色主题。看视频时任何背景色都是干扰。

### 2.9 渐变的使用边界

渐变只用在**三处**，其余一律纯色：

1. **FAB / 主行动按钮** —— 135° 主渐变
2. **进度条填充** —— 从 `primary` 到 `gradient.end`
3. **海报底部信息条** —— 从透明到 `scrim`，保证白字可读

❌ 不用渐变的地方：卡片背景、页面背景、顶栏、图标、文字。滥用渐变是廉价感的头号来源。

---

## 3. 毛玻璃规范与性能红线

> 这一节是**设计与性能的交界**，也是最容易做砸的地方。

### 3.1 三级玻璃

| 级别 | blur | tint 不透明度 | 用在哪 | 是否实时模糊 |
|---|---|---|---|---|
| **L1 轻** | 12dp | 浅 72~74% / 深 66% | 顶部栏、底部导航栏 | ✅ 是（固定位置，面积小） |
| **L2 中** | 24dp | 浅 78~80% / 深 74% | 播放器控制栏、浮动操作条、下拉菜单 | ✅ 是（面积受限） |
| **L3 重** | 40dp | 浅 86~87% / 深 84% | 底部弹层、设置面板、确认弹窗背景 | ✅ 是（弹层期间列表不滚动） |

每级玻璃的构成：

```
① 背景实时模糊（RenderEffect blur）
② 主题 tint 半透明覆盖
③ 1px 内发光边框（glass.border）—— 玻璃感的关键，别省
④ 极轻的顶部高光渐变（白色 8% → 透明，高 2dp）
```

### 3.2 ★ 性能红线（不可违反）

| # | 红线 | 原因 |
|---|---|---|
| **R1** | **滚动列表内的卡片绝对不做实时模糊** | 120Hz 下每帧 8.33ms，20 个卡片各做一次 blur 必掉帧 |
| **R2** | 实时模糊的总面积 **≤ 屏幕的 35%** | 超过后 GPU 合成开销急剧上升 |
| **R3** | 模糊半径 **≤ 40dp** | 更大的半径收益递减、成本线性上升 |
| **R4** | 背景大图与隐私遮挡必须**预处理并缓存**，禁止滚动时实时 | 使用应用自有加密缓存生成模糊/马赛克缩略图，或退化为不透明隐私遮罩；当前滚动页 `Modifier.blur` 仍待替换 |
| **R5** | 滚动进行中**冻结**所有玻璃层的模糊更新 | 检测 `isScrollInProgress`，滚动时玻璃层降级为纯色 tint，停止后 120ms 恢复模糊 |
| **R6** | Android 12 以下（API < 31）**全面降级** | `RenderEffect` 仅 API 31+。低版本用「半透明纯色 + 描边 + 阴影」，不做模糊 |

### 3.3 卡片如何在不模糊的前提下有玻璃感

这是本设计的核心技巧。滚动区的卡片用这四层叠加**模拟**玻璃，零 GPU 模糊开销：

```
┌─ 卡片构成（自下而上）────────────────────────────┐
│ ① 彩色柔和投影                                   │
│    color = shadow.tint @ spotAlpha               │
│    offset y = 4dp, blur = 16dp                   │
│    ★ 用主题色调深而非纯黑 —— 高级感来源           │
│                                                  │
│ ② 卡片底色                                       │
│    浅色主题 = surface (#FFFFFF)                  │
│    深色主题 = surfaceContainer                   │
│                                                  │
│ ③ 1px 内发光边框                                 │
│    浅色 = #FFFFFF @ 85%（顶部更亮，底部渐隐）      │
│    深色 = #FFFFFF @ 10%                          │
│                                                  │
│ ④ 内容（海报图，圆角裁切）                        │
└──────────────────────────────────────────────────┘
```

**海报卡片的额外一层（可选，未实现）**：可离线提取海报主色并缓存后给投影着色。当前工程没有 Coil 或 AndroidX Palette 依赖；若实现，应在缓存生成阶段完成，绝不能在滚动时实时计算。

### 3.4 播放器控制栏的玻璃

播放器是唯一允许较大面积模糊的地方，因为**播放时列表不滚动**：

```
顶部栏：L2 玻璃，高 56dp + 状态栏 inset
        向下渐隐（模糊强度 24dp → 0），避免硬边

底部栏：L2 玻璃，高 96dp + 导航栏 inset
        向上渐隐

中央区：完全透明，不遮挡画面

自动隐藏：4 秒无操作后整体淡出（180ms），画面 100% 无遮挡
```

> 控制栏隐藏时**同时关闭模糊 RenderEffect**，不是只改透明度——隐藏的模糊层仍然消耗 GPU。

---

## 4. 排版系统

### 4.1 字体

| 项 | 规则 |
|---|---|
| 中文 | 系统默认（MagicOS 上为 HarmonyOS Sans），不打包第三方字体（省 APK 体积） |
| 数字 | **必须启用 tabular figures（等宽数字）** —— `FontFeatureSetting("tnum")` 或 `TextStyle(fontFeatureSettings = "tnum")` |
| 为什么 | 时间码 `01:23:45`、文件大小 `12.3 GB`、百分比 `62%` 在播放中每秒变化。非等宽数字会让文字宽度跳动，视觉上极其廉价 |
| 番号 | 用 Title M / 600 字重 + tabular，突出但不喧宾夺主 |

### 4.2 层级表

| 名称 | 字号 | 字重 | 行高 | 字距 | 用途 |
|---|---|---|---|---|---|
| Display | 32sp | 700 | 1.2 | -0.5 | 大标题（详情页片名、演员页姓名） |
| Headline | 24sp | 600 | 1.25 | -0.3 | 页面主标题 |
| TitleL | 20sp | 600 | 1.3 | -0.2 | 区块标题（"继续观看"） |
| TitleM | 16sp | 600 | 1.35 | 0 | **卡片番号**、列表主文本、按钮 |
| BodyL | 15sp | 400 | 1.5 | 0 | 正文、简介 |
| BodyM | 14sp | 400 | 1.5 | 0 | 次要正文、**卡片演员名** |
| LabelL | 13sp | 500 | 1.4 | 0.1 | 标签、筛选项 |
| LabelM | 12sp | 500 | 1.35 | 0.2 | 角标、小标签 |
| Caption | 11sp | 400 | 1.3 | 0.3 | 辅助信息（文件大小、日期） |
| Mono | 13sp | 400 | 1.4 | 0 | **调试面板专用**（等宽） |

### 4.3 文字颜色分级

| 级别 | 色值 | 用途 |
|---|---|---|
| 一级 | `onSurface` | 标题、番号、正文主体 |
| 二级 | `onSurfaceVariant` | 演员名、描述、次要信息 |
| 三级 | `onSurfaceVariant` @ 62% | 时间戳、文件大小、极次要信息 |
| 强调 | `primary` | 可点击文本、当前选中项 |
| 反白 | `#FFFFFF` + 文字阴影 | 叠在海报/画面上的文字（必须加 `shadow(2dp, black@40%)` 保证可读） |

### 4.4 截断规则

| 场景 | 规则 |
|---|---|
| 卡片番号 | 单行，`TextOverflow.Ellipsis` |
| 卡片演员名 | 单行，多演员时显示「主演名 他N人」，超长省略主演名而非丢掉"他N人" |
| 列表文件名 | 最多 2 行，**中间截断**（`middle ellipsis`）—— 因为文件名前后都有信息量（前缀站点名 + 番号） |
| 详情页标题 | 不截断，完整换行 |

---

## 5. 间距 · 圆角 · 阴影

### 5.1 间距（4dp 基准栅格）

```
space.xxs   4dp     图标与文字间距
space.xs    8dp     紧凑元素间距
space.sm   12dp     卡片内边距、列表项间距
space.md   16dp     ★ 页面水平边距、标准间距
space.lg   20dp     区块之间
space.xl   24dp     大区块分隔
space.xxl  32dp     页面顶部留白
space.huge 40dp     空状态上下留白
```

**画廊网格**：列间距 12dp，行间距 16dp，页面左右边距 16dp。2 列时卡片宽 = `(屏宽 - 16*2 - 12) / 2`。

### 5.2 圆角

```
radius.xs    8dp    小标签、角标
radius.sm   12dp    按钮、输入框、小图标容器
radius.md   16dp    次级卡片、图片缩略图
radius.lg   20dp    ★ 主卡片、玻璃层、海报
radius.xl   28dp    底部弹层顶部、大弹窗
radius.full 999dp   胶囊按钮、演员头像、筛选 chip
```

> 圆角只用这 6 个值。多一个都是不一致的开始。

### 5.3 阴影（★ 彩色阴影是关键）

**浅色主题**：阴影颜色 = `shadow.tint`（主题色调深），**不用纯黑**。

| 级别 | offsetY | blur | spread | alpha | 用途 |
|---|---|---|---|---|---|
| e0 | — | — | — | 0 | 平面元素 |
| e1 | 1dp | 3dp | 0 | 5% | 输入框、chip |
| e2 | 2dp | 8dp | 0 | 6% | 列表项、次级卡片 |
| **e3** | **4dp** | **16dp** | 0 | **8%** | **主卡片（画廊海报）** |
| e4 | 8dp | 28dp | 0 | 10% | 弹层、FAB |
| e5 | 12dp | 40dp | 0 | 12% | 全屏弹窗 |

**深色主题（墨黑 Ink）**：**不用阴影**。深色背景上投影不可见，改用：
- 1px `outlineVariant` 描边区分边界
- 表面提亮（`surfaceContainer` → `surfaceContainerHigh`）表达层级

**为什么彩色阴影重要**：纯黑阴影在暖色背景上会显脏发灰。用 `#4A2F14 @ 6%` 作为暖砂主题的阴影，整个界面的色调是统一的，这是廉价 UI 和高端 UI 的分界线。

### 5.4 触摸目标

所有可点击元素的实际触摸区 ≥ **48×48dp**，视觉尺寸可以更小（用 `Modifier.minimumInteractiveComponentSize()`）。480Hz 触控采样下，误触的代价被放大，触摸区不能省。

---

## 6. 动效系统

### 6.1 时长与曲线

> 120Hz 下每帧 8.33ms。180ms 的动画 = 21 帧，足够顺滑；300ms+ 会明显觉得拖沓。

| 类型 | 时长 | 曲线 | 用途 |
|---|---|---|---|
| **即时反馈** | 100ms | `LinearOutSlowIn` | 按压态、涟漪、勾选 |
| **标准过渡** | 180ms | `FastOutSlowIn` | 显隐、淡入淡出、控制栏出现 |
| **强调过渡** | 280ms | `spring(damping=0.85, stiffness=380)` | 页面切换、弹层升起 |
| **共享元素** | 380ms | `spring(damping=0.90, stiffness=280)` | 卡片 → 详情页的海报放大 |
| **微回弹** | — | `spring(damping=0.70, stiffness=500)` | 按钮按压回弹、拖动松手 |
| **主题切换** | 220ms | 颜色 token 插值 | 保留页面、播放与滚动状态 |

### 6.2 硬性规则

| # | 规则 |
|---|---|
| 1 | **所有动画走 `Modifier.graphicsLayer`**（translationX/Y、scaleX/Y、alpha、rotationZ）。绝不用会触发重新布局的属性做动画 |
| 2 | **不做列表项逐个错峰入场**。120Hz 滚动时这是纯负担，且用户会觉得慢 |
| 3 | **单个动画不超过 400ms** |
| 4 | **同屏同时运行的动画 ≤ 3 个** |
| 5 | 尊重系统「减少动画」无障碍设置（`Settings.Global.ANIMATOR_DURATION_SCALE == 0` 时全部关闭） |
| 6 | 手势跟随类动画（滑动 seek、拖动进度）**必须是 1:1 跟手**，不加任何缓动或延迟 |

### 6.3 关键转场

| 转场 | 做法 |
|---|---|
| 画廊卡片 → 详情页 | **共享元素**：海报从卡片位置放大到详情页顶部，380ms spring。其余内容 180ms 淡入 |
| 详情页 → 播放器 | 海报淡出 + 黑场淡入 180ms，播放器直接从黑场起 |
| 列表 → 播放器（无详情页） | 全屏黑场淡入 180ms，不做位移 |
| **首帧黑场淡入** | 视频首帧就绪后，画面从**纯黑 300ms 淡入**（`FastOutSlowIn`）。暗环境下避免突然全亮刺眼。可在设置中关闭 |
| **退出黑场淡出** | 退出播放器 / 播放结束时，画面 **300ms 淡出到纯黑**，再执行页面转场 |
| 播完停留界面 | 画面定格 → scrim 淡入 180ms → 三个大按钮 280ms spring 从下方 16dp 位移淡入（**同时入场，不错峰**） |
| 惯用手切换 | 控制栏按钮 280ms spring 平移到新位置，**不做淡入淡出**（保持可追踪） |
| 底部弹层 | 从底部升起 280ms spring + 背景 scrim 淡入 180ms |
| 页面栈前进/后退 | 水平位移 12% + 淡入淡出，280ms |
| 多选模式进入 | 顶栏交叉淡入 180ms，卡片出现勾选框（缩放 0.9→1.0，微回弹） |
| 主题切换 | 颜色 token 220ms 插值，不重建整屏内容 |

### 6.4 加载动效

| 场景 | 做法 |
|---|---|
| 画廊首次加载 | **骨架屏 shimmer**，扫光周期 1200ms，从左到右 |
| 缩略图未就绪 | 骨架色块（不用转圈图标） |
| 播放器缓冲 | 环形进度 + **实时速度数字**（`2.3 MB/s`），不用无意义的转圈 |
| 下拉刷新 | 系统 `PullToRefresh`，主题色 |
| 长任务（批量清理） | 确定性进度条 + 「N / M」计数 |

> 原则：**能给确定进度就绝不给转圈**。转圈是"我不知道要多久"的信号，会放大等待焦虑。

---

## 7. 核心组件规格

### 7.1 画廊卡片（最重要的组件）

```
┌──────────────────┐  ← radius.lg (20dp)
│░░░░░░░░░░░░░░░░░░│     e3 彩色阴影
│░                ░│     1px 内发光边
│░   官方海报      ░│
│░   2:3 比例      ░│     ContentScale.Crop
│░                ░│
│░ ✓          ╭──╮░│  ← 左上已看角标 / 右下演员头像
│░            │  │░│
│▓▓▓▓▓▓░░░░░░░╰──╯░│  ← 底部 3dp 进度条
└──────────────────┘
  DEMO-001           TitleM / 600 / tabular
  演员名 他2人          BodyM / onSurfaceVariant
```

| 元素 | 规格 |
|---|---|
| 网格 | 2 列 · 列间距 12dp · 行间距 16dp · 页面边距 16dp |
| 海报 | 2:3 · `radius.lg` · `ContentScale.Crop` |
| 阴影 | e3，颜色 = `shadow.tint`（P2：混入海报主色 40%） |
| 演员头像 | 右下角，圆形，直径 = 卡片宽 × 28%，2dp 白边 + e2 阴影，距边 8dp |
| 多演员 | 最多 3 个，主演最大最靠右，其余向左依次缩小 15%、重叠 40% |
| 已看角标 | 左上角，24dp 圆形，`scrim` 底 + 白色 ✓，距边 8dp |
| 已看遮罩 | 整个海报覆盖 `watchedOverlay`（浅色主题白 35% / 深色黑 45%） |
| 进度条 | 海报底部，高 3dp，`progressTrack` 底 + `gradient` 填充，仅"看了一半"时显示 |
| 文字区 | 海报下方 8dp 间距，两行，左对齐 |
| 按压态 | `scale 0.97` + 微回弹 spring |
| 长按 | 触发多选，同时轻微震动（`HapticFeedbackType.LongPress`） |
| 骨架态 | 海报位置 shimmer 色块 + 两行文字色块 |

**降级链**（元数据缺失时）：
```
官方海报 → 横版封面裁 2:3 → 视频截帧裁 2:3 → 纯色块 + 居中番号文字
演员头像 → 演员名首字彩色圆（颜色由名字 hash 稳定映射到 12 色盘）
演员名   → 隐藏该行，番号独占（不留空行）
```

### 7.2 播放器控制栏（含惯用手模式）

**惯用手 = 右手（默认）**

```
┌─────────────────────────────────────────────────┐
│ ←  DEMO-001                 [音轨][字幕][⋮]    │ ← L2 玻璃，向下渐隐
├─────────────────────────────────────────────────┤
│                                                 │
│              （手势响应区，全透明）                │
│                        字幕层                    │
├─────────────────────────────────────────────────┤
│  01:23:45 ━━━━━━━●─────────────── 02:34:52     │ ← L2 玻璃，向上渐隐
│  [1.0x][比例][PiP]        ▶   🔇   ☆   ⟳   🔒  │
│   └─ 次要区（远端）        └─ 主行动区（拇指侧集中）│
└─────────────────────────────────────────────────┘

惯用手 = 左手：主行动区整体镜像到左侧，次要区到右侧
惯用手 = 居中：传统均匀分布，不分主次
```

| 元素 | 规格 |
|---|---|
| 背景 | **强制纯黑**，不跟随主题 |
| 顶/底栏 | L2 玻璃 + 单向渐隐（避免硬边） |
| 自动隐藏 | 4 秒无操作，180ms 淡出，**同时释放模糊 RenderEffect** |
| 时间码 | tabular figures，绝不跳动 |
| 进度条 | 高 4dp（静） → 6dp（触摸） → **8dp（精细拖动模式）**；滑块 12 → 18 → **20dp** |
| 已缓冲区间 | 进度条上用 `onSurface @ 30%` 显示缓冲到哪 |
| **书签标记** | 进度条上的 3dp 竖线（`tertiary` 色），点击跳转 |
| 手势浮层 | 屏幕中央，L3 玻璃胶囊，180ms 显示后跟随手势更新，松手 400ms 后消失 |
| 锁定态 | 只保留一个解锁按钮（**惯用手侧**居中），其余全隐藏 |

**主行动区按钮（拇指易达，惯用手侧集中）**

| 按钮 | 尺寸 | 行为 |
|---|---|---|
| ▶ 播放/暂停 | **56dp**（最大） | 单击切换 |
| 🔇 静音 | 44dp | **切换式**：静音 → 再按恢复到静音前的音量。图标反映状态 |
| ☆ 收藏 | 44dp | 单击切换收藏（填充/描边图标 + 轻震）；**长按弹出五星评分条**（省一个按钮位） |
| ⟳ 方向 | 44dp | 单击锁定当前方向 / 再按恢复自动。图标反映状态 + Toast |
| 🔒 屏幕锁 | 44dp | 锁定全部手势，只留解锁按钮 |

**次要区按钮（远端，44dp）**：倍速 `1.0x` · 画面比例 · PiP。其余进 ⋮ 菜单。

> 所有按钮触摸区 ≥ 48×48dp（视觉尺寸可小于此）。惯用手切换时按钮位置用 280ms spring 平移，不做淡入淡出。

**五星评分条**（长按 ☆ 弹出）

```
      ╭──────────────────────╮
      │  ★  ★  ★  ☆  ☆      │  ← L3 玻璃胶囊，圆角 full
      ╰──────────────────────╯     星 32dp，间距 8dp
```
点选后 **1.5 秒自动收起**；点击别处立即收起。

### 7.3 底部弹层（Bottom Sheet）

| 元素 | 规格 |
|---|---|
| 圆角 | 顶部 `radius.xl` (28dp) |
| 背景 | L3 玻璃 |
| 拖拽把手 | 顶部居中，32×4dp，`outline` 色，圆角 full |
| 背景遮罩 | `scrim`，180ms 淡入 |
| 升起动效 | 280ms spring |
| 最大高度 | 屏幕 85%，超出内部滚动 |
| 用于 | 字幕设置、排序选择、主题选择、影片详情、批量清理预览 |

### 7.4 确认弹窗

| 类型 | 规格 |
|---|---|
| 普通确认 | 标题 TitleL + 正文 BodyL + 取消/确认。确认为 `primary` 填充按钮 |
| **危险确认** | 标题前加 ⚠️ 图标（`error` 色），确认按钮为 `error` 填充，**按钮文字必须是具体动作**（「彻底删除」而非「确定」） |
| 批量删除 | 正文列出「N 个文件 · 共 XX GB」，附「可在回收站还原」说明 |
| 圆角 | `radius.xl` |
| 背景 | L3 玻璃 |

### 7.5 底部导航（仪表盘布局时）

| 元素 | 规格 |
|---|---|
| 背景 | L1 玻璃 |
| 高度 | 64dp + 导航栏 inset |
| 图标 | 24dp，选中时填充版 + `primary`，未选中时线性版 + `onSurfaceVariant` |
| 标签 | LabelM，选中时显示，未选中时也显示（不做隐藏动画，减少视觉跳动） |
| 选中指示 | 图标后方胶囊背景 `secondaryContainer`，宽 64dp 高 32dp，切换时 280ms spring 位移 |

### 7.6 筛选 Chip

| 状态 | 规格 |
|---|---|
| 未选中 | `surfaceContainer` 底 + `outline` 1px 边 + `onSurfaceVariant` 文字 |
| 选中 | `primaryContainer` 底 + 无边 + `onPrimaryContainer` 文字 + 前置 ✓ 图标 |
| 尺寸 | 高 36dp，水平内边距 14dp，圆角 full |
| 切换 | 100ms 颜色过渡 + 图标 150ms 缩放入场 |

### 7.7 演员头像

| 场景 | 尺寸 | 规格 |
|---|---|---|
| 卡片角标 | 卡片宽 × 28% | 圆形，2dp `surface` 色描边，e2 阴影 |
| 详情页列表 | 56dp | 圆形，1px `outlineVariant` 边 |
| 演员页大图 | 120dp | 圆形，3dp `surface` 边，e3 阴影 |
| 头像墙 | 72dp | 圆形，间距 12dp |
| 占位（无头像） | 同上 | 演员名首字，白色 TitleM，底色从 12 色盘按名字 hash 稳定取 |

**占位色盘**（低饱和，与所有主题兼容）：
```
#7B8FA8  #86BFA8  #7FB8C9  #E5A9B8  #D9B78F  #A899C7
#8FA8B8  #9BC4A8  #8FBFC9  #DFA8B0  #C9B08F  #A89BC7
```

### 7.8 播完停留界面

播到结尾时**不跳回列表**，停在最后一帧。

```
┌───────────────────────────────────────────┐
│                                           │
│        （最后一帧画面，静止）                │
│         上覆 scrim 遮罩 55%                │
│                                           │
│    ╭─────────╮ ╭─────────╮ ╭─────────╮   │
│    │    ↻    │ │   ▶▶    │ │    🗑    │   │
│    │  重看   │ │  下一个  │ │ 删除本片 │   │
│    ╰─────────╯ ╰─────────╯ ╰─────────╯   │
│                                           │
│      下一个：DEMO-002 · 演员名             │
└───────────────────────────────────────────┘
```

| 元素 | 规格 |
|---|---|
| 背景 | 最后一帧静止画面 + `scrim` @ 55% |
| 按钮 | **96 × 96dp**（远超最小触摸区，手持时好按），L2 玻璃底，`radius.lg` |
| 图标 / 标签 | 32dp 图标 + LabelL 标签，垂直居中 |
| 主按钮 | 「下一个」用 `primary` 填充；另两个用玻璃底 |
| 危险操作 | 「删除本片」点击后**仍走二次确认弹窗**，不直接删 |
| 自动连播开启时 | 「下一个」按钮外圈显示 **30 秒倒计时环**（`gradient` 色），走完自动播下一个 |
| 自动连播关闭时 | **保持停留**，不自动退出 |
| 下一个预告 | 底部小字 BodyM，显示下一个的番号与主演 |
| 入场 | 画面定格后 280ms spring，三个按钮从下方 16dp 位移淡入（**同时**，不错峰） |
| 无下一个时 | 只显示「重看」和「删除本片」两个按钮，居中排列 |

---

## 8. 状态设计（空 / 加载 / 错误）

> 这一节是"做 APP 最容易漏但用户天天碰到"的部分。**每个页面的每种状态都要设计**，不能只做成功态。

### 8.1 空状态清单

| # | 场景 | 插图 | 主文案 | 副文案 | 行动按钮 |
|---|---|---|---|---|---|
| 1 | 没有任何服务器 | 服务器图标 | 还没有添加共享 | 扫描局域网，或手动填写地址 | **扫描局域网** / 手动添加 |
| 2 | 扫描不到任何主机 | 雷达图标 | 没有发现 SMB 服务器 | 确认电脑已开启共享，且手机与电脑在同一网络 | 重新扫描 / 手动添加 |
| 3 | 目录为空 | 文件夹图标 | 这个文件夹是空的 | — | 返回上级 |
| 4 | 目录有文件但无视频 | 文件夹图标 | 没有找到视频文件 | 可在设置中开启「显示全部文件」 | 显示全部文件 |
| 5 | 搜索无结果 | 放大镜图标 | 没有匹配的结果 | 试试其他关键词 | 清空搜索 |
| 6 | 筛选后无结果 | 漏斗图标 | 当前筛选条件下没有内容 | — | 清除筛选 |
| 7 | 没有观看历史 | 时钟图标 | 还没有观看记录 | 看过的视频会出现在这里 | 去浏览 |
| 8 | 回收站为空 | 垃圾桶图标 | 回收站是空的 | 删除的视频会先放在这里，可以还原 | — |
| 9 | 画廊无元数据 | 画框图标 | 还没有影片信息 | 在电脑上运行刮削脚本后，这里会显示海报和演员 | 查看使用说明 |
| 10 | 演员页无作品 | 人像图标 | 没有该演员的作品 | — | — |
| 11 | 无收藏 | 星形图标 | 还没有收藏 | 长按视频可以收藏 | — |
| 12 | 批量清理无可清理项 | 对勾图标 | 没有符合条件的视频 | 试试放宽清理条件 | 修改条件 |

**空状态视觉规格**：插图 96dp（线性图标，`onSurfaceVariant` @ 40%）→ 24dp 间距 → 主文案 TitleM → 8dp → 副文案 BodyM `onSurfaceVariant` → 24dp → 行动按钮。整体垂直居中偏上 40%。

### 8.2 加载状态

| 场景 | 表现 | 不要用 |
|---|---|---|
| 首次进目录 | 骨架屏（卡片形状 shimmer），显示预估数量的占位卡 | ❌ 全屏转圈 |
| 缓存命中后台刷新 | **直接显示缓存内容**，顶部一条 2dp 进度线表示后台在刷新 | ❌ 遮挡内容的加载态 |
| 图片加载中 | 骨架色块 | ❌ 转圈图标 |
| 播放器缓冲 | 环形进度 + `2.3 MB/s` 实时速度 | ❌ 无信息转圈 |
| 扫描局域网 | 进度条 + 「已扫描 128/254」+ 已发现列表实时追加 | ❌ 只有转圈 |
| 批量删除 | 确定性进度条 + 「12 / 45」+ 当前文件名 | ❌ 不确定进度 |
| 刮削元数据解析 | 顶部细进度线（非阻塞） | ❌ 阻塞弹窗 |

### 8.3 错误状态与文案

> **所有错误文案必须是中文、具体、可行动**。禁止出现 `Exception`、`null`、`errno`、堆栈。

| 场景 | 文案 | 行动 |
|---|---|---|
| 连不上服务器 | 无法连接到 <SMB_HOST> | 检查电脑是否开机、是否在同一网络 → **重试** |
| 认证失败 | 用户名或密码不正确 | **修改凭据** |
| 权限不足 | 没有访问这个共享的权限 | 检查电脑上的共享权限设置 |
| 共享不存在 | 找不到共享「<SMB_SHARE>」 | 名称可能已更改 → **重新选择共享** |
| 文件已不存在 | 这个文件已经不在了 | 可能已被移动或删除 → **刷新** |
| 播放失败（格式） | 这个视频当前内核放不了 | **用 VLC 内核重试** |
| 播放中断网 | 连接中断，正在重连… (2/5) | 自动重试，5 次失败后 → **重试** |
| 删除失败-占用 | 文件正在被使用，无法删除 | 关闭电脑上打开该文件的程序后重试 |
| 删除失败-权限 | 没有删除权限 | 检查共享权限是否为「完全控制」 |
| 回收站跨共享 | 无法移入回收站 | 回收站与视频不在同一个共享 → **改用彻底删除** |
| 磁盘空间不足 | 电脑磁盘空间不足 | — |
| 字幕解析失败 | 字幕文件无法解析 | **手动选择其他字幕** |
| 元数据文件损坏 | 影片信息文件读取失败 | 在电脑上重新运行刮削脚本 |

**错误展示层级**：
- **非阻塞**（不影响当前操作）→ Snackbar，4 秒自动消失，带「重试」
- **阻塞**（当前操作失败）→ 页面内错误态（图标 + 文案 + 重试按钮）
- **致命**（无法继续）→ 弹窗，必须用户确认

---

## 9. 图标与插图

| 项 | 规格 |
|---|---|
| 图标库 | 当前使用 Compose `material-icons-extended` 的 **Material Icons Rounded**；迁移到 Material Symbols 属于后续决策 |
| 线宽 | 2dp（`weight 400`，`grade 0`，`optical size 24`） |
| 尺寸 | 24dp（标准）/ 20dp（紧凑）/ 32dp（强调）/ 96dp（空状态插图） |
| 选中态 | 切换为填充版（`fill 1`），配 100ms 过渡 |
| 应用图标 | 自适应图标（前景 + 背景层），支持主题化图标（Android 13+ 单色版） |
| 通知图标 | 纯白剪影，24dp |
| 插图 | 不用第三方插画库。空状态一律用**放大的线性图标** + 主题色，保持克制统一 |

---

## 10. 无障碍基线

> 不是为了合规，是为了自己用着舒服。

| 项 | 要求 |
|---|---|
| 文字对比度 | 正文 vs 背景 ≥ **4.5:1**；大字(18sp+)与图标 ≥ **3:1**。当前尚未完成全部主题和 Dynamic 的脚本化验证 |
| 触摸目标 | ≥ 48×48dp |
| 内容描述 | 所有纯图标按钮必须有 `contentDescription` |
| 状态传达 | 观看状态**不能只靠颜色**——已看完同时有 ✓ 图标，看了一半同时有进度条和百分比数字 |
| 字体缩放 | 统一验收 100%/125%/150%；使用 `sp`、可换行布局和非固定文字容器，当前仍待多尺寸目标机验证 |
| 减少动画 | 尊重系统设置，关闭时所有动画时长归零 |
| 深色模式 | 纯黑背景 + `#E8E8EA` 文字（不用纯白，降低 OLED 眩光） |

---

## 11. 实现清单

开发时按此顺序落地，每步都可独立验证。

- [△] **1. 色板常量** —— 6 套浅色、Ink 与 Dynamic 路径已存在，命名仍是精简实现
- [ ] **2. 完整主题数据结构** —— 当前没有 `LanPlayColors/GlassSpec/ShadowSpec/GradientSpec`
- [ ] **3. 自定义 CompositionLocal** —— 当前只使用 `MaterialTheme`
- [△] **4. 排版** —— 基础 Typography 已有，时间码 `tabular figures` 与 150% 字体仍待验证
- [△] **5. 形状与间距** —— Shapes 已有，统一 `Spacing` 对象未完成
- [ ] **6. 彩色阴影 Modifier** —— 未实现
- [ ] **7. 玻璃 Modifier** —— 未实现；当前以 Material 表面层级替代
- [ ] **8. 滚动冻结** —— 未实现；隐私图片实时 blur 仍需改为预处理/遮罩
- [△] **9. 画廊卡片组件** —— 功能已存在，视觉、隐私性能和多尺寸门禁未通过
- [ ] **10. 骨架屏组件** —— 未形成统一 shimmer 组件
- [△] **11. 空/错误状态组件** —— 多页面已有实现，尚未统一
- [x] **12. 主题选择器** —— 设置页使用真实迷你预览卡
- [△] **13. Ink 深色模式** —— 已有设置入口，但不是顶栏统一“一键暗黑”交互
- [ ] **14. 动效常量** —— 未形成全局 `Motion` 对象
- [ ] **15. 对比度校验** —— 尚无全部主题/Dynamic 的脚本化证据
- [△] **16. Macrobenchmark** —— harness 和 profile 生成器已存在，当前候选版本尚未在荣耀目标机重跑

---

## 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-07-26 | 初版。7 套主题完整色板、毛玻璃规范与性能红线、排版/间距/圆角/彩色阴影、动效系统、核心组件规格、空/加载/错误状态全清单、无障碍基线 |
| **v1.1** | 2026-07-26 | §7.2 播放器控制栏重写：**惯用手模式**（主行动区向拇指侧集中，左/右/居中三档）、主行动区五按钮规格（播放/静音切换/收藏兼长按评分/方向锁/屏幕锁）、进度条三档粗细（含精细拖动态）、书签标记；新增 **§7.8 播完停留界面**（最后一帧 + 重看/下一个/删除本片三个 96dp 大按钮 + 30 秒倒计时环）；§6.3 新增**首帧黑场淡入 / 退出淡出 300ms**、播完界面入场、惯用手切换转场 |
| **v1.2** | 2026-07-28 | 对齐当时 Compose 实现：明确 6 套浅色 + Ink + Dynamic、颜色插值而非整屏 Crossfade、Material Icons、无 Coil/Palette、隐私 blur 性能缺口、150% 字体和对比度待验；§11 改为实现声明清单。 |
| **v1.3** | 2026-07-29 | 移除对旧修复清单的引用；明确 §11 仅是待独立复核的实现声明，新审查必须以当前源码、Macrobenchmark 和目标机证据为准。 |
