# 局域网 SMB 视频播放器 · 需求文档

> 产品名：**LanPlay** · applicationId：`com.lanplay.player`
> 文档版本：**v3.4**
> 更新日期：2026-07-29
> 目标设备：**荣耀 WIN RT** · Android 16 · 骁龙 8 Elite · **UI 实际可用 120Hz**

### 2026-07-29 当前契约（优先于后文历史规划）

- **网络边界：** Android APP 只访问用户配置的局域网 SMB、局域网发现目标和 `127.0.0.1` 回环代理；不访问公网。演员/封面刮削只由 PC 工具执行，APP 内不提供公网刮削开关。
- **隐私遮挡：** 目标覆盖浏览、详情、仪表盘、历史和演员页的海报/头像。当前多个滚动页仍使用实时图片 blur，尚未完全符合性能红线；应改为预生成模糊图、马赛克或不透明遮罩。播放视频层禁用模糊。
- **音频焦点：** 来电、永久/临时焦点丢失和 duck 请求均暂停，不自动恢复；用户手动继续。
- **代理与字幕：** HTTP 代理仅绑定回环、随机 token、64 位 Range；VobSub 以 `.idx/.sub` 会话临时目录成组提供，停止播放或下次启动清理。VLC 只使用原生字幕 Surface；当前仅字号明确透传，颜色、描边、背景和底距不保证生效，MagicOS ASS/VobSub 视觉仍是阻塞门禁。
- **阶段门禁：** 每阶段只依赖该阶段及之前已交付能力。第 3 阶段不再要求尚属第 4 阶段的 libVLC/VobSub/进阶播放项；全量 §10 只用于最终候选版本。
- **证据口径：** §2 的 20 视频 + 20 字幕 / 161.8GB 是历史冻结基线。素材变化后必须版本化新清单；10 视频 / 69.18GB 的历史实测不得表述为全量样本通过。
- **安全边界：** SMB 客户端默认要求消息签名；签名私钥、口令、内网地址、共享名、用户名和完整路径不得进入公开源码交付、日志或剪贴板导出。本文示例已脱敏；本机 `config.toml`、`verify.ps1`、`local.properties` 仍可能保存本地默认值，外部分享目录前必须另做全量扫描和清理。

---

## 文档结构

项目规格由三份文档组成。当前工作区不保留历史审查报告和修复清单；未来新会话必须直接核对源码、重新生成的构建输出与《交付验收说明.md》。规格定义目标，不代表已经实现或通过验收：

| 文档 | 内容 | 什么时候看 |
|---|---|---|
| **需求文档.md**（本文件） | 做什么、优先级、数据模型、验收标准、路线图 | 决定做不做、做到什么程度 |
| **[设计系统.md](./设计系统.md)** | 6 套浅色 + Ink/Dynamic、表面层级与玻璃目标、排版/动效/组件/无障碍状态 | 写 UI 代码时 |
| **[播放器规格.md](./播放器规格.md)** | SMB IO 层、双内核、解码优化、蓝牙延迟补偿、完整手势系统、调试面板 | 写播放引擎时 |
| **[交付验收说明.md](./交付验收说明.md)** | 当前 APK、已验证证据与历史结果边界 | 交付与验收时 |

## 交付物

| # | 交付物 | 运行在 | 作用 |
|---|---|---|---|
| **①** | **LanPlay.apk** | 荣耀 WIN RT | 播放器主体。**全程不访问外网** |
| **②** | **lanplay-scraper**（Python + 双击 bat） | PC (Windows) | 刮削演员/封面/头像元数据到共享目录。**只有它联网** |

两者通过共享里的 `.lanplay_meta\` 解耦：没跑过刮削脚本，APP 一样正常播放，画廊自动回退到视频截帧。

---

## 目录

- [1. 背景与目标](#1-背景与目标)
- [2. 环境基线（已实测）](#2-环境基线已实测)
- [3. 现有 APP 失败的根因](#3-现有-app-失败的根因)
- [4. 产品定位与范围](#4-产品定位与范围)
- [5. 技术方案总纲](#5-技术方案总纲)
- [6. 功能需求清单](#6-功能需求清单)
- [7. 数据模型](#7-数据模型)
- [8. 页面结构](#8-页面结构)
- [9. 非功能需求（性能指标）](#9-非功能需求性能指标)
- [10. 验收测试清单](#10-验收测试清单)
- [11. 风险与对策](#11-风险与对策)
- [12. 待确认事项](#12-待确认事项)
- [13. 开发路线图](#13-开发路线图)
- [14. PC 端刮削工具](#14-pc-端刮削工具)
- [15. 完整性清单（做 APP 容易漏的）](#15-完整性清单做-app-容易漏的)
- [附录 A：服务端调优](#附录-a服务端调优建议)
- [附录 B：术语表](#附录-b术语表)
- [附录 C：番号识别规则](#附录-c番号识别规则表)

---

## 1. 背景与目标

### 1.1 现状

历史冻结基线中，PC 上有一个 SMB 共享的视频库（`<VIDEO_DIR>`，161.8 GB / 20 个视频）。想在手机上直接播放，但试过的所有现成播放器都有致命问题：卡顿、连不上、不能删、不记进度、手势难用。

### 1.2 目标

| # | 目标 |
|---|---|
| 1 | 连上 `\\<SMB_HOST>\<SMB_SHARE>\<VIDEO_SUBDIR>`，秒开、不卡、拖动跟手 |
| 2 | 外挂字幕自动识别、自动加载、渲染正确 |
| 3 | 实时观看记录，**杀后台也不丢**，自动续播 |
| 4 | 手机上直接删除（含字幕），误删可救回，支持**按观看状态一键批量清理** |
| 5 | **启动极快、120Hz 下动画不掉帧** |
| 6 | **一键扫描局域网**发现 SMB 服务器并自定义命名 |
| 7 | 文件再多也不卡，缩略图及时显示，排序自选 |
| 8 | 未看 / 看了一半 / 已看完 **三态区分** |
| 9 | **演员识别 + 头像 + 画廊模式**，可开可关 |
| 10 | **极其精致漂亮**：6 套浅色主题 + Ink/Dynamic、清晰表面层级；完整毛玻璃是待验目标 |
| 11 | **手势精确可控**：横滑幅度可调、音量亮度软上限保护、全部带记忆 |
| 12 | **无线耳机适配**：延迟补偿、按设备记忆、断连自动暂停 |
| 13 | **解码性能最大化**，充分吃满骁龙 8 Elite |

---

## 2. 历史冻结环境基线（当前待复核）

> 以下数据来自早期本机采样，只用于定义冻结测试集，不代表 2026-07-28 当前 APK 已通过。当前网络、共享、素材或设备变化时，必须生成新的版本化 manifest。

### 2.1 服务端（PC）

| 项 | 实测值 | 意义 |
|---|---|---|
| 主机名 / IP | `<SMB_NAME>` / `<SMB_HOST>` | DHCP 分配，建议固定（附录 A-5） |
| 共享名 | `<SMB_SHARE>` → `<SMB_LOCAL_ROOT>` | **含中文**，客户端必须处理 UTF-8 共享名 |
| 目标目录 | `<VIDEO_SUBDIR>` | `\\<SMB_HOST>\<SMB_SHARE>\<VIDEO_SUBDIR>` |
| SMB1 | **已关闭** | ⚠️ 客户端**必须**支持 SMB2/3 |
| SMB2/3 | 已开启 | SMB 2.1 / 3.1.1 |
| 服务端强制签名 / 加密 | 历史采样：均未强制，当前待复核 | 客户端默认要求消息签名；加密按服务端能力与显式策略决定，不以关闭签名作为默认优化 |
| SMB2 信用上限 | `2048` | ✅ **流水线预读的前提** |
| 共享 / NTFS 权限 | 均 `Everyone = Full` | ✅ 删除功能可行 |
| 账户 | `<SMB_USER>`（主）、`Guest`（启用） | 建议建专用账户（附录 A-2） |

### 2.2 媒体素材

**历史冻结规模（当前须复核）**：20 个视频（14 mp4 + 6 mkv）+ 20 个 `.srt`，0 个子目录，161.8 GB，平均 8.1 GB，最大 12.3 GB。

**编码（ffprobe 实测）**

| 文件 | 时长 | 码率 | 视频 | 分辨率 | 帧率 | 音频 |
|---|---|---|---|---|---|---|
| `source.example@DEMO-001_[4K].mkv` | 154.9 min | **10.74 Mbps** | H.264 Main **L5.1** 8bit | 3840×2160 | 29.97 | AAC 2ch |
| `source.example@DEMO-002.mp4` | 232.8 min | 6.94 Mbps | H.264 Main 8bit | 1920×1080 | 59.94 | AAC 2ch |
| `DEMO-003_CH-source.example.mp4` | 126.4 min | 3.80 Mbps | H.264 Main 8bit | 1920×1080 | 29.97 | AAC 2ch |
| `source.example@DEMO-004.mp4` | 202.8 min | 5.73 Mbps | H.264 Main 8bit | 1920×1080 | 29.97 | AAC 2ch |

**冻结 v1 样本的历史探测结果**

- 当时 20 个视频均为 H.264 Main / yuv420p 8bit，音频为 AAC 2ch，且没有内封字幕。
- 最大样本 12.3GB，因此所有偏移必须使用 64 位 `Long`。
- 120Hz 与 29.97/59.94fps 的倍率接近，理论 cadence 误差较小；是否有抖动仍以目标机动态证据为准。
- 当前冻结样本已经不完整，不能把这些结论推广为当前媒体库或当前 APK 全量通过。

**历史字幕样本**：SubRip `.srt`，UTF-8 无 BOM，简体中文，50~84KB，CRLF，与视频同名。

**文件名特征**：附录 C 保存冻结 v1 样本的预期映射；当前尚缺 20 个文件名的参数化自动测试，不能继续表述为当前 20/20 已验证。

### 2.3 网络链路

| 项 | 值 |
|---|---|
| PC 网卡 | Realtek **2.5GbE**，但实际协商 **100 Mbps** ⚠️ |
| 带宽余量 | 需 1.34 MB/s，可用 ~11 MB/s，**余量 8 倍** |

历史早期采样曾显示 100Mbps；后续小米 13 回归记录为 31.86MB/s，说明网络状态或测试路径已经变化。当前链路待重新采集；8MB/s 是门禁下限，未重测时不得排除网络因素。

### 2.4 客户端：荣耀 WIN RT

| 项 | 规格 | 对开发的意义 |
|---|---|---|
| 类型 | 手机 | 竖屏浏览 + 横屏播放，不需要平板双栏 |
| 屏幕 | 6.83" LTPS OLED 直屏，2800×1272 | ≈460 PPI，图片按显示尺寸解码 |
| 刷新率 | 硬件峰值 185Hz，**系统 UI 实际可用 120Hz** | **帧预算 8.33 ms**，比 60Hz 严一倍 |
| 触控采样 | 480 Hz | 手势必须 1:1 跟手，零缓动 |
| SoC | **骁龙 8 Elite**（Oryon + Adreno 830） | 4K H.264 硬解毫无压力 |
| 内存 / 存储 | LPDDR5X Ultra + UFS 4.1 | 缓存读写极快 |
| 电池 / 散热 | 10000 mAh + **主动散热风扇** | 长时间播放无热降频 |
| 系统 | Android 16 / MagicOS | targetSdk 36，需处理后台管控（R-1） |

> 来源：[荣耀官网](https://www.honor.com/cn/phones/honor-win-rt/spec/)、[太平洋](https://product.pconline.com.cn/mobile/honor/2786071_detail.html)、[中关村在线](https://detail.zol.com.cn/cell_phone/index2152408.shtml)

### 2.5 当前构建环境快照

| 项 | 当前值 |
|---|---|
| Android Studio | AI-253.32098.37.2534.15336583（安装路径不进入公开文档） |
| JDK | Temurin 21.0.11；源码与 Kotlin JVM target 为 17 |
| SDK | `%ANDROID_HOME%` |
| compileSdk / targetSdk / minSdk | 37 / 36 / 26 |
| Build Tools / adb | 37.0.0 |
| NDK | 28.2.13676358（本机另有 27.x） |
| Gradle Wrapper | 9.6.1 |
| AGP / Kotlin / KSP | 9.3.1 / 2.3.21 / 2.3.10 |

> 这是桌面构建环境，不等同于目标机验收环境；缺失组件应由构建错误驱动安装，不能沿用旧缓存版本假设。

---

## 3. 现有 APP 失败的根因

| 症状 | 根因 | 本项目对策 |
|---|---|---|
| **连不上** | PC 已关闭 SMB1，而很多播放器用老 `jcifs` 只支持 SMB1 | 用 **smbj**（SMB 2/3） |
| **卡顿** | SMB IO 层烂：小块同步读、无预读、seek 重建连接、不用 credits、无 WifiLock | [播放器规格 §1](./播放器规格.md#1-smb-io-层) 完整重做 |
| **手势难用** | 横滑用「屏宽 = 总时长」映射，2.5h 的片子手指动 1cm 跳 11 分钟 | [播放器规格 §5.2](./播放器规格.md#52--横滑-seek--解决幅度太大) 绝对时长映射 |
| **音画不同步** | 蓝牙延迟无补偿手段 | [播放器规格 §4](./播放器规格.md#4-音频链路与蓝牙延迟补偿) |
| **不能删 / 不记进度 / 没演员** | 产品取舍，定位是"只读播放器" | 本项目全部作为一等公民功能 |

---

## 4. 产品定位与范围

### 4.1 一句话定位

> 一个只连自己家 SMB 共享的、能删能记能认人的、手势精确、戴耳机不出戏、在 120Hz 屏上丝般顺滑且足够漂亮的专用播放器。

### 4.2 明确不做

| 不做 | 理由 |
|---|---|
| 服务端转码 / Jellyfin 那套媒体中心 | 直连原码流，PC 不装常驻服务 |
| 投屏 / DLNA / Chromecast | 场景是手机本机观看 |
| 云盘 / WebDAV / FTP / NFS | 只做 SMB，专注 |
| 账号 / 多用户 / 云同步 | 单人单设备 |
| 上架应用商店 | 侧载即可 |
| 视频编辑 / 转码 / 下载到本地 | 超出范围 |
| **APP 内自动联网刮削** | 与零外发隐私冲突，改为 PC 端刮削 |
| 内置浏览器 / 在线字幕搜索 / 广告 | 见[播放器规格 §7.2](./播放器规格.md#72-明确不学的) |

---

## 5. 技术方案总纲

### 5.1 技术栈

| 层 | 当前实现 |
|---|---|
| 语言 / UI | Kotlin 2.3.21 + Jetpack Compose Material 3（Compose BOM 2026.06.01） |
| SDK | min 26 / compile 37 / target 36 / release ABI `arm64-v8a` |
| 播放内核 | Media3 1.10.1（主）+ libVLC 3.6.5（兜底、ASS/VobSub、音频延迟） |
| SMB | smbj 0.14.0；共享枚举使用 jcifs-ng 2.1.10 |
| 本地 HTTP 代理 | NanoHTTPD 2.3.1，有界 AsyncRunner、会话/字节预算、64 位 Range |
| 数据库 | Room 2.8.4 + SQLCipher 4.15.0，WAL；当前未引入 Paging 3 |
| 设置 | DataStore Preferences 1.2.1 |
| 凭据与缓存 | Android Keystore + AES-256-GCM；应用锁 PBKDF2 + Keystore HMAC pepper |
| 图片 | 应用自有加密缓存与按目标尺寸 Bitmap 解码；当前未使用 Coil/Palette |
| 后台任务 | Kotlin 协程与应用自有后台线程；当前未使用 WorkManager |
| 编码检测 | BOM、严格 UTF-8及 GB18030/GBK/Big5/Shift_JIS/EUC-JP 启发式评分 |
| 启动优化 | Baseline Profile + Startup Profile；生成文件待目标机重新采集 |
| 性能验证 | Macrobenchmark + `tools/verify.ps1`，动态门禁尚待目标机复跑 |
| DI | Hilt 2.60.1 |

**权限清单**

```xml
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.USE_BIOMETRIC" />
<!-- 当前不声明 BLUETOOTH_CONNECT，因此不读取具体蓝牙 codec -->
```

> **不申请**任何存储 / 相册 / 通讯录 / 定位 / 摄像头权限。缓存全部写应用私有目录。

### 5.2 架构分层

```
┌──────────────────────────────────────────────────────────┐
│  UI 层 (Compose) —— 主题引擎见 设计系统.md                  │
│  首页(画廊/仪表盘可切) · 浏览 · 播放器 · 详情 · 演员          │
│  历史 · 回收站 · 批量清理 · 设置                            │
└─────────────────────────┬────────────────────────────────┘
┌─────────────────────────▼────────────────────────────────┐
│  领域层                                                    │
│  播放会话 · 观看记录 · 字幕匹配 · 删除编排 · 元数据聚合        │
│  局域网扫描 · 批量清理策略 · 音频设备管理                     │
└──────┬────────────┬─────────┬──────────┬─────────────────┘
┌──────▼─────┐┌─────▼────┐┌───▼────┐┌────▼──────────────┐
│播放内核抽象 ││ Room DB  ││DataSto ││ 元数据读取         │
│ ├ Media3   ││(记录/元  ││re      ││ (.lanplay_meta)   │
│ └ libVLC   ││ 数据/回  ││(设置/  │└───────────────────┘
│            ││ 收站/音  ││ 主题)  │
│            ││ 频档案)  │└────────┘
└──────┬─────┘└──────────┘
       │  http://127.0.0.1:PORT/stream?token=...
┌──────▼───────────────────────────────────────────────────┐
│  ★ SMB IO 层（见 播放器规格.md §1）                         │
│  本地HTTP代理 ← 预读缓冲池 ← 流水线读取器 ← smbj Session池   │
└──────┬───────────────────────────────────────────────────┘
       │  SMB2/3 over TCP 445
┌──────▼───────────────────────────────────────────────────┐
│  \\<SMB_HOST>\<SMB_SHARE>\                               │
│    ├── <VIDEO_SUBDIR>\           视频 + 字幕                        │
│    ├── .lanplay_meta\   演员/封面/头像（PC 脚本生成）        │
│    └── .lanplay_trash\  回收站                             │
└──────────────────────────────────────────────────────────┘
                            ▲ 写入
              ┌─────────────┴──────────────┐
              │ ② PC 端刮削脚本（§14）       │
              │   联网 → JavDB/JavBus/     │
              │          Gfriends          │
              └────────────────────────────┘
```

### 5.3 核心技术要点（详见配套文档）

| 主题 | 一句话 | 详见 |
|---|---|---|
| SMB IO 层 | 本地 HTTP 代理 + Range + 4~8 路流水线 + 48MB 前向预读 + 窗口内 seek 零请求 | [播放器规格 §1](./播放器规格.md#1-smb-io-层) |
| 双内核 | Media3 为主，ASS/DTS/音频延迟场景切 libVLC，SMB 层不动 | [播放器规格 §2](./播放器规格.md#2-双内核策略) |
| 解码最大化 | 高通硬解器 + SurfaceView 零拷贝 + 异步队列 + 播放时零 GPU 特效 | [播放器规格 §3](./播放器规格.md#3-解码性能最大化) |
| 蓝牙音频 | 信任系统自动补偿，提供手动微调 + 按设备记忆 + 校准工具 | [播放器规格 §4](./播放器规格.md#4-音频链路与蓝牙延迟补偿) |
| 手势系统 | 绝对时长映射 + 垂直偏移调精度 + 软上限保护 + 全局记忆 | [播放器规格 §5](./播放器规格.md#5-手势系统) |
| 主题引擎 | 当前为 6 套浅色主题 + Ink 深色模式，Android 12+ 可选动态取色 | [设计系统 §1-2](./设计系统.md#1-主题引擎架构) |
| 毛玻璃 | 当前以 Material 表面层级为主；完整玻璃和彩色阴影仍是设计目标，滚动区与视频层禁用实时背景模糊 | [设计系统 §3](./设计系统.md#3-毛玻璃规范与性能红线) |
| 元数据管线 | PC 端刮削写 `.lanplay_meta`，APP 只读，缺失静默降级 | §14 |

### 5.4 SMB 认证

认证模式为 `ANONYMOUS / GUEST / ACCOUNT`；域是账户模式的可选字段。服务器配置存入 SQLCipher Room，密码字段另经 Android Keystore AES-256-GCM 加密；DataStore 只保存非服务器类设置。文档和默认配置不得内置真实用户名或密码。

### 5.5 120Hz 性能架构

| 措施 | 说明 |
|---|---|
| **Baseline Profile** | AOT 预编译热路径，启动提升 20~40%，首次滚动 jank 显著下降。**必做** |
| **Startup Profile** | 优化 DEX 布局，减少启动页面调入 |
| `Application.onCreate` 轻量化 | 同步阶段只初始化本地错误记录和崩溃处理器，并异步清理字幕临时目录/迁移缓存；SQLCipher 首次迁移仍可能在 Hilt provider 同步执行，对应 DATA-04 |
| 延迟初始化 | SMB Session、HTTP 代理、播放内核**只在真要播时**创建 |
| **首页零网络** | 启动只读 Room 缓存渲染，SMB 刷新在后台，完成后增量更新 |
| **Strong Skipping** | Kotlin 2.0+ 默认；数据类标 `@Immutable`，集合用 `ImmutableList` |
| **LazyGrid key + contentType** | 大列表复用与 diff 的前提 |
| **动画走 graphicsLayer** | 只触发绘制，不触发布局与重组 |
| **图片按显示尺寸解码** | `ArtworkDecoder` 使用采样解码，不把原图完整塞进小卡片 |
| **缓存优先渲染** | 进目录立即显示缓存（0 延迟），后台刷新增量替换 |
| **元数据探测限流** | 并发上限 2，独立低优先级 SMB Session，播放期间降到 1 |
| **R8 full mode** | + `isShrinkResources = true` |
| **Macrobenchmark 验证** | 不靠肉眼，量化 P99 帧耗时与 jank 率 |

---

## 6. 功能需求清单

> **编号体系**：按模块字母前缀分组（新增功能不用重排编号）。
> **优先级**：**P0** 缺了项目不成立 · **P1** 第一版应该有 · **P2** 第二版。

### 6.1 C —— 连接与浏览

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| C-01 | P0 | **一键扫描局域网 SMB 服务器** | 当前实现扫描各私网 IPv4 对应 `/24` 的 TCP 445，并对命中主机做有界反向 DNS；不实现 NetBIOS/mDNS。≤3 秒目标待真实网络复测 |
| C-02 | P0 | 选主机 → 输入凭据 → **列出该主机全部共享** | 列出 `<SMB_SHARE>` 等；`$` 结尾的管理共享默认隐藏 |
| C-03 | P0 | **自定义命名并保存** | 可填「家里PC」，服务器列表显示该名称 |
| C-04 | P0 | 手动添加（扫描失败时兜底） | 手填 IP/端口/共享/凭据，「测试连接」3 秒内返回 |
| C-05 | P0 | 服务器列表：多位置，可编辑/删除/排序 | 重启 APP 后配置仍在 |
| C-06 | P0 | 凭据加密存储 | adb 查看 DataStore，密码为密文 |
| C-07 | P0 | 目录浏览，进子目录 / 返回上级 | 进入 `<VIDEO_SUBDIR>` 显示 20 个视频 |
| C-08 | P0 | 中文路径与特殊字符 | 共享名 `<SMB_SHARE>`、文件名含 `@ [ ] .` 全部正常 |
| C-09 | P0 | **默认只显示视频文件** | `<VIDEO_SUBDIR>` 下默认只见 20 个视频，看不到 `.srt` |
| C-10 | P0 | 设置项：**显示全部文件** | 打开后 `.srt` 显示；`.` 开头目录（`.lanplay_meta` / `.lanplay_trash`）**始终隐藏** |
| C-11 | P0 | **排序**：名称/大小/修改时间/时长/最后观看，升降序 | 5×2 共 10 种组合正确，**按目录分别记忆**，重启保留 |
| C-12 | P0 | 缓存优先渲染 | 二次进目录 **≤100ms** 出内容，后台刷新增量更新 |
| C-13 | P1 | 搜索：按文件名/番号/演员名实时过滤 | 输入 `START` 或演员名正确过滤 |
| C-14 | P1 | 文件信息：大小/修改时间/时长/分辨率 | 缺失时不显示，而非显示 0 |
| C-15 | P1 | 元数据探测与本地缓存 | 首次后台探测写 Room，二次直接读缓存，**不重复探测** |
| C-16 | P1 | 视频缩略图 | 取 **10%** 位置一帧，磁盘缓存，20 个 ≤ 60 秒（后台，不阻塞） |
| C-17 | P1 | 视图切换：列表 / 网格 / **画廊** | 三种流畅切换，选择被记住 |
| C-18 | P1 | 下拉刷新 | 强制重列目录 + 刷新元数据 |
| C-19 | P1 | 状态筛选：全部 / 未看 / 看了一半 / 已看完 | 数量正确 |
| C-20 | P2 | 目录统计 | 「共 N 个 · XX GB · 已看 N 个」 |

### 6.2 P —— 播放核心

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| P-01 | P0 | SMB 直连播放（不下载整个文件） | 12.3 GB 4K 文件 **≤ 3 秒**出画面 |
| P-02 | P0 | **硬件解码** | 调试面板解码器名为 `c2.qti.*` 或 `OMX.qcom.*`（**不能是 `c2.android.*`**） |
| P-03 | P0 | 流畅播放不缓冲 | 连播 2 小时，卡顿 **0 次** |
| P-04 | P0 | 拖动 seek | 缓冲窗口内**瞬时**；窗口外 **≤1.5 秒** |
| P-05 | P0 | 播放/暂停/上一个/下一个 | — |
| P-06 | P0 | **>4 GB 偏移正确** | 拖到 12.3 GB 文件末尾正常播放不崩溃 |
| P-07 | P1 | 倍速 0.5x~3.0x（0.25 步进） | 变速不变调（sonic 音频处理器） |
| P-08 | P1 | 画面比例：原始/铺满/16:9/4:3/拉伸/裁切 | — |
| P-09 | P1 | 音轨切换 | 用多音轨文件验证 |
| P-10 | P1 | 后台播放 + 前台服务通知 | 切桌面音频继续，通知栏控制有效 |
| P-11 | P1 | 画中画（PiP） | 上滑进 PiP，继续播放 |
| P-12 | P1 | 息屏防断流（WifiLock + WakeLock） | 锁屏 5 分钟解锁，播放未中断 |
| P-13 | P1 | 断线自动重连 | 拔网线 5 秒插回，自动恢复且**不从头开始** |
| P-14 | P1 | 自动连播下一个 | 播完自动播同目录下一个（可关） |
| P-15 | P1 | **刷新率适配** | 播放时向 Surface 声明实际视频帧率，由系统选择合适的倍频显示模式；退出清除声明。目标机记录最终 60/90/120Hz 选择结果 |
| P-16 | P1 | 双内核一键切换 | 保留进度与全部参数，切换 ≤1.5 秒 |
| P-17 | P1 | 解码器档位：硬解 / 硬解+ / 软解 | 切换后调试面板解码器名相应变化 |
| P-18 | P1 | **调试面板** | 见[播放器规格 §8](./播放器规格.md#8-调试面板)，含告警高亮 |

### 6.3 G —— 手势系统

> 完整交互规格见[播放器规格 §5](./播放器规格.md#5-手势系统)。以下是需求与验收。

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| G-01 | P0 | **横滑 seek 用绝对时长映射** | 1 屏宽 = 固定秒数，**与视频总时长无关**。默认 90 秒/屏宽 |
| G-02 | P0 | 横滑灵敏度可配 | 30/60/90/120/180/300 秒，改后立即生效 |
| G-03 | P1 | **垂直偏移调精度** | 横滑时上下移动手指切换 ×0.25/×0.5/×1/×2/×4 档，切档震动 + 浮层显示档位。可关 |
| G-04 | P0 | **左半屏上下滑 = 亮度** | 控制窗口亮度，不改系统亮度；退出播放器恢复系统亮度 |
| G-05 | P0 | **右半屏上下滑 = 音量** | 控制系统媒体音量流 |
| G-06 | P0 | **软上限保护** | 到达阈值（默认 50%）时**卡住 + 震动 + 提示**，抬手后 5 秒内再次上滑才能继续升高。阈值可配 40/50/60/70/关闭。**下滑不受限** |
| G-07 | P0 | **亮度/音量全局记忆** | 记住绝对值，**下次打开 APP 或看其他视频都相同**。存 DataStore（全局，非按视频） |
| G-08 | P1 | 双击左/右 = 快退/快进 | 默认 ±10 秒，可配 5/10/15/30 |
| G-09 | P1 | 双击中央 = 播放/暂停 | 可关（关闭后中央也走快进快退） |
| G-10 | P1 | 单击 = 显示/隐藏控制栏 | 4 秒无操作自动隐藏，**隐藏时释放模糊 RenderEffect** |
| G-11 | P1 | 长按 = 临时倍速 | 默认 2.0x，可配 1.5/2/2.5/3。松手恢复 |
| G-12 | P1 | 双指捏合缩放 + 双指拖动平移 | 缩放后可平移 |
| G-13 | P1 | **双指纵滑 = 字幕时间轴偏移**（借鉴 MX Player） | 1/10 屏高 = ±0.5 秒，实时生效，抬手保存到该视频 |
| G-14 | P1 | 手势方向锁定 | 移动 16dp 后判定方向，**直到抬手不再改变** |
| G-15 | P1 | 死区 | 横滑 16dp / 纵滑 12dp 内不响应 |
| G-16 | P1 | 所有手势可独立开关 + 调参 | 设置页结构见播放器规格 §5.7，附「恢复默认」 |
| G-17 | P1 | 震动反馈 | 档位切换、到达阈值、边界时震动。可关 |
| G-18 | P2 | 锁定手势（防误触） | 一键锁屏，锁定后手势失效，长按解锁 |
| G-19 | P2 | 三指点击截图 | 默认关 |
| G-20 | P2 | 边缘 24dp 让给系统返回手势 | 默认让出，可占用 |

### 6.4 S —— 字幕

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| S-01 | P0 | 自动匹配同名外挂字幕 | 打开 `xxx.mkv` 自动加载 `xxx.srt`，无需操作 |
| S-02 | P0 | 匹配规则支持后缀变体 | 精确 → 语言后缀 → 前缀模糊，见 §7.3 |
| S-03 | P0 | 格式：`srt` `ass` `ssa` `vtt` `sub`+`idx` `ttml` `smi` | `srt`/`ass`/`vtt` 必须正确 |
| S-04 | P0 | **编码自动识别** | UTF-8(有/无BOM)、UTF-16 LE/BE、GBK、GB18030、BIG5、Shift-JIS、EUC-JP。**当前素材 UTF-8 无 BOM，零乱码** |
| S-05 | P0 | 手动指定编码 | 播放中切换，字幕**立即**重载 |
| S-06 | P0 | 正确渲染 | 不乱码、不重叠、不闪烁、时间轴对齐、换行正确 |
| S-07 | P1 | 手动挑选字幕文件 | 弹出同目录字幕列表任选 |
| S-08 | P1 | 样式自定义 | 字号 50%~250%、颜色、描边色与粗细、背景框、垂直位置、底部边距、字体 |
| S-09 | P1 | 时间轴偏移 | ±0.1/±0.5 秒微调，范围 ±60 秒，**按视频记忆** |
| S-10 | P1 | 字幕开关 | 一键开关，状态按视频记忆 |
| S-11 | P2 | 双字幕同屏 | 主下副上，各自独立样式与偏移 |
| S-12 | P2 | ASS/SSA 特效完整渲染 | 走 libVLC，特效正确 |
| S-13 | P2 | 字幕搜索跳转 | 输入关键词跳到出现该词的时间点 |

### 6.5 W —— 观看记录与状态

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| W-01 | P0 | **实时记录进度** | 播放中**每 3 秒**写 Room；以下节点**立即**写：暂停、seek 完成、`onStop`、播放结束、退出播放器 |
| W-02 | P0 | **杀后台不丢记录** | 播到 10 分钟，任务管理器**强杀**，重进进度仍在（误差 ≤3 秒）。Room 用 WAL 模式 |
| W-03 | P0 | 自动续播 | 从上次位置继续，Toast「已从 XX:XX:XX 继续」+「从头开始」按钮 |
| W-04 | P0 | 续播策略可配 | 总是续播（默认）/ 每次询问 / 总是从头 |
| W-05 | P0 | **三态区分** | 未看 = 无记录或 <2%；看了一半 = 2%~95%；已看完 = ≥95% 或播到底或手动标记 |
| W-06 | P0 | **状态角标** | 未看无标记；看一半 = 底部进度条 + 百分比；已看完 = ✓ 角标 + 卡片降透明到 65% |
| W-07 | P0 | 记录不因重命名失效 | 主键 `serverId + 路径`；辅以 `size + mtime` 探测重命名自动迁移 |
| W-08 | P1 | 手动标记状态 | 长按菜单「标记为已看完」/「重置为未看」 |
| W-09 | P1 | 「继续观看」 | 最近未看完的，按时间倒序，最多 20 个 |
| W-10 | P1 | 观看历史页 | 封面/文件名/演员/进度/时长/最后观看/观看次数，可排序 |
| W-11 | P1 | 删除历史记录 | 长按多选删除；「清空全部」二次确认 |
| W-12 | P1 | **播放参数记忆** | 按视频记：速度、字幕选择、字幕开关、字幕偏移、音轨、画面比例、缩放 |
| W-13 | P1 | 收藏/星标 | 首页单独分区；**批量清理时默认排除** |
| W-14 | P2 | 记录导出/导入（JSON） | 新设备可导入恢复 |
| W-15 | P2 | 记录同步到共享 | 默认**关**，开启后写 `.lanplay_meta\history.json`，多设备共享 |

### 6.6 D —— 删除、回收站与批量清理

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| D-01 | P0 | 删除视频 | 长按 → 删除，**同时删除匹配的字幕** |
| D-02 | P0 | **回收站机制** | SMB `rename` 移到共享根 `.lanplay_trash\`。**是重命名不是复制，12 GB 也是瞬间** |
| D-03 | P0 | 二次确认 | 显示文件名、大小、「字幕一并移入」「可还原」 |
| D-04 | P0 | 回收站页面 | 原路径 / 大小 / 删除时间 |
| D-05 | P0 | 还原 | 视频+字幕一起回原路径；同名冲突时提示并允许改名 |
| D-06 | P0 | 彻底删除 | **红色警告**，按钮文字「彻底删除」而非「确定」 |
| D-07 | P0 | 长按多选批量删除 | 全选/反选，底部「已选 N 项 · XX GB」 |
| D-08 | P0 | **一键清理「已看完」** | 走强制预览流程（见下） |
| D-09 | P0 | **一键清理「看了一半」** | 同上 |
| D-10 | P0 | **一键清理两者一起** | 同上，不重复计数 |
| D-11 | P1 | 清理保护规则 | ① 默认**排除收藏**；② 可选「只清 N 天前看过的」（不限/7/30/90）；③ 可选范围「当前目录 / 全部目录」 |
| D-12 | P1 | 回收站自动清理 | 从不 / 7 / 30 / 90 天，APP 启动时检查执行 |
| D-13 | P1 | 删除同步清理本地数据 | 观看记录、缩略图、海报、雪碧图缓存一并清理 |
| D-14 | P1 | 播放器内删除 | 播放中/结束后直接删除当前视频并返回 |
| D-15 | P1 | 删除失败明确报错 | 权限不足/被占用/断网分别给中文提示，**不静默失败** |
| D-16 | P2 | 重命名文件 | 视频与字幕同步重命名 |
| D-17 | P2 | 存储空间显示 | 共享所在磁盘总量/已用/剩余 |

**【批量清理强制流程】（D-08 ~ D-10）**

```
① 选清理类型（已看完 / 看了一半 / 两者）
      ↓
② 设置保护条件（排除收藏、只清 N 天前、范围）
      ↓
③ ★ 强制预览页（不可跳过）
   每行：缩略图 + 文件名 + 演员 + 大小 + 状态 + 最后观看时间 + 勾选框（默认全选，可单独取消）
   顶部实时：「将删除 N 个 · 共 XX GB」
      ↓
④ 二次确认：「将 N 个文件移入回收站，共 XX GB。可在回收站还原。」
      ↓
⑤ 执行（确定性进度条 + 「12/45」+ 当前文件名，失败项单列）
      ↓
⑥ 结果页：「成功 N，失败 M」，失败项可查看原因并重试
```

> ⚠️ 预览页**强制**，不允许跳过。一次误操作可能是几百 GB。

### 6.7 M —— 演员元数据与画廊

> 依赖 PC 端刮削脚本（§14）。**没有元数据时画廊自动禁用并回退普通列表，不报错。**

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| M-01 | P1 | 读取 `.lanplay_meta\index.json` | 启动/下拉刷新时读取合并进 Room；`lastModified` 未变则跳过解析 |
| M-02 | P1 | **画廊模式开关** | 关 = 普通视频列表（文件名+信息）；开 = 海报画廊（封面+演员头像） |
| M-03 | P1 | **画廊卡片** | 2 列 2:3 海报，右下圆形演员头像，左上 ✓ 角标，底部进度条，下方番号+演员名。规格见[设计系统 §7.1](./设计系统.md#71-画廊卡片最重要的组件) |
| M-04 | P1 | 多演员显示 | 最多 3 个头像叠放，主演最大；文字「主演名 他N人」 |
| M-05 | P1 | **优雅降级** | 海报缺 → 封面裁 2:3 → 视频截帧 → 纯色+番号；头像缺 → 首字彩色圆；元数据全缺 → 只显示文件名，**不报错不留空洞** |
| M-06 | P1 | 影片详情页 | 大封面、番号、标题、日期、时长、片商、系列、类别、演员列表、文件信息、观看状态 |
| M-07 | P1 | 图片缓存 | 应用自有 Keystore AES-GCM 私有缓存，上限可配 512MB/1GB/2GB，LRU 淘汰并按展示尺寸解码 |
| M-08 | P2 | 演员页 | 点头像 → 该演员全部作品网格 + 大头像 + 作品数/已看数 |
| M-09 | P2 | 演员索引页 | 头像墙，按作品数/名称排序，可搜索 |
| M-10 | P2 | 按演员筛选 | 文件列表顶部筛选 |
| M-11 | P2 | 演员关注 | 关注的演员有新作品时首页高亮 |
| M-12 | — | **APP 内公网刮削（取消）** | 与零公网边界冲突；Android APP 不实现。手动补充通过本地覆盖或 PC `lanplay-scraper` 完成 |
| M-13 | P2 | 手动修正元数据 | 番号识别错时手动输入正确番号重匹配（本地覆盖表，不改共享的 index.json） |

### 6.8 B —— 浏览效率组 ★

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| B-01 | **P0** | **列表滚动位置记忆** | 从播放器返回列表时**停在原来的位置**，不跳回顶部。切换目录/筛选/排序时重置 |
| B-02 | P1 | **最近新增视图** | 按文件 `lastModified` 倒序，7 天内的打 `NEW` 角标。一眼看出哪些是新下的 |
| B-03 | P1 | **随机抽一部** | 首页/工具栏一个按钮，从「未看」中随机抽一个直接播（可设为从全部抽） |
| B-04 | P2 | 多目录合并浏览 | 把多个目录的视频合到一个列表里看 |
| B-05 | P2 | 重复番号检测 | 同番号不同文件时提示，显示两者大小/分辨率便于取舍 |
| B-06 | P2 | 按片商/系列/发行日期筛选 | 依赖刮削元数据 |
| B-07 | P2 | 面包屑快速跳转 | 点面包屑任意一级直接跳过去 |

### 6.9 A —— 播放进阶组 ★

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| A-01 | P1 | **Seek 预览缩略图** | 拖动进度条/横滑时显示目标位置画面。用**预生成雪碧图**（每 10 秒一帧，160×90），首播时后台生成并缓存。可关 |
| A-02 | P1 | **手动书签打点** | 播放中一键打点（可命名），进度条上显示标记，点击跳转，列表可管理 |
| A-03 | P1 | 视频旋转 | 0/90/180/270，按视频记忆 |
| A-04 | P1 | 视频镜像翻转 | 水平/垂直，按视频记忆 |
| A-05 | P1 | 截图保存当前帧 | 保存到应用私有目录（不写系统相册，符合隐私要求），可在 APP 内查看/分享 |
| A-06 | P1 | **下一集提前预缓存** | 用 Media3 `DefaultPreloadManager` 预加载下一个视频的开头，**切集零等待** |
| A-07 | P2 | 播放队列 | 可查看/调整当前播放列表顺序 |
| A-08 | P2 | 随机 / 单曲循环 / 列表循环 | — |
| A-09 | P2 | A-B 循环 | 打两个点循环播放中间段 |
| A-10 | P2 | 跳过片头/片尾 | 按视频设定并记忆，下次自动跳 |

### 6.10 O —— 个人整理组 ★

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| O-01 | P1 | **自定义标签** | 给视频打任意标签（多个），可按标签筛选，标签可管理（重命名/删除/改色） |
| O-02 | P1 | **个人五星评分** | 详情页/长按菜单打分，画廊卡片可选显示星级 |
| O-03 | P1 | 备注 | 每个视频可写一段自由文本 |
| O-04 | P1 | 按评分排序 | 加入排序选项 |
| O-05 | P1 | **网速测试** | 对当前共享做纯读测速（读 100MB 丢弃），报告实际 SMB 吞吐 |
| O-06 | P1 | **连接诊断** | 逐项检查：ping → 445 端口 → SMB 协商 → 认证 → 列目录 → 读文件，每项给中文结果与建议 |
| O-07 | P2 | **观看统计报表** | 总观看时长、看过多少部、最常看的演员/片商/标签、月度回顾图表 |
| O-08 | P2 | 收藏夹分组 | 收藏可分多个自定义夹 |

### 6.11 U —— 音频与蓝牙 ★

> 完整技术方案见[播放器规格 §4](./播放器规格.md#4-音频链路与蓝牙延迟补偿)。

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| U-01 | **P0** | **音频焦点处理** | 来电、其他应用播放音频时**自动暂停**。这是基础正确性，不做就是 bug |
| U-02 | **P0** | **耳机拔出/蓝牙断开自动暂停** | 监听 `ACTION_AUDIO_BECOMING_NOISY`，立即暂停避免外放 |
| U-03 | P0 | 蓝牙重连不自动恢复播放 | 显示通知让用户手动继续，避免突然出声 |
| U-04 | P1 | **蓝牙延迟补偿** | 可调 -500ms ~ +500ms，粗调 50ms / 微调 10ms。**默认 0（信任系统自动补偿）** |
| U-05 | P1 | **音画同步校准工具** | 播放中实时调整所见即所得；显示当前输出设备与编解码；「归零」一键恢复默认 |
| U-06 | P1 | **按设备记忆补偿值** | 记住每个耳机的校准值，下次连同一设备自动应用 |
| U-07 | P1 | 输出设备切换实时响应 | 播放中拔耳机/连蓝牙 → 自动切换对应补偿值 + Toast 提示 |
| U-08 | P1 | 线控 / AVRCP 按键 | 耳机的播放/暂停、上一个/下一个有效 |
| U-09 | P1 | 锁屏 / 通知栏控制 | 默认只显示脱敏标题、进度和控制按钮，不显示文件名、路径、演员或封面 |
| U-10 | P2 | 蓝牙编解码识别 | 当前未实现且未声明 `BLUETOOTH_CONNECT`；现阶段只识别输出设备大类。实现权限和降级路径后再进入验收 |
| U-11 | P2 | 音量增强到 200% | 默认关，开启时提示可能失真 |
| U-12 | P2 | 响度均衡 | 抑制忽大忽小 |
| U-13 | P2 | 均衡器 | 5~10 段 EQ + 预设 |
| U-14 | P2 | 外挂音轨 | 加载同目录独立音频文件作为音轨 |

### 6.12 T —— 主题与外观 ★

> 完整色板与规范见[设计系统.md](./设计系统.md)。

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| T-01 | P1 | **主题与深色模式** | 6 套浅色主题 + Ink 深色模式；Android 12+ 另有 Dynamic 选项 |
| T-02 | P1 | **暗黑模式** | 设置中切换；颜色 token 220ms 插值，不整屏 Crossfade、不丢页面状态 |
| T-03 | P1 | **暗黑保留主题个性** | 切 Ink 时使用**当前浅色主题的 `darkAccent`** 作为强调色，不是千篇一律的蓝 |
| T-04 | P1 | 跟随系统 / 强制浅 / 强制深 | 默认跟随系统 |
| T-05 | P1 | **主题真实预览** | 选择器显示迷你卡片预览（假海报 + 玻璃条 + 按钮），**不是纯色圆点** |
| T-06 | P1 | **不只是换色** | 每套主题有自己的玻璃着色、**彩色阴影**（主题色调深而非纯黑）、渐变 |
| T-07 | P1 | **毛玻璃质感** | 三级玻璃（L1/L2/L3）用于顶栏/控制栏/弹层；**滚动区卡片不做实时模糊**（性能红线） |
| T-08 | P1 | **首页布局可切换** | 画廊直达（默认）/ 仪表盘，设置中实时切换，共用同一套组件与数据 |
| T-09 | P1 | 主题立即生效 | 无需重启，无需重载数据 |
| T-10 | P2 | 动态取色（Material You） | 6 套浅色之外的 Dynamic 选项，默认不启用；需 Android 12+ |

### 6.13 V —— 隐私保护

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| V-01 | P1 | 应用锁 | 启动/后台恢复要求指纹或面容，失败回退 PIN；宽限期可配（立即/1分钟/5分钟） |
| V-02 | P1 | 任务切换器隐藏画面 | `FLAG_SECURE`，多任务显示纯色遮罩，同时禁止截屏录屏 |
| V-03 | P1 | 图片模糊开关 | 缩略图/海报/头像一键高斯模糊，长按临时取消 |
| V-04 | P1 | 不写入系统媒体库 | 不调 MediaStore，不产生相册记录；缓存全在 `filesDir`/`cacheDir` |
| V-05 | P1 | **零公网外发** | 只允许用户触发的本地子网 445 扫描/反向 DNS、已配置 SMB 目标和 `127.0.0.1` 回环；不得出现公网目的地址、遥测、广告或崩溃上报 |
| V-06 | P2 | 伪装图标/名称 | 可改为普通工具类外观 |
| V-07 | P2 | 一键清空痕迹 | 历史 + 图片缓存 + 元数据缓存 + 最近列表 + 截图，二次确认 |
| V-08 | P2 | 缓存加密 | 图片与元数据缓存用 Keystore 密钥加密落盘 |

### 6.14 X —— 设置与其他

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| X-01 | P1 | 设置页分组 | 连接 / 播放 / 手势 / 字幕 / 音频 / 外观 / 画廊与元数据 / 隐私 / 存储与缓存 / 高级 / 关于 |
| X-02 | P1 | 缓冲参数可调 | 预读 16/32/48/64MB、读块 512KB/1MB/2MB、并发 2/4/6/8，立即生效 |
| X-03 | P1 | 缓存管理 | 分项显示缩略图/海报/头像/雪碧图/元数据缓存大小，可分别清理 |
| X-04 | P1 | **首次启动引导** | 见 §15.1 |
| X-05 | P1 | 关于页 | 版本号、构建时间、开源许可、长按版本号开调试面板 |
| X-06 | P1 | 恢复默认设置 | 分组恢复（只恢复手势 / 只恢复播放 / 全部），二次确认 |
| X-07 | P2 | 错误日志 | 最近 200 条，可查看/复制/导出 |
| X-08 | P2 | 数据备份与恢复 | 导出全部设置 + 观看记录 + 标签评分为一个文件，可导入 |
| X-09 | P2 | 性能覆盖层 | 开发者选项，实时 FPS / 帧耗时 / 重组次数 |

### 6.15 N —— 单手与观影场景优化 ★

> 这一组解决"手机拿在手上、只有一只手方便操作、环境偏暗"时的实际问题。技术细节见[播放器规格 §5.8~5.11](./播放器规格.md#58-屏幕方向锁定)。

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| N-01 | **P0** | **屏幕方向锁定 · 快捷层** | 控制栏方向按钮，**切换式**：按下 = **锁死此刻的实际方向**（含反向横屏），怎么翻都不转；再按 = 恢复自动。图标反映当前状态 + Toast 提示 |
| N-02 | **P0** | **屏幕方向 · 默认档** | 设置 → 播放 → 进入播放器时的方向：**自动**（默认）/ 强制横屏 / 强制竖屏。选择被记忆<br>⚠️ 「自动」档用 `SCREEN_ORIENTATION_SENSOR`，**会覆盖系统的旋转锁定开关**——用户全局关了自动旋转，播放器内依然能转 |
| N-03 | **P1** | **手势响应区可调** | 可设定只在屏幕指定区域响应手势：全屏（默认）/ 下 2/3 / 下 1/2 / 中间 60% / 自定义上下边距。**区域外触摸只响应单击**（显隐控制栏），不触发滑动手势 |
| N-04 | P1 | 手势区可视化调整 | 设置中调整时显示半透明区域示意图，实时预览边界 |
| N-05 | **P1** | **物理音量键调音量** | 播放中按侧边音量键调节，**同样受软上限保护**（G-06），浮层与手势调节完全一致 |
| N-06 | P2 | 音量键长按切换视频 | 长按音量上 = 下一个，长按音量下 = 上一个。**默认关**（避免误触） |
| N-07 | **P1** | **进度条精细拖动** | 长按进度条 400ms 进入精细模式，拖动速度降为 **1/8**，可定位到秒。浮层显示「精细」标识 |
| N-08 | P1 | **进度条快捷跳转** | 双击进度条弹出 0% / 25% / 50% / 75% 快捷点，单击直接跳转 |
| N-09 | **P1** | **一键静音** | 控制栏独立按钮，**切换式**（再按恢复到静音前的音量，不是恢复到默认值）。比滑到底再滑回来快得多 |
| N-10 | P1 | **一键收藏** | 控制栏按钮，星标切换，即时生效无需确认，轻震反馈 |
| N-11 | P1 | **一键评分** | 控制栏按钮，点开弹出五星条，选完自动收起（1.5 秒） |
| N-12 | **P1** | **惯用手模式** | 左手 / 右手 / 居中三档。控制栏关键按钮（播放、静音、收藏、评分、锁定、方向）向对应侧集中，**拇指不用横跨 6.83 寸屏** |
| N-13 | **P1** | **播完不跳回列表** | 播到结尾停在**最后一帧**（画面保留），叠加三个大按钮：**重看 / 下一个 / 删除本片**。可在设置中改回「自动播下一个」 |
| N-14 | P1 | 播完界面的自动行为 | 30 秒无操作后：开了自动连播 → 播下一个；否则**保持停留**，不自动退出 |
| N-15 | **P1** | **黑场淡入淡出** | 开始播放时画面从纯黑 **300ms 淡入**；播放结束/退出播放器时 **300ms 淡出**到黑。避免暗环境下突然全亮刺眼。可关 |

---

## 7. 数据模型

> 本节代码块是早期概念模型，不是当前可复制的 Room schema。当前权威结构见 `LanPlay/app/src/main/java/com/lanplay/player/data/db/Entities.kt` 与 `LanPlay/app/schemas/.../4.json`。数据库为 SQLCipher Room v4；演员资料已按 `(serverId, name)` 隔离，稳定 server UUID 仍是已知缺口。

### 7.1 Room 实体（历史概念模型）

```kotlin
// ── 服务器配置 ────────────────────────────────
@Entity(tableName = "smb_server")
data class SmbServer(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val displayName: String,          // 用户自定义 "家里PC"
    val host: String, val hostName: String?, val port: Int = 445,
    val shareName: String, val domain: String?,
    val username: String,
    val encryptedPassword: ByteArray, val passwordIv: ByteArray,
    val authMode: AuthMode,           // ANONYMOUS / ACCOUNT / CUSTOM
    val defaultPath: String,
    val sortOrder: Int, val createdAt: Long
)

// ── 观看记录（核心） ──────────────────────────
@Entity(tableName = "watch_record", indices = [
    Index(value = ["serverId", "fullPath"], unique = true),
    Index(value = ["fileSize", "lastModified"]),   // 重命名探测
    Index(value = ["lastWatchedAt"]),
    Index(value = ["watchState"])                  // 批量清理查询
])
data class WatchRecord(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val serverId: Long, val fullPath: String, val fileName: String,
    val fileSize: Long, val lastModified: Long,

    // 进度与状态
    val positionMs: Long, val durationMs: Long,
    val progressPercent: Float,
    val watchState: WatchState,       // UNWATCHED / IN_PROGRESS / COMPLETED
    val manuallyMarked: Boolean = false,

    // 统计
    val firstWatchedAt: Long, val lastWatchedAt: Long,
    val playCount: Int, val totalWatchedMs: Long,

    // 播放参数记忆（W-12）
    val playbackSpeed: Float = 1.0f,
    val subtitlePath: String?, val subtitleCharset: String?,
    val subtitleEnabled: Boolean = true, val subtitleOffsetMs: Long = 0,
    val audioTrackIndex: Int = -1,
    val aspectRatioMode: Int = 0,
    val zoomScale: Float = 1f, val zoomOffsetX: Float = 0f, val zoomOffsetY: Float = 0f,
    val rotationDegrees: Int = 0, val mirrorH: Boolean = false, val mirrorV: Boolean = false,
    val skipIntroMs: Long = 0, val skipOutroMs: Long = 0,

    // 个人整理（O 组）
    val isFavorite: Boolean = false,
    val rating: Int = 0,              // 0~5 星
    val note: String? = null
)

enum class WatchState { UNWATCHED, IN_PROGRESS, COMPLETED }

// ── 标签（O-01） ──────────────────────────────
@Entity(tableName = "tag", indices = [Index(value = ["name"], unique = true)])
data class Tag(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String, val colorHex: String, val sortOrder: Int
)

@Entity(tableName = "record_tag", primaryKeys = ["recordId", "tagId"],
        indices = [Index("tagId")])
data class RecordTag(val recordId: Long, val tagId: Long)

// ── 书签打点（A-02） ──────────────────────────
@Entity(tableName = "bookmark", indices = [Index("recordId")])
data class Bookmark(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val recordId: Long, val positionMs: Long,
    val label: String?, val thumbnailPath: String?, val createdAt: Long
)

// ── 文件元数据缓存（技术层） ──────────────────
@Entity(tableName = "media_meta",
        indices = [Index(value = ["serverId", "fullPath"], unique = true)])
data class MediaMeta(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val serverId: Long, val fullPath: String, val fileName: String,
    val fileSize: Long, val lastModified: Long, val isDirectory: Boolean,
    val durationMs: Long?, val width: Int?, val height: Int?,
    val videoCodec: String?, val audioCodec: String?,
    val bitrate: Long?, val frameRate: Float?,   // frameRate 用于刷新率适配
    val thumbnailPath: String?,
    val spriteSheetPath: String?,                // seek 预览雪碧图（A-01）
    val probedAt: Long, val probeFailed: Boolean = false
)

// ── 影片元数据（来自 index.json） ──────────────
@Entity(tableName = "movie_info", indices = [
    Index(value = ["serverId", "fullPath"], unique = true), Index(value = ["code"])
])
data class MovieInfo(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val serverId: Long, val fullPath: String,
    val code: String,                 // "DEMO-001"
    val title: String?, val titleZh: String?,
    val releaseDate: String?, val runtimeMin: Int?,
    val studio: String?, val label: String?, val series: String?,
    val genres: List<String>,         // TypeConverter -> JSON
    val posterRelPath: String?, val coverRelPath: String?,
    val source: String?, val scrapedAt: Long,
    val userOverride: Boolean = false  // 手动修正过（M-13）
)

// ── 演员 ──────────────────────────────────────
@Entity(tableName = "actor", indices = [Index(value = ["name"], unique = true)])
data class Actor(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val name: String, val nameZh: String?,
    val avatarRelPath: String?, val isFollowed: Boolean = false
)

@Entity(tableName = "movie_actor", primaryKeys = ["movieId", "actorId"],
        indices = [Index("actorId")])
data class MovieActor(
    val movieId: Long, val actorId: Long,
    val isMain: Boolean, val orderIndex: Int
)

// ── 回收站 ────────────────────────────────────
@Entity(tableName = "trash_item", indices = [Index("groupId")])
data class TrashItem(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val serverId: Long,
    val originalPath: String,         // "<VIDEO_SUBDIR>/xxx.mkv"
    val trashPath: String,            // ".lanplay_trash/1753500000_xxx.mkv"
    val fileName: String, val fileSize: Long,
    val deletedAt: Long,
    val groupId: String,              // 同次删除的 UUID（视频+字幕同组）
    val itemType: ItemType            // VIDEO / SUBTITLE
)

// ── 音频设备档案（U-06） ──────────────────────
@Entity(tableName = "audio_device_profile")
data class AudioDeviceProfile(
    @PrimaryKey val deviceKey: String,   // 设备名 + 地址 hash（不存明文 MAC）
    val displayName: String,             // "AirPods Pro"
    val outputType: AudioOutput,
    val codec: String?,
    val audioDelayMs: Long,              // 用户校准值
    val lastUsedAt: Long
)

// ── 浏览状态（B-01 滚动位置记忆） ──────────────
@Entity(tableName = "browse_state",
        indices = [Index(value = ["serverId", "dirPath"], unique = true)])
data class BrowseState(
    @PrimaryKey(autoGenerate = true) val id: Long = 0,
    val serverId: Long, val dirPath: String,
    val scrollIndex: Int, val scrollOffset: Int,
    val sortField: String, val sortAscending: Boolean,
    val viewMode: String,             // LIST / GRID / GALLERY
    val updatedAt: Long
)
```

### 7.2 全局设置（DataStore）

```kotlin
// 手势与播放（全局记忆，非按视频）—— G-07 要求
lastVolumePercent: Float        // 0~1，进播放器时恢复
lastBrightnessPercent: Float    // 0~1，进播放器时恢复
seekSensitivitySeconds: Int     // 90（默认）
seekPrecisionEnabled: Boolean   // true
volumeSensitivity: Float        // 1.0 = 1屏高100%
brightnessSensitivity: Float
volumeSoftLimitPercent: Float   // 0.5，0 表示关闭
brightnessSoftLimitPercent: Float
softLimitArmedSeconds: Int      // 5
doubleTapSeekSeconds: Int       // 10
longPressSpeed: Float           // 2.0
hapticEnabled: Boolean          // true
// ...每个手势的独立开关

// 外观
themeId: String                 // "mist"
darkMode: DarkMode              // FOLLOW_SYSTEM / LIGHT / DARK
homeLayout: HomeLayout          // GALLERY / DASHBOARD
galleryModeEnabled: Boolean

// IO
prefetchSizeMb: Int             // 48
readBlockSizeKb: Int            // 1024
concurrentReads: Int            // 6

// 其他
resumePolicy: ResumePolicy      // ALWAYS / ASK / NEVER
decoderMode: DecoderMode        // HW / HW_PLUS / SW
imageCacheLimitMb: Int          // 1024
```

### 7.3 字幕匹配算法

```
输入：视频路径，例如 "<VIDEO_SUBDIR>/source.example@DEMO-001_[4K].mkv"

1. base = 去扩展名 → "source.example@DEMO-001_[4K]"
   dir  = "<VIDEO_SUBDIR>"

2. 精确匹配（优先级最高）
   {base}.srt / .ass / .ssa / .vtt / .ttml / .smi
   VobSub 使用 {base}.idx + 同 stem {base}.sub，优先以 .idx 为入口并要求成组存在

3. 语言后缀匹配
   lang ∈ [chs, sc, zh-CN, zh-Hans, zh, cn, 简体, 中文, chi,
           cht, tc, zh-TW, zh-Hant, 繁体, eng, en, jpn, ja, jp]
   {base}.{lang}.{ext} / {base}_{lang}.{ext} / {base}.{lang}.forced.{ext}

4. 前缀模糊匹配（兜底）
   dir 下所有字幕文件中，文件名以 base 开头的；
   多个时按语言优先级排序（简中 > 繁中 > 英 > 日 > 其他）

5. 未命中 → 不加载，提供「手动选择字幕」入口（S-07）

编码识别：
  a) BOM：EF BB BF→UTF-8 / FF FE→UTF-16LE / FE FF→UTF-16BE
  b) 无 BOM → 严格 UTF-8 解码，成功即判定
  c) 失败 → 尝试 GB18030/GBK/BIG5/Shift-JIS/EUC-JP，按中文字符、替换字符和控制字符启发式评分
  d) 结果仍可能误判，UI 必须允许手动指定编码；多编码真实样本尚待动态门禁
```

### 7.4 观看状态判定

```kotlin
fun computeState(positionMs: Long, durationMs: Long, manual: WatchState?): WatchState {
    manual?.let { return it }                       // 手动标记优先
    if (durationMs <= 0) return UNWATCHED
    val p = positionMs.toFloat() / durationMs
    return when {
        p >= COMPLETED_THRESHOLD  -> COMPLETED      // 默认 0.95
        p >= IN_PROGRESS_THRESHOLD -> IN_PROGRESS   // 默认 0.02
        else                       -> UNWATCHED
    }
}
```

> 两个阈值做成高级设置项，默认 2% / 95%。

---

## 8. 页面结构

> 视觉规格见[设计系统.md](./设计系统.md)，手势见[播放器规格 §5](./播放器规格.md#5-手势系统)。

```
① 首页（两种布局可切换 · T-08）
   ├─ A. 画廊直达（默认）
   │    顶部：路径 + 搜索 + 排序 + 视图切换 + ⋮
   │    次行：「继续观看」横滑细卡片
   │    主体：状态筛选 chips + 海报网格
   └─ B. 仪表盘
        库统计 → 继续观看大卡 → 最近新增 → 演员横滑 → 随机抽一部
        底部导航：首页 · 浏览 · 历史 · 设置

② 文件浏览页（列表 / 网格 / 画廊三视图）
   ├─ 顶部：面包屑 · 搜索 · 排序 · 状态筛选 · 视图切换
   ├─ 长按：多选模式 → 「已选 N 项 · XX GB」 → 删除
   ├─ ⋮ 菜单：一键清理 · 刷新元数据 · 目录统计 · 随机抽一部
   └─ 下拉刷新

③ 播放器页（全屏横屏，背景强制纯黑）
   ├─ 顶部栏：返回 · 番号/文件名 · 音轨 · 字幕 · ⋮
   ├─ 中央：手势响应区（全透明）
   ├─ 底部栏：播放/暂停 · 时间 · 进度条(含缓冲区间+书签点) · 倍速 · 比例 · 锁定 · PiP
   ├─ 字幕层：可拖动调垂直位置
   └─ ⋮ 菜单：字幕设置 · 字幕偏移 · 音画同步 · 播放速度 · 旋转镜像 ·
              书签 · 截图 · 切换内核 · 影片详情 · 删除本片 · 调试面板

④ 影片详情页    大封面 + 全部元数据 + 演员列表 + 标签/评分/备注 + 播放/删除
⑤ 演员页 / 演员索引页
⑥ 历史页 · ⑦ 回收站页 · ⑧ 批量清理预览页
⑨ 设置页（11 个分组）
⑩ 首次启动引导（§15.1）
```

---

## 9. 非功能需求（性能指标）

### 9.1 启动与 UI（120Hz 专项）

| 指标 | 目标 | 测量 |
|---|---|---|
| **冷启动到首页可交互** | ≤ **600 ms** | Macrobenchmark `StartupTimingMetric` |
| 温启动 | ≤ 250 ms | 同上 |
| **画廊滚动 P99 帧耗时** | ≤ **8.33 ms** | `FrameTimingMetric`，120Hz 帧预算 |
| **画廊滚动 jank 率** | ≤ **1%** | 滚动 100 个卡片 |
| 页面切换掉帧 | **0 帧** | 同上 |
| 手势响应延迟 | ≤ **1 帧**（8.33 ms） | 480Hz 触控，必须跟手 |
| 主题切换 | 220ms 无闪烁 | 肉眼 + 录屏逐帧 |
| 内存（浏览） | ≤ 250 MB | Profiler |
| 内存（播放 4K） | ≤ 350 MB | Profiler |
| APK 体积 | ≤ **60 MB** | 含 libVLC，仅 arm64-v8a |

### 9.2 播放

| 指标 | 目标 |
|---|---|
| 首帧（1080p / 4K 12.3GB） | ≤ 2.0 s / ≤ 3.0 s |
| Seek 窗口内 / 窗口外 | 瞬时(<200ms) / ≤ 1.5 s |
| 2 小时连播卡顿 | **0 次** |
| SMB 有效吞吐 | ≥ **8 MB/s** |
| 缓冲水位 | 稳定 ≥ 30 秒 |
| 2 小时连播丢帧 | ≤ 10 帧（≈0.005%） |
| 播放 4K 时 CPU | ≤ 8% |
| 播放时屏幕刷新率 | 向系统声明源帧率并记录系统最终选择；不强制假定为 60Hz |
| 播放 1 小时耗电 | ≤ 12% |
| 内核切换耗时 | ≤ 1.5 s |

### 9.3 浏览与元数据

| 指标 | 目标 |
|---|---|
| 列目录（20 文件，首次 / 缓存命中） | ≤ 1.0 s / **≤ 100 ms** |
| 列目录（5000 文件，首次） | ≤ 5.0 s（流式，首屏 ≤ 800ms） |
| 缩略图全量生成（20 个） | ≤ 60 s（后台，不阻塞） |
| `index.json` 解析（20 / 5000 条） | ≤ 200 ms / ≤ 2 s |
| 海报头像（缓存命中 / 首次经 SMB） | ≤ 16 ms / ≤ 500 ms |
| 局域网扫描（/24 网段） | ≤ **3 s**（200 并发，超时 300ms） |
| 删除 12 GB 文件 | ≤ **1.0 s**（rename，无传输） |
| 批量清理预览（100 项） | ≤ 500 ms |
| 雪碧图生成（2.5h 视频） | ≤ 3 分钟（后台低优先级） |

### 9.4 兼容性

Android 8.0 (26) ~ 16 (36) · `arm64-v8a` · 竖屏浏览 + 横屏播放 · 60~120Hz 全范围 · 正确处理挖孔与手势导航安全区（Android 15+ 强制边到边）· 系统字体放大 130% 布局不破。

---

## 10. 验收测试清单

### 10.1 连接与扫描
- [ ] 「扫描局域网」在批准的私网 `/24` 内 ≤3 秒列出测试主机；记录反向 DNS 结果，不要求 NetBIOS/mDNS
- [ ] 选主机 → 输 `<SMB_USER>` + 密码 → 列出共享含 `<SMB_SHARE>`
- [ ] 命名为「家里PC」保存，列表显示该名称
- [ ] 手动添加也能成功
- [ ] 杀 APP 重开配置仍在；adb 查 DataStore 密码为密文

### 10.2 浏览与性能
- [ ] 进 `<VIDEO_SUBDIR>` **只显示 20 个视频**，看不到 `.srt`
- [ ] 开「显示全部文件」→ `.srt` 出现；`.lanplay_meta`/`.lanplay_trash` **始终不显示**
- [ ] 首次列目录 ≤1 秒；**二次进入 ≤100ms**
- [ ] 10 种排序组合正确，**按目录分别记忆**，重启保留
- [ ] 搜索 `START` / 演员名正确过滤
- [ ] 缩略图 60 秒内全出，生成期间**浏览不卡**
- [ ] **冷启动 ≤600ms**（Macrobenchmark 实测，非肉眼）
- [ ] **画廊滚动 P99 ≤8.33ms，jank ≤1%**（Macrobenchmark 实测）
- [ ] 快速甩动 100 个卡片：**无掉帧、无白块、无跳动**
- [ ] **从播放器返回列表，停在原位置不跳顶部**（B-01）

### 10.3 播放核心
- [ ] 4K 12.3GB 文件 **≤3 秒**出画面
- [ ] 连播 **2 小时**卡顿 **0 次**
- [ ] 1080p60 文件流畅无掉帧
- [ ] 调试面板：吞吐 ≥8 MB/s，缓冲 ≥30 秒
- [ ] **解码器名为 `c2.qti.*` 或 `OMX.qcom.*`**（不是 `c2.android.*`）
- [ ] 播放中记录 `Surface.setFrameRate` 声明和 `dumpsys display` 最终模式；退出后清除声明并恢复系统选择
- [ ] 拖到已缓冲位置瞬时；拖到 2 小时处 ≤1.5 秒
- [ ] **拖到文件末尾（>4GB 偏移）正常播放不崩溃**
- [ ] 倍速 1.5x/2.0x 音调正常
- [ ] 锁屏 5 分钟解锁未中断
- [ ] 拔 PC 网线 5 秒插回，自动重连从断点继续
- [ ] 切后台音频继续；PiP 继续播放
- [ ] 切 libVLC 内核，进度与参数全部保留

### 10.4 ★ 手势（重点）
- [ ] **横滑 1 屏宽 = 90 秒**（用 2.5 小时的片子测，手指移动 1cm ≈ 7 秒，不是 11 分钟）
- [ ] 灵敏度改成 30 秒后，同样距离只跳 ~2.3 秒
- [ ] 横滑时上下移动手指，**档位切换有震动，浮层显示「精细/标准/快速」**
- [ ] 浮层显示 `01:23:45 → 01:25:12  (+1:27)`，**松手才 seek**
- [ ] 左半屏上下滑改亮度，右半屏改音量，**互不干扰**
- [ ] **音量升到 50% 卡住 + 震动 + 提示「抬手后再次上滑可继续」**
- [ ] 抬手后 5 秒内再上滑，可继续升到 100%
- [ ] 5 秒后再滑，**重新卡在 50%**
- [ ] **下滑任何时候都不受限**
- [ ] 亮度同上
- [ ] **退出 APP 完全杀掉再进，播放器的音量和亮度与上次相同**（G-07）
- [ ] **换一个视频播放，音量亮度仍相同**
- [ ] 双击左/右 ±10 秒；双击中央播放/暂停
- [ ] 长按 2.0x，松手恢复
- [ ] 双指纵滑调字幕偏移，实时生效，抬手保存
- [ ] 方向锁定：横滑中途上下移动**不会变成音量**
- [ ] 死区：轻微移动不触发手势

### 10.5 字幕
- [ ] 自动加载同名 `.srt`，**零乱码**（UTF-8 无 BOM 简中）
- [ ] 时间轴对齐、换行正确、不重叠不闪烁
- [ ] 手动切 GBK 再切回 UTF-8，立即重载
- [ ] 字号 200% 放大且不出屏
- [ ] 字幕偏移 +2.0 秒，退出重进被记住
- [ ] 字幕开关状态按视频记忆
- [ ] 手动选择字幕能列出同目录所有字幕

### 10.6 观看记录与状态
- [ ] 播 10 分钟正常退出，重进从 10 分钟继续 + Toast
- [ ] **播到 10 分钟，任务管理器强杀，重进进度仍在（误差 ≤3 秒）**
- [ ] 三态角标正确：未看无标记 / 看一半有进度条 / 已看完 ✓ 且卡片变暗
- [ ] 播到 96% 退出标记「已看完」
- [ ] 手动标记「已看完」/「重置为未看」生效
- [ ] 状态筛选数量正确
- [ ] PC 上重命名视频，APP 刷新后**记录自动迁移**
- [ ] 速度 1.5x、旋转 90°、缩放 退出重进全部保留

### 10.7 删除与批量清理
- [ ] 长按删除，弹窗显示文件名、大小、「字幕一并移入」
- [ ] **删除 12.3 GB 文件 ≤1 秒**（验证是 rename 不是复制）
- [ ] PC 上 `.lanplay_trash\` 内视频和 `.srt` 都在，带时间戳前缀
- [ ] 还原后视频和字幕**一起**回到 `<VIDEO_SUBDIR>\`，文件名恢复
- [ ] 多选 3 个批量删除成功
- [ ] **一键清理「已看完」**：预览页列出全部，可取消勾选，显示总数与总大小
- [ ] **一键清理「看了一半」/「两者」**：同上，不重复计数
- [ ] **收藏的视频在预览中默认不出现**
- [ ] 「只清 30 天前看过的」条件生效
- [ ] 执行有确定性进度条，失败项单列
- [ ] 彻底删除红色警告，确认后 PC 上文件真消失
- [ ] 删除后记录、缩略图、海报、雪碧图缓存被清理
- [ ] 断网删除给明确中文报错，不静默失败

### 10.8 元数据与画廊
- [ ] PC 跑完刮削后 `.lanplay_meta\` 生成 `index.json` + `posters\` + `actors\`
- [ ] APP 下拉刷新读到元数据，**番号识别率 ≥90%**
- [ ] 画廊**开**：2 列海报 + 右下头像 + 番号 + 演员名
- [ ] 画廊**关**：变回普通视频列表，无演员元素
- [ ] 已看完卡片 ✓ 角标 + 65% 透明度；看一半有进度条
- [ ] 多演员显示「主演名 他N人」，头像叠放
- [ ] 某片没刮到 → 回退视频截帧，**不报错不留空洞**
- [ ] 头像缺失 → 首字彩色占位圆
- [ ] **`.lanplay_meta` 整个删掉，画廊自动禁用，APP 正常工作**
- [ ] 点演员头像进演员页，显示该演员全部作品
- [ ] 二次进画廊海报秒出（缓存命中）

### 10.9 ★ 音频与蓝牙（重点）
- [ ] **播放中来电 → 自动暂停**
- [ ] **播放中打开其他播放音频的应用 → 自动暂停**
- [ ] **播放中拔掉有线耳机 → 立即暂停，不外放**
- [ ] **播放中蓝牙耳机断开 → 立即暂停**
- [ ] 蓝牙重连 → **不自动播放**，通知栏可继续
- [ ] 耳机上的播放/暂停、上一个/下一个按键有效
- [ ] 锁屏显示封面、标题、进度、控制按钮
- [ ] 音画同步校准：调整实时生效，画面继续播
- [ ] 校准值勾选「记住这个设备」，断开重连后**自动应用**
- [ ] 播放中从蓝牙切到扬声器，**补偿值自动切换 + Toast 提示**
- [ ] 「归零」恢复默认

### 10.10 主题与外观
- [ ] 6 套浅色主题、Ink 深色模式与 Dynamic（支持设备）路径正确，颜色插值 220ms 无闪烁/状态丢失
- [ ] **一键暗黑**：切到墨黑，且**强调色仍是当前主题的色系**（选薄荷 → 暗黑下是薄荷绿）
- [ ] 跟随系统：系统切深色 APP 自动跟随
- [ ] 主题选择器显示**真实迷你预览卡**，不是色点
- [ ] 每套主题的**阴影带各自色调**（暖砂偏暖、海盐偏冷），不是统一黑
- [ ] 顶栏/控制栏/弹层有毛玻璃效果，能透出下方内容
- [ ] **滚动画廊时不掉帧**（红线 R1 生效）
- [ ] 首页布局切换（画廊直达 ↔ 仪表盘）实时生效
- [ ] 播放器界面**始终纯黑**，不跟随浅色主题
- [ ] Android 12 以下降级为半透明纯色，不崩溃不难看

### 10.11 隐私
- [ ] 应用锁：启动需指纹，取消无法进入
- [ ] 多任务界面纯色遮罩；截屏被阻止
- [ ] 图片模糊开关生效
- [ ] 手机相册/文件管理器搜不到本 APP 产生的图片
- [ ] 扫描期间只出现批准的本地子网 445/反向 DNS；停止扫描后只允许当前 SMB 目标与 `127.0.0.1` 回环，**无公网目的地址**
- [ ] APK 中不存在 APP 内公网刮削入口、统计/广告/公网崩溃上报 SDK
- [ ] APK 反编译无统计/广告/崩溃上报 SDK

### 10.12 ★ 单手与观影场景（N 组）
- [ ] **控制栏方向按钮按一下，把手机翻到任意角度（含反向横屏、倒置）画面都不转**
- [ ] 再按一下恢复自动旋转
- [ ] 设置里「进入播放器时的方向」三档（自动/强制横屏/强制竖屏）都生效，**选择被记忆**
- [ ] **系统全局关闭自动旋转时，播放器内「自动」档依然能转**（`SENSOR` 覆盖系统设置）
- [ ] 控制栏方向按钮图标正确反映当前状态 + Toast 提示
- [ ] 手势响应区设为「下 1/2」后，**上半屏滑动不触发任何手势**，只有单击有效
- [ ] 设置中调整手势区时有半透明示意图实时预览
- [ ] **物理音量键能调音量**，且**同样在 50% 卡住**（软上限保护生效）
- [ ] 物理音量键的浮层与手势调节的浮层**样式一致**
- [ ] **长按进度条 400ms 进入精细模式**，拖动速度明显变慢，可定位到秒
- [ ] 双击进度条弹出 0/25/50/75% 快捷点，单击跳转正确
- [ ] **一键静音是切换式**：静音后再按，恢复到静音前的音量（不是默认值）
- [ ] 一键收藏、一键评分即时生效，评分条 1.5 秒后自动收起
- [ ] **惯用手模式**：切到左手，关键按钮集中到左侧；切到右手集中到右侧
- [ ] **播到结尾停在最后一帧**，显示「重看 / 下一个 / 删除本片」三个大按钮
- [ ] 播完 30 秒无操作：开了连播则播下一个，否则**保持停留不退出**
- [ ] **开始播放时画面从黑色淡入 300ms**，不是突然全亮
- [ ] 退出播放器时淡出到黑，不是突然切走
- [ ] 关闭黑场淡入淡出后，行为恢复为立即显示

### 10.13 完整性（§15）
- [ ] 首次启动引导完整走通
- [ ] 12 种空状态全部有设计，文案正确（§15.2）
- [ ] 每个页面返回键行为正确（§15.3）
- [ ] 横竖屏切换状态不丢
- [ ] 权限拒绝后降级正常，不崩溃
- [ ] 系统字体放大 130% 布局不破

---

## 11. 风险与对策

| # | 风险 | 影响 | 对策 |
|---|---|---|---|
| R-1 | **MagicOS 后台管控杀进程** | 播放中断、进度丢失 | ① `FOREGROUND_SERVICE_MEDIA_PLAYBACK`；② 首次运行引导设「不限制后台」；③ **进度每 3 秒落盘，被杀也不丢** |
| R-2 | **毛玻璃与 120Hz 冲突** | 滚动掉帧 | [设计系统 §3.2](./设计系统.md#32--性能红线不可违反) 六条红线，尤其"滚动区不实时模糊" + "滚动中冻结模糊" |
| R-3 | **120Hz 帧预算 8.33ms** | UI 掉帧 | Baseline Profile + Strong Skipping + key/contentType + graphicsLayer + 按尺寸解码。**Macrobenchmark 量化验证** |
| R-4 | 网卡只跑 100 Mbps | 未来放 REMUX 不够 | 提示排查网线/交换机（附录 A-1）。IO 参数可配 |
| R-5 | **单文件 >4 GB 偏移溢出** | 播到 4GB 崩溃 | 所有偏移强制 `Long`，验收专项测试 |
| R-6 | SMB rename 跨共享失败 | 删除报错 | 回收站强制建在共享根；跨共享时明确报错而非静默复制 |
| R-7 | Android 9+ 禁明文 HTTP | 本地代理连不上 | `network_security_config` 仅为 `127.0.0.1` 开 cleartext |
| R-8 | Android 15+ 强制边到边 | 控制栏被系统栏遮挡 | 正确处理 `WindowInsets` + `IMMERSIVE_STICKY` |
| R-9 | 本地端口被其他应用访问 | 内容泄露 | 仅监听 `127.0.0.1` + URL 一次性 token + 退出即关代理 |
| R-10 | libVLC 让 APK 变大 | 体积超标 | 只打 arm64-v8a + R8 full mode + 资源压缩；必要时做按需下载模块 |
| R-11 | **Media3 无音频延迟公开 API** | 蓝牙补偿难实现 | ① 先在 libVLC 上实现验证（`setAudioDelay` 一行）；② 再攻 Media3 自定义 `MediaClock`；③ 受阻则提示用户切 VLC 内核 |
| R-12 | **重复补偿导致更不同步** | 音画偏移变大 | **默认补偿 0**，信任系统 `AudioTrack` 自动同步。预设表只作校准起点参考，**不自动应用** |
| R-13 | 蓝牙 codec 识别与最小权限冲突 | 扩大权限面 | 当前不声明 `BLUETOOTH_CONNECT`，只识别设备大类；未来实现 codec 前必须重新评审权限与降级 |
| R-14 | 探测/缩略图抢播放带宽 | 播放卡顿 | 独立低优先级 SMB Session，并发上限 2，**播放期间自动降到 1** |
| R-15 | Wi-Fi 信号差 | 4K 卡顿 | 调试面板暴露实时吞吐 + 网速测试（O-05），区分是 Wi-Fi 还是 APP 问题 |
| R-16 | 刮削站点需代理/反爬/改版 | 元数据刮不到 | ① 脚本支持代理；② 多源降级；③ **刮削失败不影响 APP 任何核心功能**；④ 支持手动补录 |
| R-17 | 番号识别错误 | 显示错演员 | 清洗规则表（附录 C）+ `index.json` 可人工核对 + APP 内手动修正（M-13） |
| R-18 | **批量清理误删** | 一次损失几百 GB | ① **强制预览页**逐项可取消；② 默认排除收藏；③ 全部进回收站；④ 二次确认 |
| R-19 | 雪碧图生成占用 IO | 首播卡顿 | 后台最低优先级，播放期间暂停生成，可在设置中关闭 |
| R-20 | 主题/Dynamic 对比度不达标 | 看不清 | 脚本化校验 6 套浅色、Ink、Dynamic 与全部文字层级；当前 UI-07 未关闭 |

---

## 12. 待确认事项

| # | 事项 | 当前假设 | 影响 |
|---|---|---|---|
| Q-1 | **PC 上是否有可访问 JavDB/JavBus 的代理** | 假设有，脚本支持 HTTP/SOCKS5 配置 | 无代理则刮削可能失败，需换数据源或手动补录 |
| Q-2 | 手机 Wi-Fi 频段 | 假设 5 GHz | 2.4 GHz 吞吐低但仍够用 |
| Q-3 | 常用的无线耳机型号与编解码 | 假设支持 AAC 或 aptX | 影响延迟补偿的默认建议值 |
| Q-4 | 是否有其他想接入的共享目录 | 假设只有 `<VIDEO_SUBDIR>` | 架构已支持多服务器多目录 |
| Q-5 | 未来是否会有 HEVC/10bit/REMUX 素材 | 假设短期不会 | 若会，libVLC 兜底更重要，需升千兆 |
| Q-6 | 是否需要多设备共享进度 | 假设不需要（W-15 默认关） | 影响记录存储默认值 |
| Q-7 | APP 名称与图标 | 暂定 `LanPlay` | 仅影响命名 |
| Q-8 | 演员中文译名来源 | 假设 JavDB 中文站自带，无则只显示原名 | 影响显示效果 |
| Q-9 | **是否需要「观影舒适组」的其余功能** | 本轮未选，暂不做 | 睡眠定时器、护眼降亮、夜间模式。随时可加，见 §12.1 |

### 12.1 已提出但本轮未选的功能（随时可加，实现成本都不高）

| 功能 | 说明 | 为什么可能有用 | 加进来的成本 |
|---|---|---|---|
| **睡眠定时器** | 「播完本集停」/「N 分钟后停」/「播完队列停」，到点渐弱音量再暂停 | 睡着了不会通宵放，进度也保留 | 半天 |
| **超低亮度模式** | 在画面上叠一层可调半透明黑膜，能降到**比系统最低亮度还暗**。亮度滑到 0 后继续下滑进入超低区 | 黑暗环境里系统最低亮度仍然刺眼，这是暗光看片的常见补救 | 半天，与亮度手势共用一套逻辑 |
| **自动锁定手势** | 播放 N 分钟无操作后自动锁定所有手势，需长按解锁 | 手机搁在身上/枕边时不会被碰到就乱跳 | 半天 |
| **画面位置永久微调** | 把画面整体平移/缩放到看着舒服的位置，作为**全局默认**记住 | 视角不正时不用每次都调 | 半天（缩放平移已在 G-12，只需加"记为全局默认"） |
| 定时降亮度上限 | 到设定时间后亮度手势的上限自动减半 | 深夜误滑到最亮很刺眼 | 2 小时，与软上限共用机制 |
| 暖色滤镜 / 降蓝光 | 画面叠可调强度的暖色层 | 夜间看眼睛舒服 | 2 小时 |
| 播放结束震动提示 | 播完轻震一下 | 戴耳机时知道结束了 | 1 小时 |
| **A-B 循环** | 已在 A-09（**P2，第四阶段**）。若想提前到第二阶段可以说 | 标记一段反复看 | 已排期，只是靠后 |
| **自动跳过片头片尾** | 已在 A-10（**P2，第四阶段**）。同上 | 每次省几十秒 | 已排期，只是靠后 |
| 音量键长按切视频 | 已在 N-06（**P2，默认关**） | 完全不用摸屏幕换片 | 已排期 |

---

## 13. 开发路线图（历史规划）

> 当前源码已经跨越四个历史阶段；本节只用于解释需求演进，不代表当前实现状态。实时状态必须由当前源码、构建证据和《交付验收说明.md》共同核对。写入源码但未动态验证的能力既不能列为“未来未实现”，也不能据此标为通过。
>
> 每阶段结束都应有可安装 APK；阶段门禁只能依赖该阶段及之前能力，全量 §10 仅用于最终候选版本。

### 第 1 阶段：能播（骨架 + IO 核心）— 最关键

- 工程初始化（Kotlin + Compose + Media3 + smbj + Room + Hilt）
- SMB 连接、认证、列目录
- **SMB IO 层**：本地 HTTP 代理 + Range + 流水线 + 预读（[播放器规格 §1](./播放器规格.md#1-smb-io-层)）
- Media3 集成，最简播放器
- 简易文件列表

**验收**：4K 文件 3 秒出画面，连播 30 分钟不卡，吞吐 ≥8 MB/s，**解码器名为 `c2.qti.*`**

> ⚠️ **这一阶段做完必须先实测吞吐和卡顿。不达标先调优，不要往下堆功能。**

### 第 2 阶段：能用（字幕 + 记录 + 删除 + 扫描 + 手势）

- 局域网一键扫描 + 共享列举 + 自定义命名（C-01~C-04）
- 字幕自动匹配 + 编码识别 + 渲染（S-01~S-06）
- 实时观看记录 + 三态 + 续播（W-01~W-07）
- 删除 + 回收站 + 批量清理（D-01~D-10）
- **完整手势系统**（G-01~G-07 为 P0，含软上限与全局记忆）
- **屏幕方向锁定**（N-01，P0 —— 躺姿场景的前提，不做其他都白搭）
- **音频焦点 + 耳机断开暂停**（U-01~U-03）
- 排序 + 默认隐藏字幕文件 + 滚动位置记忆（C-09~C-11, B-01）

**验收**：§10 中全部 P0 项通过

### 第 3 阶段：好看又流畅（设计系统 + 性能 + 画廊）

- **6 套浅色主题 + Ink/Dynamic + 表面层级**；完整毛玻璃和彩色阴影按设计系统真实状态清单推进
- **Baseline Profile + Macrobenchmark**，把启动与 jank 打到指标
- 刷新率适配（P-15）
- PC 端刮削脚本（§14）→ 生成元数据
- 画廊模式 + 详情页 + 演员页（M-01~M-07）
- 首页两种布局（T-08）
- 手势进阶（G-03 精度调节、G-12 缩放、G-13 字幕偏移）
- **单手与场景优化剩余项**（N-02~N-15：手势响应区、物理音量键、进度条精细拖动与快捷跳转、一键静音/收藏/评分、惯用手模式、播完停留界面、黑场淡入淡出）
- **蓝牙延迟补偿 + 校准工具**（U-04~U-07）
- 字幕深度控制（S-07~S-10）
- 缩略图 + 继续观看 + 历史页（C-16, W-09~W-11）
- 后台播放 + PiP + 息屏防断流 + 断线重连（P-10~P-13）
- 隐私套件（V-01~V-05）
- 设置页 + 调试面板（X-01~X-06, P-18）
- **完整性清单**（§15：引导、空状态、错误状态、返回键规范）

**验收**：第 1～3 阶段已实现条目通过，并达到 §9 UI/Macrobenchmark 指标；第 4 阶段能力不作为本阶段前置门禁。全量 §10 仅用于最终候选版本。

### 第 4 阶段：加分项

- libVLC 兜底内核 + ASS 特效（S-12）
- 播放进阶组剩余（A-01 雪碧图预览、A-02 书签、A-06 预缓存等）
- 个人整理组（O-01~O-08）
- 浏览效率组剩余（B-02~B-07）
- 演员索引/筛选/关注（M-08~M-11）
- 音频增强（U-11~U-14）
- 隐私进阶（V-06~V-08）
- 动态取色（T-10）
- 数据备份恢复（X-08）

---

## 14. PC 端刮削工具

> 交付物 ②：`lanplay-scraper`。独立于 APP，**只有它联网**。

### 14.1 定位与选型

双击就能跑完的 Python 脚本：扫描视频目录 → 从文件名提番号 → 联网查演员/封面 → 下载头像 → 生成 APP 能读的元数据。

| 项 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| HTTP | `httpx`（HTTP/2 + 代理支持） |
| 解析 | `selectolax` 或 `beautifulsoup4 + lxml` |
| 图片 | `Pillow`（海报裁 2:3、头像正方形化） |
| 配置 | `config.toml` |
| 启动 | `run.bat`（UTF-8 无 BOM + CRLF，`chcp 65001`，基于 `%~dp0`，结尾 `pause`） |
| 安装 | `install.bat` 一次性创建 venv 装依赖 |

### 14.2 数据源

| 顺序 | 源 | 提供 | 备注 |
|---|---|---|---|
| 1 | **[JavDB](https://javdb.com)** | 番号/标题/日期/时长/片商/系列/类别/演员/封面 | 主力，数据最全，有中文站 |
| 2 | **[JavBus](https://www.javbus.com)** | 同上 + 演员头像 | JavDB 失败时降级 |
| 3 | AVSOX | 无码番号 | 特定类型 |
| 4 | **[Gfriends](https://github.com/gfriends/gfriends)** | **演员头像**（AI 优化、去水印） | 头像专用库，通过 `Filetree.json` 索引查找 |

**策略**：按序尝试，命中即停；头像优先 Gfriends，缺失时回退站点头像。

> 参考实现：[JavSP](https://github.com/Yuukiy/JavSP)、[MDCx](https://github.com/sqzw-x/mdcx) 的番号识别与多源聚合逻辑经过大量实战，遇到难点可对照。

### 14.3 输出结构

```
\\<SMB_HOST>\<SMB_SHARE>\.lanplay_meta\
  ├── index.json          主索引（文件名 → 元数据）
  ├── actors.json         演员反向索引（演员 → 作品列表）
  ├── posters\            2:3 竖版海报（画廊卡片用）
  ├── covers\             横版封面（详情页用）
  ├── actors\             演员头像（正方形）
  ├── failed.txt          失败清单
  └── scraper.log         完整日志
```

**`index.json` 格式**

```json
{
  "version": 1,
  "generatedAt": "2026-07-26T20:00:00+08:00",
  "scanRoot": "<VIDEO_SUBDIR>",
  "stats": { "total": 20, "matched": 18, "failed": 2 },
  "items": {
    "source.example@DEMO-001_[4K].mkv": {
      "code": "DEMO-001",
      "title": "<原标题>", "titleZh": "<中文标题>",
      "releaseDate": "2024-03-15", "runtimeMin": 155,
      "studio": "<片商>", "label": "<厂牌>", "series": "<系列>",
      "genres": ["<类别1>", "<类别2>"],
      "actors": [
        { "name": "<演员名>", "nameZh": "<中文译名>",
          "avatar": "actors/<演员名>.jpg", "isMain": true }
      ],
      "poster": "posters/DEMO-001.jpg",
      "cover": "covers/DEMO-001.jpg",
      "source": "javdb", "scrapedAt": "2026-07-26T20:00:00+08:00"
    }
  },
  "failed": [ { "file": "<文件名>", "reason": "番号未识别" } ]
}
```

### 14.4 处理流程

```
① 读取并严格校验 config.toml
② 递归扫描视频目录，以相对 video_dir 的 POSIX 路径作为索引 key
③ 读取已有 index.json，先剔除已删除/改名条目，再跳过仍有效的成功项（增量模式）
④ 对每个新增或强制刷新的文件：
   a. 清洗文件名 → 提番号（附录 C）
   b. 查数据源（按优先级，带重试与退避）
   c. 下载封面 → Pillow 裁 2:3 海报
   d. 每个演员：查 Gfriends → 下载 → 正方形化
⑤ 生成 index.json + actors.json + 图片
⑥ 输出统计，失败清单写 failed.txt
⑦ pause
```

### 14.5 配置文件

```toml
[paths]
video_dir  = '<VIDEO_DIR>'
output_dir = '<META_DIR>'

[network]
proxy = ''                 # 留空直连；支持 http:// 和 socks5://
timeout = 20
delay_min = 1.0            # 请求间隔，避免触发反爬
delay_max = 3.0
retry = 3

[sources]
javdb = true
javbus = true
gfriends = true

[scrape]
incremental = true
download_poster = true
download_cover = true
download_actor_avatar = true

[output]
poster_max_size = 900
cover_max_size = 1600
avatar_size = 400
jpeg_quality = 88

[gfriends]
index_url = 'https://raw.githubusercontent.com/gfriends/gfriends/master/Filetree.json'
```

### 14.6 功能需求

| 编号 | 优 | 功能 | 验收标准 |
|---|---|---|---|
| SC-01 | P0 | 双击 `run.bat` 即可运行 | 中文路径正常，中文输出不乱码，结束 `pause` |
| SC-02 | P0 | 从文件名提番号 | 批准的版本化样本识别率 ≥90%；附录 C 的冻结 v1 20 项须加入参数化测试后才能写 20/20 通过 |
| SC-03 | P0 | 查询演员、标题、封面 | 识别成功的番号中 ≥90% 查到演员 |
| SC-04 | P0 | 下载封面并裁 2:3 海报 | `posters\{番号}.jpg` 存在且为 2:3 |
| SC-05 | P0 | 下载演员头像 | `actors\{演员名}.jpg` 存在且正方形 |
| SC-06 | P0 | 生成 `index.json` + `actors.json` | 格式符合 §14.3，UTF-8，APP 能正确解析 |
| SC-07 | P0 | 增量模式 | 递归索引使用相对路径；二次运行只处理新增/失效项，并 prune 已删除或改名条目 |
| SC-08 | P0 | 代理配置 | 配置后能正常访问数据源 |
| SC-09 | P0 | 失败不中断 | 单个失败记入 `failed.txt` 并继续 |
| SC-10 | P1 | 数据源自动降级 | JavDB 失败自动试 JavBus |
| SC-11 | P1 | 请求限速与退避 | 随机间隔 1~3 秒，429/503 指数退避 |
| SC-12 | P1 | 进度显示 | `[12/20] DEMO-001 ✓ 演员名` |
| SC-13 | P1 | 手动补录 | `manual.toml` 手动指定「文件名 → 番号」，覆盖自动识别 |
| SC-14 | P1 | 日志文件 | 完整日志写 `scraper.log` |
| SC-15 | — | 计划任务模板（不自动创建） | 当前不实现；只有用户明确授权后才可另行提供 |
| SC-16 | P2 | 清理孤儿元数据 | 视频已删时可选清理对应海报/条目 |

### 14.7 Windows 侧实现约束

- `run.bat` / `install.bat`：**UTF-8 无 BOM + CRLF**，开头 `chcp 65001 > nul`，路径基于 `%~dp0`，结尾 `pause`
- 所有文件写入**显式指定 UTF-8**
- 路径用 `pathlib.Path`，正确处理中文与 `@ [ ] .`
- 不假设当前工作目录，全部基于脚本自身位置
- 输出目录不存在时自动创建

---

## 15. 完整性清单（做 APP 容易漏的）

> 这一节是"功能清单里不会写、但不做 APP 就不完整"的部分。

### 15.1 首次启动引导

```
第 1 屏  欢迎
        LanPlay 图标 + 一句话说明 + 「开始」

第 2 屏  连接你的电脑
        [扫描局域网]（主按钮）
        [手动填写地址]（次按钮）
        ↓ 扫描结果列表 → 选主机 → 输凭据 → 选共享 → 命名

第 3 屏  ★ 保持后台运行（关键，避免 MagicOS 杀进程）
        说明：「为了息屏和切后台时不中断播放，需要允许 LanPlay 后台运行」
        [去设置]（跳转 系统设置→应用→LanPlay→省电策略）
        [稍后]

第 4 屏  外观
        6 套浅色主题 + Dynamic 的迷你预览卡；Ink 由深色模式控制
        暗黑模式：跟随系统 / 浅色 / 深色

第 5 屏  完成
        「都好了」+ [开始使用]
        小字提示：想要海报和演员信息？在电脑上运行刮削脚本
```

**规则**：可随时跳过；引导只出现一次（DataStore 标记）；设置中可「重新查看引导」。

### 15.2 空状态（12 种，文案见[设计系统 §8.1](./设计系统.md#81-空状态清单)）

没有服务器 · 扫不到主机 · 目录为空 · 无视频文件 · 搜索无结果 · 筛选无结果 · 无历史 · 回收站空 · 无元数据 · 演员无作品 · 无收藏 · 批量清理无可清理项。

**每一个都要有**：图标 + 主文案 + 副文案 + 行动按钮。不允许出现空白页。

### 15.3 返回键行为规范

| 页面 | 返回键行为 |
|---|---|
| 首页 | 连按两次退出（第一次 Toast「再按一次退出」） |
| 文件浏览（子目录） | 回上级目录 |
| 文件浏览（根目录） | 回首页 |
| **多选模式** | **退出多选，不返回上页** |
| **搜索激活中** | **关闭搜索，不返回上页** |
| 播放器（正常） | 保存进度 → 返回列表（**恢复原滚动位置**） |
| **播放器（锁定中）** | **无效**（防误触），需先解锁 |
| 播放器（缩放后） | 先重置缩放，再按才返回 |
| 底部弹层 | 关闭弹层 |
| 弹窗 | 取消弹窗（危险弹窗默认为「取消」） |
| 批量清理预览 | 二次确认「放弃本次清理？」 |
| 设置子页 | 回设置主页 |
| 引导流程 | 回上一屏；第一屏则退出 APP |

### 15.4 状态保持

| 场景 | 要求 |
|---|---|
| 横竖屏切换 | 播放位置、滚动位置、搜索词、筛选条件、多选状态**全部保持** |
| 分屏 / 小窗 | 布局正确，播放不中断 |
| 进程被杀后重启 | 恢复到首页；播放进度已落盘（W-02） |
| 系统深色模式切换 | 主题实时跟随，不重建 Activity（`configChanges` 处理） |
| 字体大小变化 | 布局自适应不破 |

### 15.5 权限请求时机与降级

| 权限 | 请求时机 | 拒绝后的降级 |
|---|---|---|
| `POST_NOTIFICATIONS` | 首次开始播放前 | 后台播放仍工作，但无通知栏控制。提示一次，不反复骚扰 |
| `USE_BIOMETRIC` | 用户主动开启应用锁时 | 回退到 PIN 码 |
| `BLUETOOTH_CONNECT` | 当前不申请 | 不读取具体蓝牙 codec；未来实现时重新设计权限说明与降级 |

> 所有权限请求前先用一屏说明**为什么需要**，不直接弹系统框。

### 15.6 首次运行的默认值表

| 项 | 默认值 |
|---|---|
| 主题 | 晨雾 Morning Mist |
| 暗黑模式 | 跟随系统 |
| 首页布局 | 画廊直达 |
| 视图 | 网格（有元数据时自动切画廊） |
| 排序 | 修改时间 · 降序 |
| 显示全部文件 | 关 |
| 续播策略 | 总是自动续播 |
| 解码器 | 硬解 |
| 预读 / 读块 / 并发 | 48 MB / 512 KB / 6 |
| 横滑灵敏度 | 1 屏 = 90 秒 |
| 横滑精度调节 | 开 |
| 音量/亮度灵敏度 | 1 屏 = 100% |
| **音量/亮度软上限** | **50%** |
| 软上限解锁窗口 | 5 秒 |
| 双击步长 | 10 秒 |
| 长按倍速 | 2.0x |
| 震动反馈 | 开 |
| 边缘手势让出 | 开 |
| 蓝牙延迟补偿 | **0 ms**（信任系统） |
| 应用锁 | 关 |
| FLAG_SECURE | **开** |
| 图片模糊 | 关 |
| 图片缓存上限 | 1 GB |
| 回收站自动清理 | 从不；用户可显式选择 7/30/90 天 |
| 自动连播 | 开 |
| Seek 预览缩略图 | 开 |
| **屏幕方向** | **自动**（用户首次锁定后记忆其选择） |
| **手势响应区** | **全屏** |
| **物理音量键调音量** | **开**（受软上限保护） |
| 音量键长按切视频 | 关 |
| 进度条精细拖动 | 开（长按 400ms 触发） |
| **惯用手** | **右手** |
| **播完行为** | **停在最后一帧**（显示 重看/下一个/删除） |
| 播完 30 秒无操作 | 保持停留（除非开了自动连播） |
| **黑场淡入淡出** | **开**（300ms） |

### 15.7 数据与稳定性

| 项 | 要求 |
|---|---|
| Room 迁移 | 每次改表结构必须写 `Migration`，禁止 `fallbackToDestructiveMigration`（会丢观看记录） |
| 数据库备份 | 关键写入（观看记录）用 WAL，保证掉电/被杀不丢已提交事务 |
| 崩溃保护 | 全局 `UncaughtExceptionHandler` 记录到本地日志（不上报），下次启动可查看 |
| 存储不足 | 缓存写入失败时静默降级（不缓存），不崩溃；设置页显示可用空间 |
| 缓存上限 | 图片缓存超限时 LRU 淘汰，不无限增长 |
| 版本号 | `versionName` 语义化 `主.次.修订`；`versionCode` 单调递增 |
| 时间显示 | 全部使用设备本地时区；相对时间（「3 天前」）与绝对时间（「2026-07-23 20:14」）长按可切换 |

### 15.8 图标与品牌资源

| 资源 | 要求 |
|---|---|
| 应用图标 | 自适应图标（前景 + 背景层） + **主题化图标**（Android 13+ 单色版） |
| 启动图 | 用 Android 12+ SplashScreen API，纯色底 + 图标，**不做动画**（与"启动极快"冲突） |
| 通知图标 | 纯白剪影 24dp |
| 快捷方式 | 长按应用图标 → 「继续观看」直达最近未看完的视频（P2） |

---

## 附录 A：服务端调优建议

> 在 PC 上做，与 APP 开发无关，**都是可选的**。

### A-1 排查百兆瓶颈（建议做）

网卡是 2.5GbE 却只跑 100 Mbps，按序排查：① 换 Cat 5e/Cat 6 网线 ② 确认路由器/交换机是千兆口 ③ 验证：

```powershell
Get-NetAdapter | Where-Object Status -eq 'Up' | Select-Object Name, LinkSpeed
```

目标看到 `1 Gbps` 或 `2.5 Gbps`。**收益**：余量从 8 倍变 80 倍。

### A-2 创建专用 SMB 账户（建议做）

```powershell
# 管理员身份运行
$pw = Read-Host -AsSecureString '为 smbplay 账户设置密码'
New-LocalUser -Name 'smbplay' -Password $pw -PasswordNeverExpires -AccountNeverExpires -Description 'SMB 播放器专用账户'
Add-LocalGroupMember -Group 'Guests' -Member 'smbplay' -ErrorAction SilentlyContinue
```

收紧共享权限（可选）：

```powershell
Revoke-SmbShareAccess -Name '<SMB_SHARE>' -AccountName 'Everyone' -Force
Grant-SmbShareAccess -Name '<SMB_SHARE>' -AccountName 'smbplay' -AccessRight Full -Force
```

> ⚠️ 执行前确认没有其他设备依赖 `Everyone` 权限。

### A-3 SMB 服务端微调（可选）

服务端策略必须在目标环境重新采集。客户端默认要求 SMB 消息签名；不得为提升吞吐自动降级签名。只有用户显式选择兼容模式并看到风险提示时，才可连接不支持签名的旧服务器。如遇性能瓶颈，先采集证据：

```powershell
Set-SmbServerConfiguration -MaxThreadsPerQueue 64 -Force
Get-SmbServerConfiguration | Select-Object EnableMultiChannel, EnableLeasing, EnableSMB2Protocol
```

### A-4 防火墙确认

```powershell
Get-NetFirewallRule -DisplayGroup '文件和打印机共享' | Where-Object Enabled -eq 'True' | Select-Object DisplayName, Profile, Direction
```

### A-5 固定 PC 的局域网 IP（建议做）

若 `<SMB_HOST>` 由 DHCP 分配，路由器重启后可能变化。推荐在路由器按 MAC 绑定固定地址；APP 的局域网扫描是兜底手段。真实 IP 不进入公开文档。

---

## 附录 B：术语表

| 术语 | 解释 |
|---|---|
| **SMB** | Windows 的文件共享协议，就是右键"共享此文件夹"用的那个 |
| **SMB1/2/3** | 版本。SMB1 是 90 年代老协议有漏洞，Windows 现在默认关闭。你的电脑关了 SMB1，所以只支持 SMB1 的老播放器连不上 |
| **码率** | 视频每秒占多少数据。10 Mbps ≈ 每秒 1.25 MB |
| **H.264 / HEVC** | 视频压缩格式。H.264 兼容性最好，所有手机都能硬解。**你的文件全是 H.264** |
| **硬解 / 软解** | 硬解 = 专用解码芯片，省电流畅；软解 = CPU 硬算，费电发烫 |
| **8bit / 10bit** | 色彩精度。**你的是 8bit，兼容性最好** |
| **Seek** | 拖动进度条跳转 |
| **预读 / 缓冲** | 提前把后面的内容下载到内存。**解决卡顿的核心手段** |
| **流水线** | 不等上个请求返回就发下一个。像点外卖一次点 5 家 vs 吃完一家再点。**打满带宽的关键** |
| **RTT** | 请求发出到收到回复的时间，局域网约 1-3 毫秒 |
| **Range 请求** | HTTP 里"只要文件第 X 到 Y 字节"的语法。拖进度条不用下整个文件靠它 |
| **Media3 / ExoPlayer** | Google 官方安卓播放器库，YouTube 用的就是它 |
| **libVLC** | VLC 的核心库，格式支持最全 |
| **libass** | 渲染 ASS/SSA 特效字幕的库。Media3 不带，libVLC 自带 |
| **SRT / ASS** | 字幕格式。SRT 最基础（序号+时间+文字，**你的都是这个**），ASS 带特效 |
| **BOM** | 文件开头几个特殊字节，标记编码。**你的字幕是 UTF-8 无 BOM** |
| **GBK / GB18030 / UTF-8** | 中文编码。用错就是乱码 |
| **Room** | 安卓官方本地数据库库 |
| **WAL** | 数据库"预写日志"模式，写入立即落盘。**杀后台不丢进度靠它** |
| **Keystore** | 安卓硬件级密钥保险箱，加密存密码用 |
| **FLAG_SECURE** | 禁止截屏，多任务界面显示遮罩 |
| **前台服务** | 带通知栏的常驻服务，切后台保持播放不被杀 |
| **PiP** | 画中画，视频缩成小窗浮在其他应用上 |
| **WifiLock** | 告诉系统"我在传大数据别省电"。**不加息屏会断流** |
| **音频焦点** | 安卓的音频仲裁机制。来电或其他应用播音时，你的应用应该让路（暂停） |
| **A2DP** | 蓝牙立体声音频协议 |
| **SBC/AAC/aptX/LDAC/LC3** | 蓝牙音频编解码。延迟从高到低：SBC > LDAC > AAC > aptX > LC3 |
| **音画同步 / 延迟补偿** | 蓝牙传输有延迟，声音比画面慢。补偿就是把画面也延后一点对上 |
| **刷新率 / Hz** | 屏幕每秒画多少次。120Hz = 每帧只有 8.33 毫秒 |
| **帧预算** | 每帧允许消耗的最长时间，超了就掉帧 |
| **jank** | 掉帧率。目标 ≤1% |
| **判读抖动 (Judder)** | 视频帧率与刷新率不整除时的周期性微抖。**120Hz ÷ 29.97 ≈ 4，整除，所以你不会遇到** |
| **Baseline Profile** | 安卓的"预编译清单"，告诉系统哪些代码提前编译成机器码。**启动快 20-40%** |
| **Macrobenchmark** | 安卓官方性能测试框架，量化测启动时间和掉帧率，不靠肉眼 |
| **重组 (Recomposition)** | Compose 里 UI 重新计算的过程。重组太多 = 掉帧 |
| **LazyGrid** | Compose 的高性能列表，只渲染看得见的项 |
| **图片缓存/解码** | 当前使用应用自有 AES-GCM 缓存和 `BitmapFactory` 采样解码；未引入 Coil |
| **RenderEffect** | Android 12+ 的硬件加速特效 API，毛玻璃模糊靠它 |
| **雪碧图 (Sprite Sheet)** | 把很多小缩略图拼成一张大图。拖进度条的预览图用它，一次加载全部 |
| **刮削 (Scrape)** | 从网上抓取影片信息并整理到本地。Kodi/Jellyfin/Emby 都有 |
| **番号** | 影片编号如 `DEMO-001`，刮削靠它匹配 |
| **Gfriends** | 开源演员头像库项目，AI 优化过低清头像，主流刮削器都在用 |
| **APK / 侧载** | APK 是安卓安装包；侧载 = 不走应用商店直接装 |
| **ABI (arm64-v8a)** | CPU 架构，现在手机都是 arm64，只打包这一个 APK 小一半 |

---

## 附录 C：番号识别规则表

> 基于当前 20 个文件的实际命名归纳。PC 端脚本（SC-02）按此实现。

### C-1 清洗步骤

```
① 去扩展名
② 去下载站前缀   ^[0-9a-zA-Z]+\.(com|net|xyz|cc|me|tv|org|to|io)@
   "source.example@DEMO-001_[4K]"  → "DEMO-001_[4K]"
③ 去下载站后缀   [-_][0-9a-zA-Z]+\.(com|net|xyz|cc|me|tv|org)$
   "DEMO-003_CH-source.example"  → "DEMO-003_CH"
④ 去画质标记（不分大小写）
   _\[4K\] / \[4K\] / _4K / _4KS / -4K / _FHD / _HD / -HD
   _1080p / -1080p / _720p / _2160p / _UHD
⑤ 去版本后缀（不分大小写，仅结尾）
   -C / _C / -CH / _CH          （中文字幕）
   -U / _U / -UC / -uncensored  （无码流出）
   -leak / _leak / -hack / _hack
   -x264 / -x265 / -HEVC
⑥ 提番号        ([A-Za-z]{2,7})[-_ ]?(\d{2,5})
⑦ 规范化        字母大写 + 连字符   →  "DEMO-001"
```

### C-2 当前 20 个文件的预期结果

| 原文件名 | 番号 |
|---|---|
| `source.example@DEMO-001_[4K].mkv` | `DEMO-001` |
| `source.example@DEMO-006_[4K].mkv` | `DEMO-006` |
| `source.example@DEMO-007_[4K].mkv` | `DEMO-007` |
| `source.example@DEMO-008_[4K].mkv` | `DEMO-008` |
| `source.example@DEMO-009_[4K].mkv` | `DEMO-009` |
| `source.example@DEMO-010_[4K].mkv` | `DEMO-010` |
| `source.example@DEMO-011-U.mp4` | `DEMO-011` |
| `source.example@DEMO-012.mp4` | `DEMO-012` |
| `source.example@DEMO-013.mp4` | `DEMO-013` |
| `source.example@DEMO-014.mp4` | `DEMO-014` |
| `source.example@DEMO-015.mp4` | `DEMO-015` |
| `source.example@DEMO-002.mp4` | `DEMO-002` |
| `source.example@DEMO-005_4KS.mp4` | `DEMO-005` |
| `DEMO-003_CH-source.example.mp4` | `DEMO-003` |
| `DEMO-016_CH-source.example.mp4` | `DEMO-016` |
| `source.example@DEMO-017.mp4` | `DEMO-017` |
| `source.example@DEMO-018.mp4` | `DEMO-018` |
| `source.example@DEMO-019.mp4` | `DEMO-019` |
| `source.example@DEMO-020.mp4` | `DEMO-020` |
| `source.example@DEMO-004.mp4` | `DEMO-004` |

> 下表是冻结 v1 样本的预期映射，不是当前自动测试结果。将 20 个文件名加入参数化测试并实际运行前，状态保持“待验证”。

### C-3 特殊番号格式（未来素材可能遇到）

| 类型 | 格式 | 正则 | 数据源 |
|---|---|---|---|
| FC2 | `FC2-PPV-1234567` | `FC2[-_ ]?(PPV)?[-_ ]?(\d{6,8})` | JavDB |
| HEYZO | `HEYZO-1234` | `HEYZO[-_ ]?(\d{4})` | AVSOX |
| Caribbean | `CARIB-123456-789` | `(\d{6})[-_](\d{3})` | AVSOX |
| 1Pondo | `1PON-123456_789` | `(\d{6})[-_](\d{3})` | AVSOX |
| 素人 | `300MAAN-123` | `(\d{3}[A-Z]{3,6})[-_]?(\d{2,4})` | JavDB |
| 带前缀数字 | `T28-123` | `([A-Z]\d{1,3})[-_]?(\d{2,5})` | JavDB |

---

## 变更记录

| 版本 | 日期 | 内容 |
|---|---|---|
| v1.0 | 2026-07-26 | 初版。基于本机实测（ffprobe / SMB / 网络 / 权限）与首轮需求对齐 |
| v2.0 | 2026-07-26 | 交付物增为 2 个（APK + 刮削脚本）；新增演员元数据与画廊；确认设备与开发环境；局域网扫描提至 P0；新增按状态一键批量清理；观看记录改 3 秒写入；三态判定；番号识别规则表 |
| **v3.0** | 2026-07-26 | **① 刷新率修正**：185Hz → **实际可用 120Hz**（帧预算 8.33ms），判读抖动问题不存在，帧率适配降为省电优化<br>**② 文档拆三份**：新增 [设计系统.md](./设计系统.md)（7 套主题完整色板、毛玻璃规范与性能红线、排版/彩色阴影/动效/组件/空错误状态）与 [播放器规格.md](./播放器规格.md)（SMB IO、双内核、解码最大化、蓝牙延迟补偿、完整手势系统、调试面板）<br>**③ 编号体系重构**：F-xx 改为按模块字母前缀（C/P/G/S/W/D/M/B/A/O/U/T/V/X/SC）<br>**④ 新增手势系统 G 组 20 项**：横滑绝对时长映射解决"幅度太大"、垂直偏移调精度、**音量亮度软上限保护**、**全局记忆**<br>**⑤ 新增音频 U 组 14 项**：音频焦点、耳机断开暂停、**蓝牙延迟补偿+校准工具+按设备记忆**<br>**⑥ 新增主题 T 组 10 项**：7 套主题、一键暗黑保留个性、毛玻璃、首页布局可切换<br>**⑦ 新增 B/A/O 三组 25 项**：浏览效率、播放进阶、个人整理<br>**⑧ 新增 §15 完整性清单**：首次引导、12 种空状态、返回键规范、状态保持、权限时机、默认值表、数据稳定性、品牌资源<br>**⑨ 新增解码性能最大化专章**（播放器规格 §3）与竞品借鉴清单（§7）<br>**⑩** 风险扩至 20 条，功能总数约 **150 项** |
| **v3.1** | 2026-07-26 | 新增 **§6.15 N 组「单手与观影场景优化」15 项**：<br>· **屏幕方向锁定**（N-01，**P0**，提到第二阶段）—— 手持观看的前提<br>· 手势响应区可调（N-03）—— 只在指定区域响应，杜绝手掌误触<br>· 物理音量键调音量（N-05）+ 软上限保护联动<br>· 进度条精细拖动（N-07）与快捷跳转（N-08）<br>· 一键静音（切换式）/ 收藏 / 评分（N-09~N-11）<br>· **惯用手模式**（N-12）—— 按钮向单侧集中<br>· **播完停在最后一帧**并显示「重看/下一个/删除本片」（N-13~N-14）<br>· **黑场淡入淡出**（N-15）—— 暗环境不刺眼<br>同步更新：验收清单新增 §10.12（18 项）、默认值表新增 10 项、路线图第 2/3 阶段、§12.1 未选功能表补全 10 项并标注实现成本。功能总数约 **165 项** |
| **v3.2** | 2026-07-28 | 对齐当时源码与安全加固：区分规格/状态/证据，统一 APP 公网边界、SMB 签名、回环代理、VobSub、音频焦点、刷新率和阶段门禁；环境与素材改为历史冻结基线；技术栈更新到当时依赖；文档示例全部脱敏。 |
| **v3.3** | 2026-07-29 | 清除对旧审查报告和修复清单的依赖；新会话从当前源码、构建输出和交付证据独立建立结论。规格继续只定义目标，不承载审查状态。 |
| **v3.4** | 2026-07-29 | 对齐当前数据库恢复、VLC 原生字幕边界与隐私参数化实现；移除已清理的历史状态入口，动态门禁仍不冒充已通过。 |
